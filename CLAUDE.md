# webtrade — مرصد الأسهم الأمريكية

تطبيق ويب عربي (RTL) لأكبر 70 شركة أمريكية: أسعار، شارت شموع، مؤشرات فنية،
أساسيات، مزاج السوق، أخبار، حاسبة مخاطرة، تنبيهات. بلا أي مكتبة خارجية.

**المرجع الكامل: `SPEC.md`** — اقرأه قبل أي تغيير معماري.

---

## ⚠️ أهم شيء: مشروع واحد في مكانين

هذا المشروع يعيش في موضعين، وهما **نسختان من نفس المستودع لا مشروعان**:

| الموضع | الدور |
|---|---|
| `D:\Ai\ClaudeCode\trade\webtrade` (محلي) | التطوير والتشغيل الفعلي |
| `github.com/Alkongrs2014/Alkongrs2014` | مصدر الحقيقة + نشر الصفحة |

**قواعد ملزمة لمنع التباعد:**

1. **الفرع الافتراضي `main` هو مصدر الحقيقة.** أي تغيير يُدمج فيه.
2. **ابدأ كل جلسة بـ `git pull`** وانتهِ بـ `git push`. لا تترك عملاً
   غير مدفوع — الموضع الآخر لن يراه.
3. **لا تعدّل نفس الملف في الموضعين دون مزامنة بينهما.** لو شككت،
   `git status` و`git log --oneline -5` أولاً.
4. **مجلد `data/` و`.env` محليان فقط** — مستبعدان في `.gitignore`
   عمداً. `data/` بيانات متولّدة، و`.env` يحوي مفاتيح.
5. فرع `data` في GitHub منفصل تماماً عن `main` (orphan) — تكتبه مهام
   GitHub Actions. **لا تدمجه في `main` أبداً.**

### الفرق الوحيد المقصود بين الموضعين
متغيّر `PREFER_YAHOO`:
- محلياً `=1` → Yahoo أولاً (غير محظور، بلا سقف، توازٍ)
- على GitHub غير مضبوط → Twelve Data أولاً بميزانية محدودة

نفس الشيفرة، سلوك مختلف حسب الشبكة. **لا تفرّع الشيفرة لنسختين.**

---

## البنية

```
stocks/index.html      التطبيق كاملاً (5 تبويبات: الآن، الأسهم، الفرص، تحليل، الأخبار)
stocks/config.js       الإعدادات — يكتشف مكانه تلقائياً (localhost أم ويب)
stocks/symbols.json    كون الرموز بثلاث طبقات (انظر «الطبقتان» أدناه)
scripts/build-universe.mjs أداة صيانة تبني symbols.json وتتحقق من كل رمز
scripts/fetch-quotes.mjs   أسعار فقط — الحلقة السريعة (كل دقيقتين)
scripts/fetch-market.mjs   أسعار + شموع + مؤشرات (كل 10 دقائق)
scripts/fetch-news.mjs     أخبار عربية ومترجمة (مع كل تحديث سوق)
scripts/fetch-daily.mjs    أساسيات + ترتيب (يومي)
scripts/lib/news.mjs       خلاصات RSS + الترجمة وذاكرتها
scripts/lib/session.mjs    حالة جلسة السوق — مشتركة بين المهمتين
scripts/lib/finnhub.mjs    أسعار لحظية وأساسيات
scripts/lib/twelvedata.mjs شموع (أساسي سحابياً)
scripts/lib/yahoo.mjs      شموع (أساسي محلياً) + Stooq احتياطاً
scripts/lib/indicators.mjs المؤشرات — مشتركة بين الخادم والمتصفح
scripts/lib/options.mjs    Black–Scholes والجريكس واستخراج التقلّب الضمني
stocks/plan.js             نواة الخطة — مشتركة بين المتصفح والخادم
stocks/evaluate.js         الطزاجة والدخول والزمن والآفاق — مشتركة كذلك
stocks/scans.js            شروط الماسح — نسخة واحدة للمتصفح والخادم
stocks/strategies.js       ماسح الاستراتيجيات العشر — بواباتٌ بإيقاعين (مشترك)
stocks/consensus.js        الإجماع وحالة السوق والتعارض (مشترك)
stocks/confluence.js       درجة التوافق الموحّدة — من الإجماع إلى رقمٍ موقَّع (مشترك)
scripts/market-direction.mjs توجّه السوق — المؤشّر وأكبر 15 على ثلاثة فريمات (بلا شبكة)
stocks/session.js          الجلسات والتقويم والتوقيت — مشترك، والمرساة فيه
scripts/providers/         طبقة المزوّد: alpaca . yahoo . binance . finnhub
scripts/lib/env.mjs        تحميل .env من أيّ مدخل
scripts/lib/log.mjs        سجلّ التشغيل بتوقيت الرياض
scripts/replay.mjs         إعادة تشغيل يومٍ بلا نظرٍ إلى المستقبل
scripts/audit-missed.mjs   تقرير الفرص الفائتة — قديم مقابل جديد
scripts/check-session.mjs  فحص التوقيت الصيفي والعطلات وأنصاف الأيام
scripts/backtest.mjs       الأرشيف التاريخي — سجلّ كل شرط على خمس سنوات
scripts/track-signals.mjs  السجلّ الحيّ — يثبّت ما ظهر فعلاً ويتابعه
scripts/fetch-events.mjs   الأحداث القوية — تقويم الفدرالي الرسمي
scripts/fetch-options.mjs  عقود الخيارات — دورة نصف ساعة مستقلة
scripts/analytics.mjs      قوة نسبية وبيتا وارتباط وفجوات — اشتقاقٌ بلا جلب
scripts/fetch-filings.mjs  إيداعات SEC — 8-K وتداول المطّلعين (دورة عشر دقائق)
scripts/lib/sec.mjs        EDGAR: تيار الإيداعات وخريطة CIK وتفصيل Form 4
stocks/score.js            نواة النتيجة والنطاقات — نسخة واحدة للمتصفح والخادم
stocks/indicators.js       المؤشرات كلها + ADX والتباعد والانضغاط — مشتركة كذلك
scripts/check-ui.mjs       فحص الواجهة — نحوٌ ومعرّفات وتبويبات ونسخٌ مكرّرة
local/run.mjs          المشغّل المحلي (env + جلب + خادم + نشر)
local/*.bat            اختصارات ويندوز
```

---

## الأوامر

```bash
# الفحص الذاتي لكل مهمة — بلا شبكة، شغّلها كلها قبل أي دفع
node scripts/fetch-quotes.mjs --check
node scripts/fetch-market.mjs --check
node scripts/fetch-news.mjs   --check
node scripts/fetch-daily.mjs  --check
node scripts/fetch-options.mjs --check
node scripts/fetch-filings.mjs --check
node scripts/backtest.mjs      --check
node scripts/track-signals.mjs --check
node scripts/fetch-events.mjs  --check
node scripts/build-universe.mjs --check
node scripts/analytics.mjs     --check
node scripts/check-ui.mjs                 # الواجهة: نحوٌ ومعرّفات وتبويبات
node scripts/check-session.mjs            # التوقيت الصيفي والعطلات والمرساة
node scripts/replay.mjs        --check    # منع النظر إلى المستقبل
node scripts/audit-missed.mjs  --check

node local/run.mjs quotes                # أسعار فقط (~90 ثانية)
node local/run.mjs market                # شموع ومؤشرات + أخبار
node local/run.mjs news                  # أخبار وحدها
node local/run.mjs options               # عقود الخيارات (دورة نصف ساعة)
node local/run.mjs filings               # إيداعات SEC (بلا مفتاح ولا حصّة)
node local/run.mjs backtest              # الأرشيف التاريخي (يومياً، ~500 طلب)
node local/run.mjs signals               # تثبيت إشارات اليوم (بلا شبكة)
node local/run.mjs events                # تقويم الفدرالي (يومياً)
node local/run.mjs learn                 # قراءة السجل واقتراحاته (بلا شبكة، مع daily)
node local/run.mjs analytics             # قوة نسبية وارتباط وفجوات (طلب واحد، مع daily)
node local/run.mjs both                  # كل شيء
node local/run.mjs serve                 # خادم على localhost:8080
node local/run.mjs publish               # نشر data/ على فرع data
node local/run.mjs quotes --publish      # جلب ثم نشر (النشر بشرط نجاح الجلب)
```

---

## معرفة تشغيلية مكتسبة بالتجربة

> هذه ليست آراء — كل بند كلّف تشخيصاً فعلياً. لا تعِد اكتشافها.

### مصادر البيانات — الواقع لا التوثيق
| المصدر | الحقيقة المُختبَرة |
|---|---|
| Yahoo | ⛔ يرفض عناوين السحابة بـ429 · ✅ يعمل منزلياً |
| Finnhub مجاني | أسعار وأساسيات ✅ · توزيع التوصيات وصفقات المطّلعين ✅ · **الشموع والتقويم الاقتصادي 403** · رموز `^GSPC` مرفوضة |
| Twelve Data مجاني | شموع ✅ · **8/دقيقة و800/يوم** |
| Stooq | ⛔ يعيد صفحة حظر **بحالة 200** لا 429 |
| تقويم الفدرالي | ✅ `federalreserve.gov/json/calendar.json` بلا مفتاح ولا حصّة |
| مكتب إحصاءات العمل (BLS) | ✅ **بترويسات متصفح كاملة** · 403 بترويسة ناقصة — لا يحجب الآلات بل الطلب الذي لا يشبه المتصفح |
| خيارات ياهو | ✅ بالـcrumb · **يصفّر bid/ask/IV خارج الجلسة** (انظر أدناه) |
| SEC EDGAR | ✅ بلا مفتاح ولا حصّة · **بترويسة `User-Agent` تعرّف بالمستهلك** · حدّه 10/ثانية · نفس درس BLS |

### فرضيات جُرّبت وثبت خطؤها — لا تكرّرها
1. ❌ «التراجع الطويل يحل 429» — يعلّق التشغيل 20 دقيقة بلا فائدة.
   الحظر ممتد لا عابر؛ **الفشل السريع أفضل** لأنه يُبقي آخر بيانات سليمة.
2. ❌ «ترويستا Referer/Origin تقللان الرفض» — **العكس**. إرسال `Origin`
   من الخادم يجعل الطلب يبدو تجاوزاً لـ CORS.
3. ❌ «بصمة TLS هي السبب» — `curl` نجح مرة ثم فشل؛ كان حظ عنوان IP.

### الطبقتان — لماذا لا كون واحد
| الطبقة | العدد | ما يُجلب | الدورة |
|---|---|---|---|
| الأساسية `symbols` | 90 مرشّحاً، يُختار أعلى 70 | 15د + ساعة + يومي، أساسيات، أخبار | 10 دقائق |
| `crypto` | 26 عملة | نفس الفريمات الثلاثة | 10 دقائق |
| `wide` | 414 (بقية S&P 500) | **اليومي وحده** | يومياً، بسقف `WIDE_PER_RUN` لكل تشغيل |

جلب 510 رموز × 3 فريمات كل عشر دقائق = 1500 طلب/دورة، وهو ما يستدعي 429
حتى من شبكة منزلية. اليوميُّ وحده يكفي للبحث ولمستويات الدعم والمقاومة
و52 أسبوعاً — وهو كل ما يُطلب من رمز خارج المرصودة.

- **`wide.json` تراكمي** — كل تشغيل يجدّد حصّته ويدمجها فوق القديم. بوابة
  سلامة ترفض كتابته أقصر مما كان.
- **`u` في صفوف `wide.json` عمر الشمعات لا عمر السعر.** `fetch-quotes`
  يحدّث السعر ولا يمسّ `u` — لمسه يجعل `fetch-market` يظنّ اليوميّ حديثاً
  فلا يجدّده أبداً.
- **الكريبتو خارج إحصاء السوق** (الاتساع، المزاج، القطاعات، الرابحون
  والخاسرون). مداه اليومي أوسع بمراتب، فبيتكوين وحده يزيح المتوسط ويحتل
  القوائم كل يوم. وفترات التداول تُؤخذ من رمز أمريكي حصراً، وإلا قالت
  الترويسة «السوق مفتوح» ليل السبت.
- **الأسماء العربية للأساسية والكريبتو فقط.** الطبقة الواسعة تعرض اسمها
  الإنجليزي: الترجمة الآلية (MyMemory) تشوّه أسماء الشركات، والإنجليزي
  الصحيح أصدق من عربي مفبرك. `nm(r)` في الواجهة تتكفّل بالسقوط.
- **البحث يعمّ الكون كاملاً بمجرّد كتابة نص** حتى والمُبدّل على «المرصودة»
  — من كتب «بيتكوين» يقصده، وحصر البحث افتراضياً يعيد «لا نتائج» عن رمز
  موجود.

### ثلاث دورات لا دورة واحدة
| المهمة | الدورة | لماذا |
|---|---|---|
| `fetch-quotes` | دقيقتان | السعر وحده. 70 رمزاً × طلب = ~90 ثانية عند حد 60/دقيقة |
| `fetch-market` | 10 دقائق | الشمعة المكتملة لا تتغيّر كل دقيقة، وإعادة جلبها تزاحم الأسعار على نفس الحصّة |
| `fetch-daily` | يومياً | الأساسيات تتغيّر مرة كل ربع سنة |
| `fetch-filings` | 10 دقائق | الإيداعات تصل على مدار الساعة، وEDGAR بلا حصّة فلا ميزانية تُزاحم |

**لا تحسب المؤشرات في `fetch-quotes`.** تحديث السعر بلا تحديث الشمعة لا
يغيّر RSI ولا MACD فعلاً، وحسابها من سعر واحد يعطي رقماً كاذباً.

### مصائد كامنة أُصلحت — احذر إعادتها
- **الشمعات تُحفظ مضغوطة** `[t,o,h,l,c,v]` وتحتاج `unpackCandles` عند
  القراءة. بدونها: `Cannot read properties of undefined (reading 'toFixed')`.
- **صلاحية فريم 15د صفر** = «قديم دائماً». لذا تُرتَّب المعالجة حسب
  الحاجة لا القيمة السوقية، وإلا احتكرت الرموز الممتلئة الميزانية.
- **عدّاد قاطع الدورة لـ Yahoo وحده** — Stooq يعيد 200 فيصفّره لو شملته.
- **لا تبتلع الأخطاء** — `catch {}` في حلقة على 70 رمزاً يخفي السبب.
  اطبع أول خطأ فعلي وعدده.
- **اعرض `—` لا رقماً ملفّقاً** عند غياب أي حقل. الشمعة الناقصة تُتخطّى.
- **`isFinite` العالمية تحوّل `null` إلى صفر** فتقبله كرقم صالح. استعمل
  `Number.isFinite`. سهم بلا سعر كان يدخل متوسط قطاعه كـ«تغيّر 0%».
- **`normPct` لِعائد التوزيعات وحده.** ROE ونمو الإيراد والهامش كسور
  تتجاوز الواحد بشكل مشروع (ROE إنفيديا 1.17 = 117%)، فتُضرب في 100
  دائماً عبر `asPct`. `normPct` كانت تعيدها كما هي فتُعرض «1%».
- **`market.json` لا يحفظ `proxy` إلا حين يُستعمل**، فمصدر بدائل
  المؤشرات هو `symbols.json` لا الملف المحفوظ — وإلا تجمّدت المؤشرات.
- **فترات التداول المحفوظة تصف يوم جلبها.** استعمالها في اليوم التالي
  يعطي «بعد الإغلاق» والسوق مفتوح. `statusNow` ترجع للتقدير حينها.

- **صلاحيةٌ ثابتة للفريم اليومي تُجمّد مستويات اليوم كلَّه — بلّغ عنها
  المستخدمون قبل أن يكشفها فحص.** كانت 20 ساعة، و20 لا تقسم 24، فوقتُ
  الجلب ينزلق أربع ساعات للخلف كل يوم: يقع داخل الجلسة أياماً وقبل
  الافتتاح أياماً. يوم 2026-09-11 وقع 11:37 UTC — قبل الافتتاح بساعتين —
  فبقي **487 رمزاً من 510** على شمعة أمس طوال الجلسة.
  - والأسوأ أن العلّة **تتضاعف في `levelsFrom`**: البيفوت كانت تُؤخذ من
    `d[d.length - 2]` بافتراض أن آخر شمعة هي شمعةُ اليوم قيد التكوّن.
    فحين تغيب شمعة اليوم تقفز إلى **أول أمس** — بيفوت META خرجت 661.51
    بدل 657.87، فبدت أهدافُها محقَّقةً عند الافتتاح.
  - **لا شيء يبدو معطلاً**: السعر حيّ وفريم 15د حيّ والشارت يتحرّك،
    والمستويات وحدها متحجّرة. نفس مصيدة «الأرقام تبدو صحيحة وتخصّ سهماً
    آخر» مطبَّقةً على الزمن بدل الرمز.
  - الحلّ شقّان: `pivotBar` تختار آخر يوم **بالتاريخ** لا بالموضع (فتصحّ
    مع البيانات الناقصة والطازجة وعطلة نهاية الأسبوع معاً)، و
    `DAILY_LIVE_AGE` تقصّر صلاحية اليومي إلى نصف ساعة **أثناء الجلسة**.
  - `POST` مشمولة في «الجلسة» عمداً: بدونها يكون آخر جلب قبل الإغلاق
    بنصف ساعة، فيُحفظ سعرُ 19:40 إغلاقاً رسمياً — **ورقمٌ خاطئ هنا
    ينتقل إلى بيفوت الغد كلّه**. و`PRE` مستثناة: شمعة اليوم لم تبدأ.
  - **التقصير للطبقة الأساسية وحدها.** تقصيرُه للواسعة يجعل 414 رمزاً
    تستحق التجديد كل نصف ساعة والسقف 60 لكل تشغيل — فلا تكتمل دورةٌ
    أبداً. ولهذا `live` تُمرَّر إلى `stale` ولا تُحسب بداخلها.
  - **المقارنة الذهبية هي شرط القبول**: مع بياناتٍ طازجة يجب أن تخرج
    نفس الأرقام بالضبط. أُثبت في `?selftest=1` (35/35)، و`R1` الناقصة =
    `R1` الطازجة.

- **ياهو يصفّر `bid` و`ask` و`impliedVolatility` للخيارات خارج ساعات
  التداول** ولا يُبقي إلا `lastPrice` و`openInterest` و`lastTradeDate`.
  الفلتر الذي يشترط عرضاً وطلباً يعيد **صفر عقد** ليلاً — وهو ما حدث في
  أول تشغيل. لذلك وضعان: `live` بعرض وطلب حقيقيين، و`last` يستخرج
  التقلّب الضمني من سعر آخر صفقة بالتنصيف. الوضع يُقرأ من نسبة العقود
  المسعَّرة لا من ساعة الحائط، ولا يكفي عقدٌ أو عقدان مسعَّران لقلب
  السلسلة إلى `live` — كان ذلك يُفرغ سلاسل PANW وCRM تماماً.
- **في الوضع المغلق يُقاس الزمن من لحظة السعر لا من الآن.** سعرُ عقدٍ
  نُفِّذ الجمعة مع زمنٍ متبقٍّ محسوب الثلاثاء يخلط لحظتين، فيُنسب فارق
  الزمن كلّه إلى التقلّب: تقلّب VZ الضمني خرج 117% بدل 22%.
- **`divY` في `fundamentals.json` مخزَّن بمقياسين**: أبل `0.0034` (كسر)
  وفيرايزون `5.5842` (نسبة مئوية) — هكذا يعطيه ياهو. الواجهة تتعايش عبر
  `normPct` لأنها تعرض النسبة، أما الحساب فلا: تمرير `5.5842` كعائد
  توزيعات إلى Black–Scholes يخفض السعر الآجل إلى 59% من الفوري. أي
  مستهلك حسابي يجب أن يطبّع بنفسه (`divYield` في `fetch-options`).

- **تواريخ ياهو للعملات الرقمية تحمل أسعاراً "ما قبل الإدراج"**: UNI-USD
  يقفز من 0.000038 إلى 0.598 في يوم — عائد 1,573,986%. رقم واحد كهذا وسط
  نصف مليون ملاحظة رفع متوسط عائد اليوم الواحد ثلاث نقاط مئوية، فصار خط
  أساس الأرشيف 19% لعشرين يوماً بدل 1%. لذلك: الكريبتو خارج الأرشيف،
  و`trimArtifacts` تقطع السلسلة عند أي قفزة يومية فوق 80%، والإحصاء
  **بالوسيط لا المتوسط**.
- **ملفات `sym/*.json` تحفظ 260 شمعة يومية فقط** (`KEEP`)، وهي أقصر من
  فترة تسخين الأرشيف نفسها (EMA200 + نافذة 52 أسبوعاً = 260). فالأرشيف
  يجلب تاريخه بنفسه في الذاكرة ولا يخزّنه — خمس سنوات لخمسمئة رمز
  ~25 ميغابايت تُنشر بلا أن يقرأها أحد، ومخرَجه ثلاثة كيلوبايتات.

- **تقريب الأسعار بالخانات العشرية يمحو الأصول الرخيصة.** شيبا إينو
  بسعر `0.0000051` صار صفراً مع `Math.round(v*10000)/10000` — وكذلك كل
  شمعاته وATR وشرارته. `rp()` تقرّب بالأرقام المعنوية لما دون الواحد
  وبأربع خانات لما فوقه، وهي **مكرّرة عمداً في `fetch-market` و
  `fetch-quotes`** لأن اختلافها بينهما يجعل السعر يقفز بين الدورتين.
  وفي الواجهة `priceDigits` تفعل الشيء نفسه للعرض.
  لم يظهر هذا إلا حين رفضت بوابة النشر الصفَّ — وهي تعمل لهذا بالضبط.

- **الاتجاه معلومة تُحسب ولا تُستعمل — كان أخطر خلل مرّ بالمشروع.**
  `scoreFrom` تعطي ‎−100..+100‎ و`labelOf` تقول «هابط قوي»، ثم **لا مستهلك
  واحد يفرّع على الإشارة**: `tradePlan` كانت شراءً دائماً، فسهمٌ نتيجته
  ‎−97‎ تُعرض له أهداف **فوق** سعره لأنها تُشتق من `resAll`. ونفس العلة في
  الإحصاء: `update` و`outcome` تقيسان صعوداً لكل الإشارات، فـ«توافق
  الفريمات ▼» تُحتسب رابحة حين **يصعد** السعر — أي أن الأرشيف كان يحكم
  على شرطٍ يعمل بأنه فاشل. القياس بالسنة على 96 رمزاً: 61 منها هابط
  فأصبحت خططها الستون كلها مقلوبة.
  - الحلّ **معامل إشارة في مسار واحد** (`planFrom(inputs, d)`) لا فرعين:
    نسختان من نفس الرياضيات — شراء وبيع — تتباعدان بأول تعديل.
  - `dir` في `scans.js` هو مصدر الاتجاه الوحيد، وغيابه يعني ‎+1‎. ولا
    يُكتب في سجلّ الإشارة إلا حين يخالف ‎+1‎ (كما `mkt`) لأنه يتراكم بعشرات
    الآلاف.
  - **قلب العائد لا يكفي**: أقصى ربح على البيع هو **أدنى قاع** لا أعلى
    قمة (`signOutcome`)، وحافة الشرط الهابط تُقاس على **خط أساس هابط**
    وإلا خرجت كاذبة بضِعف الفرق.
  - `mfe`/`mae` مسارٌ تراكمي **لا يُعاد حسابه من سعر واحد**، فسجلات ما قبل
    التصحيح لا تُحوَّل: تُوسَم `conv:"long"` وتُغلق وتُستثنى من الإحصاء
    (28 سجلاً)، وتُعاد الإشارة صحيحةً في الدورة التالية.
  - بوابة `validatePlan` تردّ أي خطة أهدافها في الجهة الخاطئة وتسجّلها في
    `diag()` / `window.__diag()`. **خطة كاذبة تُخفى ولا تُعرض** — نفس قاعدة
    «اعرض ‎—‎ لا رقماً ملفّقاً» على خطة كاملة لا حقل.
  - `?selftest=1` يشغّل `planSelfTest` بلا شبكة، وفيه **مقارنة ذهبية**:
    مدخلات صعود ثابتة يجب أن تعيد أرقام ما قبل التعديل بالضبط. أُثبت على
    الـ96 رمزاً كلها بفرض `dir=+1`: صفر فرق في كل حقل. إصلاحٌ يصحّح
    الهبوط ويحرّك الصعود أسوأ من العلة.

- **`trend.json` تسلسل النتيجة — يُحمَّل عند فتح سهم لا عند فتح التطبيق.**
  دمجُه في `signals.json` يُثقل كل تحميل صفحة ببيانات لا تُقرأ في أغلبها،
  ويكرّر تسلسل السهم بعدد شروطه المحقَّقة. والنقطة تُحفظ **بشرطين فقط**:
  حركة ‎±5‎ نقاط، أو **عبور حدّ نطاق** (‎±15‎/‎±45‎ — حدود `labelOf` نفسها)
  ولو كانت الحركة نقطةً واحدة، لأن انقلاب الاتجاه لا يجوز أن يُفقد. بلا
  هذا الشرطين: 96 رمزاً × 144 دورة = ~14 ألف نقطة يومياً تصف حركةً لا
  معنى لها. والزمن **بالثواني** لا بالملّي — ثلاثة أرقام في كل نقطة تصير
  18 كيلوبايت بلا أن تُقرأ. وبوابته على **عدد الرموز** لا النقاط: النقاط
  تتقلّص بالتشذيب كل تشغيل فلا تصلح مقياساً.

- **`sc` و`tfs` في سجلّ الإشارة لقطةٌ لا تُحدَّث أبداً.** النتيجة الفنية
  وحالة الفريمات الأربعة كما كانت **حين ظهرت الإشارة**. إعادة حسابها
  لاحقاً من بيانات لم تكن متاحة تجعل السجلّ يدّعي أنه رأى ما لم يرَه.
  وهي تُكتب على السجلات **الجديدة وحدها** — فلا تتوقّع ظهورها في تشغيل
  بلا إشارة جديدة (‎added: 0‎)، وهو ما أوهم أول فحص أن الحقل لا يُكتب.

- **ثلاث دوال زمن لثلاث حاجات، ولا واحدة تنوب عن أخرى.** `ago` نقطةٌ في
  الماضي («قبل ساعة»)، و`dur` عدّادٌ تنازلي بصيغة ساعة الحائط («1:04:22»)
  **مأخوذةٌ لترويسة السوق**، و`age` طولُ مدة («ساعة و4 دقائق»)، و`ageShort`
  مختصرها لخانة الرقم («1س 4د»). تسميةُ دالة العمر `dur` تجعل «عمر
  الفرصة» يظهر «3:48:51» — وقع فعلاً، وسبَبه أن تعريفَ الدالة الأخير
  يغلب في نفس النطاق بلا أي خطأ.

- **`.stat .v` فيها `nowrap` مع قصٍّ بثلاث نقاط.** خانة `.grid3` ثُلث
  العرض، فأي نص أطول من ~10 محارف يُقصّ على الهاتف بصمت: «3 ساعة و50
  دقيقة» تظهر «3 ساعة و5…». الرقمُ الكبير يبقى قصيراً، والنص الكامل ينزل
  إلى `sub` تحته لأنه يلتفّ.

### السجل واللقطة الثابتة — بنيةُ منع النظرة إلى المستقبل
- **`snap` تُكتب مرة واحدة ولا تُلمس، و`out` وحدها تُحدَّث.** الفصل بنيوي
  لا اختياري: نظامٌ يعيد حساب توصيةٍ قديمة ببيانات اليوم يخرج بأرقام أجمل
  دائماً لأنه يحسب وقد صار المستقبل معلوماً. اللقطة تُبنى **لحظة** ظهور
  الإشارة أو لا تُبنى أبداً — ولا يُملأ السجل بأثر رجعي.
- **`updateOutcome` تردّ فوراً إذا كانت الصفقة منتهية** (`TERMINAL` أو
  `!open`). بلا هذا الحدّ كانت الصفقةُ التي ضُرب وقفها تسجّل أهدافها حين
  يرتدّ السعر بعدها، فتظهر «خاسرة بلغت أهدافها الثلاثة». حلقةُ التشغيل
  تنادي المفتوحةَ وحدها فلم يظهر في تشغيل — كشفه الفحص الذاتي.
- **`wait` حالةٌ مستقلة لا «مفتوحة».** خطةٌ دخولها عند دعمٍ لم يُلمس **لم
  تُفتح**، وحسابُ «بلغ الهدف» عليها يمنح النظام صفقةً رابحة لم يكن يمكن
  الدخول فيها. وانتهاء أفقها بلا دخول `cancel` لا خسارة.
- **المفتوحة ليست رابحة ولا خاسرة، والملغاة ليست خسارة — في الحساب لا في
  النص.** مقام كل نسبة هو ما دخل وأُغلق. وعدُّ المفتوحات في المقام يجعل
  نسبة النجاح تنخفض كلما ظهرت إشارة جديدة، فتقيس النشاط لا الأداء.
- **مقام نسبة الهدف الثالث خططٌ لها ثلاثة أهداف أصلاً.** خطة بهدفين لا
  تُحاسب على ثالث لم تعرضه. والصفقة التي ضُرب وقفها **تبقى في المقام** —
  استثناؤها يرفع النسبة كذباً بحذف كل ما فشل.
- **`signals.json` للمفتوح و`history.json` للمغلق.** اللقطة أضافت
  ~400 بايت للسجلّ، والأول يُقرأ في **كل تحميل صفحة**. والبوابة على
  **المجموع** لا على أحدهما: نقلُ سجلٍّ بين الملفين تقلّصٌ مشروع في
  الأول، وضياعُه تقلّصٌ في المجموع — والثاني وحده خلل (`guardTotal`).
- **القياس عند حدود الدورة (عشر دقائق):** قفزةٌ داخل الدورة لا تُرى.
  هذا يُنقص عدد الأهداف المحقَّقة **ولا يزيده** — وهو الخطأ المقبول، لأن
  العكس يمنح النظام نجاحاً لم يحدث.
- **الطبقة التاسعة بلا سلطة، ويُختبر ذلك:** `learn.mjs` لا يكتب إلا
  `learn.json` و`meta.json`، ولا يستورد `plan.js` ولا `indicators`، وفحصه
  الذاتي يقرأ شيفرة نفسه ويرفض أي إسناد إلى تعريف شرط أو وزن. تقييدٌ
  مقصود: نظامٌ يعدّل أوزانه على مئتي حالة يُلاحق الضجيج ويسمّيه تعلُّماً،
  والأرشيف احتاج 482 ألف شمعة ليقول إن حافة شرطٍ نصفُ نقطة.

### مصائد المرحلة الأخيرة — كلّفت تشخيصاً فعلياً
- **`stocks/plan.js` مشتركة لأن الخادم يحفظ ما رآه المستخدم.** نسخةٌ
  ثانية على الخادم تتباعد بأول تعديل فيصير السجلّ يقول إن الهدف كان 106
  والمستخدم رأى 112. أُثبت التطابق: 57 رمزاً، صفر فرق في الاتجاه والدخول
  والوقف وATR والعائد وكل هدف. وهي نفس علّة `scans.js` ونفس حلّها.
- **وحدةُ الزمن في ETA يجب أن تكون شمعةَ ATR نفسه لا فريمَ الإشارة.**
  القسمة تعطي «عدد شمعات ATR»، فضربها بطول شمعةٍ أخرى خلطُ وحدتين: ATR
  اليومي مع شمعة أسبوعية أخرج **93 يوماً** لهدفٍ على بُعد 26%. ولذلك
  `etaFor` تأخذ `atrTf` صريحاً لا `tf`.
- **استثناءٌ في بطاقةٍ واحدة كان يُبقي بيانات السهم السابق معروضة.**
  متغيّرٌ غير معرَّف في خطة الصفقة عرض خطة AAPL على شاشة NVDA بلا أي أثر
  ظاهر — أخطر من شاشة فارغة، لأن الأرقام تبدو صحيحة وتخصّ سهماً آخر.
  `renderDetail` ترسم كل بطاقة على حِدة وتكتب خطأها في مكانها وتسجّله في
  `diag`. **الفحص الذي يكشف هذا** هو تمرير كل رمز على كل بطاقة (96 × 6).
- **`<select>` يُبنى مرةً واحدة يتجمّد على بيانات أول رسم.** قائمة شروط
  السجل كانت تُبنى قبل وصول `history.json` فتثبت على شروط المفتوحات
  وحدها، ثم يصل السجل بشروط لا يمكن التصفية بها أبداً. تُعاد في كل رسم
  مع حفظ الاختيار.
- **الإحصاء يستبعد الكريبتو والقائمة تعرضه** — فالفارق يُقال صراحةً في
  سطر العدد وبوسمٍ على الصفّ. «3 رابحة» في الإحصاء و4 صفوف تحت مرشّح
  «رابحة» يُقرأ خللاً بلا تفسير.
- **الإحصاء لا يتبع التصفية.** «نسبة النجاح» التي تتغيّر بتغيير الفلتر
  تفقد معناها — الفلتر يعيد القائمة وحدها.
- **`details summary::before` فيها «؟»** لأن كل كاشف في التطبيق سؤال.
  الكاشف الذي ليس سؤالاً (تسلسل، صفقة) يأخذ `class="plain"`.

### مصائد ويندوز في سكربتات التشغيل — كلّفت تشخيصاً فعلياً
- **`cmd.exe` يفسد ملف `.bat` بـUTF-8 تحت `chcp 65001`.** يتتبّع موضعه
  بالبايتات ويعيد قراءته بعد كل أمر، فمع نص عربي طويل يختلّ التتبّع
  وتُقرأ الأوامر مقطّعة (`'/SC' is not recognized`). ولا يفشل بوضوح:
  بعض المهام تُنشأ وبعضها لا. لذلك منطق الجدولة كله في `schedule.ps1`،
  وملفات `.bat` أسطر إنجليزية قليلة تستدعيه.
- **`.ps1` بلا BOM تقرأها PowerShell 5.1 كـANSI** فيتشوّه العربي وينهار
  التحليل. تُحفَظ UTF-8 **مع BOM**، و`.gitattributes` يثبّت ذلك.
- **`Register-ScheduledTask` ترفض التكرار اللانهائي**: `[TimeSpan]::MaxValue`
  يخرج منها `P99999999DT23H59M59S` فيردّ المجدول «قيمة خارج المدى».
  الإنشاء بـ`schtasks.exe /SC MINUTE /MO n`، والضبط بعده بـ`Set-ScheduledTask`.
- **`WshShell.Run` بلا انتظار يعطّل حمايات المجدول الثلاث.** كان
  `run-hidden.vbs` يعود فور إطلاق node، فيسجّل المجدول «نجح 0x0» قبل أن
  يبدأ العمل: السقف الزمني لا يقتل تشغيلاً عالقاً، ونتيجة آخر تشغيل تقول
  «نجح» دائماً، و`IgnoreNew` لا يرى تداخلاً قط. صار ينتظر ويعيد رمز
  الخروج الحقيقي. قفل `data/.run.lock` وُجد أصلاً لأن حماية المجدول
  كانت معطّلة بصمت.

- **`schtasks /SC MINUTE` تبدأ من لحظة إنشائها، فالمهام المنشأة معاً
  تتصادم إلى الأبد.** أُنشئت الثلاث في تشغيل واحد ‎16:47‎، فصارت نقطة
  بدايتها واحدة: الأسعار كل دقيقتين والسوق كل عشر والعقود كل ثلاثين —
  وكلها على دقائق فردية، فكل إطلاق للسوق يصادف إطلاقاً للأسعار.
  - **والانسحاب يبدو نجاحاً**: الأسعار تأخذ القفل (تنتهي في ~13 ثانية)،
    فتنسحب المتأخّرة وتخرج بـ‎rc=0‎ — فيسجّل المجدول «نجح» وهي لم تعمل.
    لا سطر في السجل يقول ذلك، والدليل الوحيد فجوةُ زمنٍ في `meta.json`.
  - قياس 2026-09-11: فجوة السوق **41 دقيقة** على دورة عشر دقائق،
    والعقود **91 دقيقة** على دورة نصف ساعة.
  - الحلّ إزاحات صريحة بـ`/ST`: الأسعار على الدقائق **الزوجية**، وكل ما
    عداها **فردية** بإزاحات مختلفة (السوق ‎:01‎، العقود ‎:05‎، اليومي
    ‎09:27‎). فلا تشترك مهمتان في دقيقة، ويعود القفل شبكةَ أمان بدل أن
    يكون هو القاعدة.
  - **اليومي خاصةً**: انسحابُ مهمةٍ تعمل مرة في اليوم يضيّع يوماً كاملاً
    لا عشر دقائق. كان ‎09:30‎ — دقيقة زوجية — ونجا بالصدفة وحدها لأن شبكة
    الأسعار كانت فردية حينها.
  - وهذا ما يجعل الإصلاح أعلاه (صلاحية اليومي نصف ساعة) ذا أثر فعلي:
    صلاحيةٌ بنصف ساعة لا تعني شيئاً إن كان مُغذّيها يعمل كل 41 دقيقة.

- **حجم عقد الخيارات مقابل مراكزه القائمة يتضخّم قرب الانتهاء بطبيعته**
  (المراكز تُغلق قبل الاستحقاق)، فتصدّرت قائمةَ «النشاط غير المعتاد»
  عقودُ الغد بنسب 25–50 ضعفاً — تحيّز بنيوي لا إشارة. `FLOW.minDays`
  يستبعد ما يبقى له أقل من أسبوع.
- **`options.json` تراكمي مثل `wide.json`**: الطبقة الواسعة تُجدَّد
  بالتدوير (`OPT_WIDE_PER_RUN`)، فبلا دمج يخرج الملف بحصّة التشغيل
  الأخير وحدها وتختفي بقية الرموز من الواجهة. وصفٌّ قديم يبقى بقيمه حتى
  يأتي دوره — وهو سلوك مقصود لا خلل.

### طبقة العقود العميقة — مصائد كلّفت تشخيصاً فعلياً
- **استحقاق اليوم كان يسقط، فتُلصق عقودُه بتاريخِ غدٍ.** ياهو يعطي تاريخ
  الانتهاء عند **منتصف ليل UTC**، والعقد يعيش حتى إغلاق نيويورك
  (~21:00 UTC). فمن ظهيرة يوم الاستحقاق فصاعداً كان `pickExpiries` يراه
  منتهياً ويتخطّاه، بينما نداء ياهو الأول يردّ بسلسلة **اليوم** — فتُحفظ
  عقود 0DTE تحت ترويسة «بعد 3 أيام». العقود نفسها سليمة (كلٌّ يحمل
  انتهاءه)، والخلل في كل ما يُحسب على **مستوى الاستحقاق**. الحدّ الآن
  `+21h` — نفس إزاحة `yearsToExpiry` كي لا تختلف دالتان في تعريف
  «منتهٍ» — و`asChain` تأخذ التاريخ من العقود لا من المطلوب.
- **`days` المقرَّبة لا تصلح للحساب.** عقد اليوم `days: 0`، وأي احتمال
  يُقسم على زمنٍ صفر يسقط كله — بينما زمنه الحقيقي ساعات. الحقل `t`
  (سنوات بستّ خانات) إلى جانبها، و0DTE هو أكثر ما يُتداول.
- **منحنى العائد مضلَّعٌ رؤوسه عند السترايكات، والشبكة المنتظمة تقفز
  فوقها.** أقصى خسارة الستراد **عند** سترايكه بالضبط، فخرجت 370.74 بدل
  383 لأن أقرب نقطتين كانتا على جانبيه. الشبكة تمرّ بكل سترايك وبالسعر.
  وهذا خطأٌ لا يظهر استثناءً: الرقم يبدو صحيحاً تماماً لمن لا يحسبه.
- **«مفتوح» يُقاس صعوداً وحده.** السهم لا يهبط تحت الصفر، فكل طرفٍ هابط
  محدودٌ رياضياً. قياس الميل عند الطرف الأدنى كان يقلب ذلك: البوت
  المشترى يرتفع عائده كلما هبط السهم، فيُقرأ ميله السالب «خسارة غير
  محدودة» — وهي في الحقيقة **أعلى ربحه**.
- **موضع الحدّ يُوصف بمداه لا بأعلى سترايك.** ثلاث حالات: «فوق 222.5»
  و«بين 215 و222.5» و«عند 217.5» — وأخذُ `hiK` في كلها يكذب على
  الكوندور والستراد معاً. والمنطقة قد تكون **منفصلتين**: خسارة الكوندور
  القصوى تحت جناحه الأدنى **و** فوق الأعلى، ووصفها «من الصفر إلى
  اللانهاية» يجعلها تبدو شاملةً لكل سعر.
- **الجاما تُعدّ ولا تُطرح.** طرحُ جانبٍ من آخر وتسميته «تعرّض صنّاع
  السوق» يقتضي معرفة من في الجانب الآخر من كل عقد — معلومةٌ لا يملكها
  أحد خارج المقاصّة. الجانبان منفصلان، والاسم «أين تتركّز الجاما».
- **أقصى الألم والجدران ونسبة بوت/كول تُقاس على السلسلة الخام** لا على
  المرشَّحة سيولةً: أطراف السلسلة حيث تتراكم المراكز الكبيرة. أما ما
  يحتاج جريكس (الحركة المتوقّعة، الجاما) فعلى المقيَّمة وحدها.
- **الحركة المتوقّعة ستراد لا خنق.** جمع قسطين على سترايكين مختلفين
  رقمه أكبر بلا معنى — يُشترط سترايك واحد للجانبين.
- **`ivhist.json` نقطةٌ لكل يوم لا لكل تشغيل.** الدورة نصف ساعة، فالكتابة
  في كل تشغيل تجعل «رتبة 252 يوماً» رتبةَ ستة عشر يوماً بينما يقول
  العدّاد 252. وبالألف كعدد صحيح: الدقة كافية لرتبةٍ مئوية، والكسور
  تضاعف ملفاً يحمل مئة ألف رقم.
- **رتبة التقلّب المحقَّق تسدّ الفجوة حتى تنضج الضمنية.** تاريخها في
  شمعاتنا أصلاً (`hvSeries` نافذةٌ متدحرجة، 239 نقطة لسنة)، وتجيب سؤالاً
  آخر مفيداً: هل يتحرّك هذا السهم الآن أكثر من عادته؟ والحالة تُقال
  صراحةً («يُبنى: يوم من 252») بدل إخفاء القسم شهراً.
- **قراءة السوق تُجمع بالأعداد لا بمتوسط النسب.** متوسّطُ نسبٍ يعطي رمزاً
  حجمه مئة عقد وزنَ رمزٍ حجمه مئة ألف. والوسيط للضمني/المحقَّق لأن رمزاً
  واحداً نسبته 6× يزيح المتوسط ولا يزيح الوسيط.
- **`bsN` في المتصفح نسخةٌ للعرض لا منطق تداول.** الخطة والسجل لا يمرّان
  بها، فلا تتباعد نتيجةٌ محفوظة عمّا رآه المستخدم — وهي السبب الذي جعل
  `plan.js` و`scans.js` مشتركةً ولم يجعل هذه كذلك. ودقّة تقريبها
  ~1.2e-7، فالفحص يقيس بها لا بصفرٍ مطلق.
- **`?selftest=1` يشمل رياضيات التركيبات** بقيمٍ محسوبة يدوياً لا مأخوذة
  من تشغيلٍ سابق: 28/28 لنواة الخطة و7/7 للتركيبات.

### مصائد الواجهة العريضة
- **الأعمدة تدفّقٌ بنّائي لا شبكة.** الشبكة تحاذي الصفوف فتترك تحت
  البطاقة القصيرة فجوةً بطول أطول جارتها.
- **ما يأخذ العرض كاملاً يُعرَف بمحتواه لا باسمه**: `:has(canvas)` و
  `:has(.otwrap)` و`:has(.mwrap)` — الأسماء تتقادم.
- **الارتفاع لا يُعرف من CSS.** `layoutWide()` تقيسه، والقياس **يسبق**
  التوسيع: تُزال السمة ثم يُقاس ثم تُعاد، وإلا اعتمد القرار على قرار
  الرسم السابق فتذبذب.
- **`requestAnimationFrame` لا يُنفَّذ في لسانٍ مخفي** — `setTimeout`
  تعمل في الحالتين.
- **القياس مرةً عند تبديل الشاشة لا يكفي**: بطاقاتٌ تطول بعد وصول ملفّها.
  `ResizeObserver` على `main` يلتقطها، ولا يدور على نفسه لأن الإزالة
  والقياس والإعادة تجري في إطارٍ واحد متزامن.
- **`$$` في نصّ بديل لـ`String.replace` تصير `$` واحدة** — تسلسلُ هروبٍ
  يعني «دولاراً حرفياً». استبدل بدالة لا بنص.
- **شاشتان محتواهما قائمةٌ واحدة** («الأسهم» و«الأخبار») تُستثنيان من
  الأعمدة: تقسيمها عمودين يقطعها في منتصفها. القائمة نفسها تتعمّد.

### الواجهة تسقط صامتة — ولذلك `check-ui`
- **خطأٌ نحوي واحد في `index.html` يُسقط كل الشيفرة**، فتُعرض صفحةٌ ساكنة
  بلا رسالة. وقع مرتين في جلسة واحدة: علامةُ اقتباس فُتحت مفردةً وأُغلقت
  مزدوجة، وتسلسل `\n` صار **سطراً حقيقياً** داخل تعبير نمطي.
- **سبب الثاني بنيوي**: قالبٌ نصّي يولّد شيفرة، وأول علامة اقتباس خلفية
  غير مهروبة **تنهي القالب** فيفقد ما بعده حمايةَ `String.raw` — فتتحوّل
  `\n` و`\r` إلى محارف فعلية. أي مولّد شيفرة يجب أن يُحلَّل ناتجه قبل
  الحفظ، وهو ما يفعله `check-ui` في ثانية.
- ويفحص ما لا يراه المحلّل النحوي: **معرّفٌ تناديه الشيفرة ولا وجود له**
  يعيد `null` بصمت فتتوقّف بطاقةٌ عن الرسم بلا استثناء ظاهر. و**تطابق
  التبويبات والشاشات**، و**ألا تُنسخ النواة المشتركة** داخل الصفحة،
  و**ألا يدخل اعتماد خارجي** غير خطوط جوجل.

### التحليلات المشتقّة — مصائد
- **الفائض لا العائد.** في سوقٍ صعد 20% يبدو كل سهم قوياً. والرتبة مئينٌ
  **داخل الأسهم وحدها**: مدى الكريبتو يزيح التوزيع كله.
- **العوائد لوغاريتمية** في الارتباط والبيتا لأنهما يُبنيان على الجمع،
  والنسبي لا يُجمع (‎+50%‎ ثم ‎−50%‎ ليس صفراً).
- **اليوم الناقص يُسقَط ولا يُملأ بصفر**: «لم يتداول» ليس «لم يتحرك»،
  وملؤه يخفض الارتباط زوراً.
- **بيتا عندنا 120 يوماً يومية مقابل SPY**، ورقم ياهو خمس سنوات شهرية.
  الفرق مقصود ويُقال في الواجهة. ووسيط بيتا الكون 0.45 لا 1.0 —
  طبيعيٌّ حين يقود المؤشّرَ عددٌ قليل من الأسماء الكبيرة، لا خلل حساب.
- **المصفوفة مثلثٌ أعلى مسطَّح** بأعداد صحيحة ‎×100‎، و‎−128‎ علامة «لا
  عيّنة» لا ارتباطٌ سالب. وعلى المرصودة وحدها: 510 رمزاً = 130 ألف زوج.
- **مساحة مربّع الخريطة بجذر القيمة السوقية** لا بها: أبل تساوي مئة شركة.
  واللون يُشبَع عند حدٍّ معلن وإلا ابتلع سهمٌ قفز 40% كل التدرّج.
- **`pct()` تُلحق العلامة بنفسها** — إضافة «%» إلى ناتجها تطبع «+3.1%%».

### مصائد العقود المتأخرة
- **الستراد عند أقرب سترايك مشترك لا أقرب سترايكٍ لكل جانب.** الجانبان
  يُرشَّحان بالسيولة كلٌّ وحده، فقد يسقط بوت 227.5 ويبقى كوله — فيردّ
  الشرط «سترايكان مختلفان» وتضيع الحركة المتوقّعة على السلسلة كلها.
  حدث لرموز الأرباح الثلاثة عشر كلها دفعةً واحدة.
- **الحركة المتوقّعة للأرباح من استحقاقٍ يغطّي التاريخ.** استحقاقٌ ينتهي
  قبل الإعلان لا يسعّره أصلاً، وحركته تصف أسبوعاً عادياً وتُقرأ خطأً.

### المحفظة — القاعدة الوحيدة التي تصف المستخدم
- **`localStorage` وحده**: لا خادم ولا حساب ولا شيء يُرسل.
- **الرمز مرتين يُدمج بمتوسط تكلفةٍ مرجّح** لا صفّين: صفّان يجعلان الوزن
  والارتباط يحسبان المركز الواحد مركزين.
- **مركزٌ بلا سعر يُستثنى ويُقال عدده** — رمزٌ خارج الكون المرصود لا سعر
  له عندنا، وحسابه بصفرٍ يكذب على قيمة المحفظة كلها.
- **متوسط الارتباط مرجّحٌ بحاصل الوزنين**: ارتباط مركزين صغيرين لا يصف
  المحفظة.
- **CSV بفاصلة منقوطة وبادئة BOM**: إكسل العربي يفتح الفاصلة العادية في
  عمود واحد، وبلا BOM يقرأ UTF-8 محارفَ مشوّهة. **والرقم الغائب فراغٌ لا
  صفر** — إكسل يحسب الصفر ويتجاهل الفراغ، والفرق بين «خسر صفراً» و«لا
  نعرف» هو الفرق بين إحصاءٍ صحيح وكاذب.

### عامل الخدمة — مصيدةٌ تقلب الفائدة إلى ضرر
- **التطبيق يُلحق `?t=…` بكل طلب بيانات** ليتجاوز ذاكرة المتصفح، فكل
  سحبةٍ عنوانٌ جديد. تخزينُها بمفتاحها الخام يعطي عكس المقصود تماماً:
  الذاكرة تتضخّم بنسخةٍ لكل دقيقتين، **ولا تُطابق واحدةٌ منها أبداً** عند
  انقطاع الشبكة لأن العنوان التالي مختلف. المفتاح يُطبَّع بحذف الاستعلام.
- **ثلاث استراتيجيات لثلاث طبيعات**: الهيكل «قديمٌ ثم يُجدَّد» (تخزينه
  مباشرةً يجمّد التطبيق على نسخةٍ قديمة أبداً)، والبيانات **الشبكة أولاً**
  (سعرٌ من الذاكرة سعرٌ كاذب)، والخطوط الذاكرة أولاً.
- **لكل ملفٍ كسول صلاحيةٌ من دورة كتابته** لا من دورة التحديث: العقود
  ربع ساعة، والتحليلات والأساسيات ثلاث ساعات. بلا ذلك يُعاد سحب 346
  كيلوبايت كل دقيقتين لملفٍ يُكتب كل نصف ساعة — ظهر في سجل الشبكة:
  عشرون طلباً متطابقاً في نصف ساعة.
- **لا عقود للعملات الرقمية**: الطلب يعود 404 في كل فتحة، وحارسٌ واحد
  يمنع سؤالاً عمّا لا وجود له.
- **سقف 140 مدخلاً للبيانات**: 510 ملف رمز تملأ حصّة المتصفح. والحذف
  بترتيب الإدخال لأن الواجهة لا تتيح زمن آخر قراءة.
- **التحديث يظهر في الزيارة **الثانية**.** «قديمٌ ثم يُجدَّد» يعني أن
  أول فتحة بعد النشر تعرض النسخة المخزَّنة ويُجلب الجديد في الخلفية.
  وقع هذا في التطوير: تعديلٌ في الشيفرة لم يظهر بعد إعادة تحميل واحدة
  وبدا أنه لم يُطبَّق. أعِد التحميل مرتين، أو احذف الذاكرة.
- **يُسجَّل بعد `load` لا معه**: تسجيله في أثناء التحميل يزاحم طلبات
  البيانات على نفس الاتصال فيؤخّر أول رسم — عكس المقصود.

### النتيجة الفنية — بواباتٌ لا مقياس، وثلاث مصائد فيها

- **النتيجة مجموع سبع بوابات نعم/لا، وكل بوابة تنقلب كاملةً.** فسهمٌ سعره
  فوق متوسّطه بسنت يأخذ ‎+1.0‎، وتحته بسنت يأخذ ‎−1.0‎ — انقلاب ‎23.5‎
  نقطة على فريمه من حركةٍ لا يراها أحد. بلّغ عنها مستخدمٌ لاحظ أبل تتذبذب
  ‎95 → 60 → 98‎ في عشرين دقيقة.
  - القياس: **93 رمزاً من 96** فيه بوابةٌ يفصلها عن الانقلاب أقلّ من
    ‎0.1%‎ من السعر. و**195 حالة** في `trend.json` تغيّرت فيها النتيجة
    والسعر لم يتغيّر بأيّ كسر. وبيتكوين انتقل ‎−22.75 → +11.37‎ بحركة
    ‎0.071%‎ — أي **تغيّر وسمُه** بلا حركةٍ تُذكر.
  - الحلّ **منطقة ميتة بـ ATR** (`DEAD_ATR = 0.15`): البوابة التي طرفاها
    متقاربان لا تصوّت لأيّ جهة — وزنها في المقام وصفرٌ في البسط، فيُدفع
    الحائر نحو الصفر وهو معنى «عرضي». وبـ ATR لا بنسبةٍ من السعر: العتبة
    تضيق في السهم الهادئ وتتّسع في المتقلّب تلقائياً.
  - وكشفت خللاً كامناً: `px > e200` تعيد `false` عند **التساوي التامّ**،
    فسلسلةٌ مجمّدة تخرج ‎−50.59‎ «ميل هابط» لأصلٍ لا يتحرّك. صارت صفراً.
  - الأثر المقيس على 510 رمزاً: وسيط الفرق ‎5.88‎ نقطة، و117 رمزاً تغيّر
    وسمُه، و**متناظر** (244 ارتفعت · 261 انخفضت) — فلا تحيّز اتجاهي.
    وأمثلةٌ فاحصة: CRWD سعره ‎+49.7‎ فوق EMA200 وكان ‎5.88‎ «عرضي» لأن
    ثلاث بوابات حادّة تسحبه، فصار ‎47.06‎؛ وCCI سعره ‎−8.2‎ تحت EMA200
    وكان ‎−5.88‎ لأن ثلاثاً ترفعه بفروقٍ من مليمتر، فصار ‎−47.06‎.

- **شمعات الحجم الصفري كانت تُنصّف ATR وتضيّق بولنجر 12 ضعفاً — وهذا كان
  أخطر مما ظهر أولاً.** فريم 15د وحده يُطلب بـ`prePost: true`، وياهو
  **يحشو** الساعات المغلقة بشمعاتٍ حجمها صفر تحمل آخر سعرٍ مكرَّراً.
  - أبل: **156 من 260** شمعة بحجم صفر (‎60%‎). ATR خرج ‎0.535‎ بدل
    ‎1.055‎، وعرض بولنجر ‎0.35‎ بدل ‎4.07‎ — **اثنا عشر ضعفاً**، وMACD
    hist ‎−0.063‎ بدل ‎−0.609‎.
  - فلا النتيجة وحدها كانت تهتزّ: كل مستهلكٍ لـ ATR أو بولنجر على 15د
    كان يقرأ أرقاماً مضلّلة، والعتبة الميتة نفسها تُشتق من ATR.
  - `tradingOnly` تُسقطها **قبل** `slice(-KEEP)`: الطلب يعيد 3841 شمعة،
    منها 1560 بحجم حقيقي، فيبقى 260 شمعةَ تداولٍ تغطّي عشرة أيام تداول.
  - **البوابة شرطُ سلامة**: مصدرٌ لا يعطي حجماً (Twelve Data وStooq)
    تُصفّره `Math.round(x.v || 0)` فيمحو السلسلة كاملةً — فإن لم يبقَ ما
    يكفي EMA200 يُعاد الأصل. وعلى 15د وحده: الساعة واليومي بلا `prePost`
    ونسبةُ حجمهما الحقيقي ‎100%‎.

- **الوسم يُثبَّت بهيستريسس، والرقم لا يُثبَّت.** المنطقة الميتة تُهدِئ
  الرقم ولا تمنع رقماً يستقرّ على الحدّ من عبوره ذهاباً وإياباً. ومن يرى
  «عرضي» ثم «ميل صاعد» ثم «عرضي» في نصف ساعة يقرأها إشاراتٍ متناقضة لا
  رقماً يهتزّ. `bandStable` لا تغيّر النطاق إلا بتجاوز حدّه بـ‎3‎ نقاط.
  - **يُحسب في الخادم لا في المتصفح**: الهيستريسس يحتاج ذاكرةً بالدورة
    السابقة، والخادم وحده يملكها (`prev`). ويُحفظ `band` في صفّ الملخّص
    وفي `market.json`، والمتصفح يعرضه كما هو — فلا يختلف وسمُ شاشتين.
  - والحلقتان لا شرطٌ واحد: النتيجة قد تقفز نطاقين في دورة، فالمشيُ
    خطوةً خطوة يضمن أن كل حدٍّ عُبر بهامشه.

- **`stocks/score.js` — النتيجة كانت مكتوبة أربع مرات.** في
  `indicators.mjs`، ومضمَّنةً **بلا اسم** داخل `analyze` في `index.html`،
  وحدودُ نطاقاتها مرةً ثالثة في `track-signals.mjs`. والفحص الذي يمنع
  النسخ كان يبحث عن **اسم الدالة** فمرّت النسخة المضمَّنة منه. صار يبحث
  عن بصمة الشيفرة نفسها (`max += w; sc +=`، وأوسمة النطاقات، و`TF_WEIGHT`).
  - وكشف التوحيدُ خلافاً صامتاً: `bandOf` في `track-signals` كان يضمّ
    ‎−45‎ إلى «ميل هابط» و`labelOf` يضمّه إلى «هابط قوي» — أي أن ملفَّ
    قياس انقلابات الاتجاه كان يختلف عن الوسم المعروض عند الحدّ بالضبط.

- **`ARB-USD` عند ياهو ليس أربيتروم.** أصلٌ ميت مجمَّد على ‎0.000629‎
  بحجم صفر منذ 260 يوماً، والتطبيق يعرضه سعراً ويحسب له اتجاهاً ويرشّحه
  للفرص. أربيتروم الحقيقية `ARB11841-USD` بسعر ‎0.14‎ وحجم 240 مليوناً.
  - **ولم يكشفه شيء**: السعر رقمٌ صالح، والشارت خطٌّ مستقيم، والنتيجة في
    مداها. نفس مصيدة «الأرقام تبدو صحيحة» مطبَّقةً على **رمزٍ كامل**.
  - `frozenSeries` تستبعده، وشرطاها **مجتمعان**: مسطَّحةٌ (مدى 30 يوماً
    أقلّ من نصف بالمئة) *وبلا* حجم — فسهمٌ هادئ له حجم. وعلى مصدر ياهو
    وحده لأنه الذي يعطي الحجم فعلاً.

### التحميل عند الحاجة — 88 كيلوبايت بدل 751
- **كان بدء التطبيق يسحب كل شيء**: 346 لملخّص العقود و173 للأخبار و107
  للأساسيات و105 للطبقة الواسعة و89 للتحليلات — وأغلبها لا يُقرأ في
  الزيارة الواحدة أصلاً. الآن يبدأ بسبعة ملفات خفيفة، وكل ثقيلٍ يُجلب
  أول مرة تُفتح شاشته.
- **الوعد يُحفظ لا الملف**: نداءان متزامنان على نفس الملف يعطيان طلباً
  واحداً — وهو ما يحدث فعلاً حين يُفتح سهمٌ يحتاج ثلاثة ملفات معاً.
- **الفشل لا يُخزَّن**: نُسقِط الوعد عند الخطأ كي تُعاد المحاولة عند فتح
  الشاشة مرةً أخرى. تخزينه يجعل انقطاعاً عابراً يُعطّل القسم للأبد.
- **`lazyReload()` تعيد جلب ما جُلب وحده.** ونداءٌ صريح بجانبها كان
  يجلب نفس الملف **مرتين** في كل تحديث — ظهر في قياس الطلبات لا في
  أي خطأ.
- **شاشة السهم تحتاج ثلاثة**: الأساسيات والأخبار والأداء النسبي. تُطلب
  معاً لا متسلسلة، وكل بطاقة تُرسم حين يصل ملفها.
- **الطبقة الواسعة لا تلزم أي شاشة افتراضية** — تُطلب أول حرف بحث، أو
  تبديل نطاق القائمة، أو فتح العقود. وغيابها تدهورٌ هادئ: التطبيق يبقى
  على المرصودة ولا يعطل.

### الأرشيف كان يقيس غير المعروض — والمعروضُ خاسر

قياسٌ على **1,084,574 شمعة** (عشر سنوات، 499 رمزاً، 407 أيام سوقٍ هابط).

- **أكبر فجوة مرّت بالمشروع بعد الاتجاه**: `outcome` تقيس عائد ‎1/5/20‎
  يوماً، والتطبيق **لا يعرض ذلك**. يعرض خطةً بدخولٍ ووقفٍ وثلاثة أهداف،
  ولم تُختبر تاريخياً ولا مرة. فكان الأرشيف يشهد لشرطٍ بشهادةٍ لا تخصّ ما
  يفعله المستخدم به — والرقم صحيحٌ ويجيب سؤالاً آخر.
- **والنتيجة حين قِيست: الخطة خاسرة على كل شرط.** التوقّع ‎−0.25%‎ إلى
  ‎−1.45%‎ بعد التكلفة، والوقف يُضرب ‎59–78%‎. وخط أساس الخطة نفسه (يومٌ
  عشوائي) ‎−0.60%‎.
- **ومسحُ مضاعِف الوقف لا يغيّر شيئاً** (‎1×ATR‎ ‎−0.70‎ … ‎4×‎ ‎−0.86‎).
  وهذا ليس معامِلاً يحتاج ضبطاً بل **بصمة نظامٍ بلا حافة**: توسيع الوقف
  يخفض نسبة الضرب ويرفع حجم الخسارة الواحدة، وثباتُ الحاصل يعني أن
  هندسة الهدف/الوقف نفسها لا تنتج ربحاً. والسبب رياضيّ: احتمال لمس
  الأقرب قبل الأبعد في مشيٍ عشوائي هو نسبة بُعديهما المقلوبة.
- **والتناقض الاتجاهي هو أكبر مساهم مفرد.** `planDirOf` تشتقّ الاتجاه من
  **النتيجة الفنية** لا من الشرط الذي أظهر السهم. وسهمٌ قرب قاع 52
  أسبوعاً نتيجتُه سالبة بالضرورة — فالشرط يقول «ارتدادٌ يُشترى» والخطة
  تحته **بيع**. ‎21,610‎ من ‎23,233‎ إشارة (‎93%‎) متناقضة.

| الشرط | متوافق | متناقض | لو فُرض اتجاه الشرط |
|---|---|---|---|
| تباعد صاعد ▲ | ‎−0.34%‎ | ‎−2.05%‎ | **‎+0.24%‎** |
| قرب قاع 52 أسبوعاً | ‎−0.08%‎ | ‎−1.01%‎ | ‎−0.05%‎ |
| حجم غير معتاد | ‎−0.39%‎ | ‎−1.42%‎ | ‎−0.34%‎ |
| تباعد هابط ▼ | ‎−0.79%‎ | ‎−0.32%‎ | ‎−0.78%‎ |

- **الترشيح والتدخّل شيئان مختلفان ويجب قياس الثاني.** «الإشارات التي
  وافق فيها الاثنان» عيّنةٌ منتقاة، و«كل الإشارات بخطةٍ اتجاهُها اتجاهُ
  الشرط» سياسةٌ يمكن اعتمادها. ولذلك `planAt` تخزّن **المستويات** وتشتقّ
  الخطة بأيّ اتجاه، فيُقاس المساران من نفس الحساب.
- **والصفّ الأخير في الجدول يمنع التعميم**: «تباعد هابط ▼» فرضُ اتجاهه
  **يسوء**. متّسقٌ مع كل ما سبق — هذا الكون يدفع على الارتداد ويعاقب على
  الاستمرار، صعوداً كان أو هبوطاً.
- **ولا تُغيَّر الخطة في العرض وحده**: السجلّ يحفظ ما رآه المستخدم، وتغيير
  العرض بلا الخادم يجعل المحفوظ غير المرئي — نفس علّة `plan.js` المشتركة.
  فالتناقض **يُقال** بالرقم المقيس وتُترك القراءة للمستخدم.

### التركيبات — الحافة مشروطة والرقم المجمّع يطمسها

- «تباعد صاعد ▲» حافته ‎+1.01‎ إجمالاً، و**‎+1.97‎** في سهمٍ اتجاهه هابط
  قوي (‎+0.98‎ خارج العيّنة، 3,598 إشارة). منطقيّ: التباعد إشارةُ ارتداد
  فلا معنى لها إلا حيث يوجد ما يرتدّ عنه.
- نجت **13 من 34** تركيبة بلغت حدّ العيّنة، بثلاث بوّابات **مجتمعة**:
  ‎n ≥ 200‎، وحافةٌ موجبة داخل العيّنة، **وحافةٌ موجبة خارجها**. والساقط
  في البوّابة الثالثة وحدها يُحصى ويُذكر عدده — «جُرّبت وسقطت» معلومة.
- **الأزواج المتناقضة تُستبعد**: «توافق ▲» مع «توافق ▼» معاً ليس تركيبةً
  بل تناقضٌ لا إشارة له.
- **`bandOf` تعيد ‎0..4‎ والصفر صالح** — فالفحص بـ`null` لا بالصدق، وإلا
  سقط نطاق «هابط قوي» كلُّه بصمت. وهو النطاق الذي فيه أقوى حافة.

### خارج العيّنة — الحدّ الذي يمنع الكذب على النفس

- الاكتشاف على أقدم ‎70%‎ والتحقّق على أحدث ‎30%‎ (بعد ‎2023-09-12‎).
- **الحدّ زمنيّ لا بالرموز**: قسمةُ الرموز تُبقي نفس الفترة في الجهتين
  فيتسرّب إليهما أثر السوق نفسه، والفترتان المختلفتان وحدهما تختبران
  الثبات عبر الزمن.
- **وخط أساس الفترة المتأخّرة يقابل عيّنتها**: مقارنة إشارةٍ ظهرت في 2025
  بخط أساس عشر سنوات تنسب إليها فرق السوقين لا فرق الشرط.
- بلا هذا الحدّ، كل تركيبةٍ تُضاف لاحقاً ستبدو أفضل من سابقتها لأنها
  تُختار على نفس البيانات التي تُقاس عليها.

### التكلفة تفاؤلٌ ثابت الاتجاه لا ضجيج

`scripts/lib/costs.mjs` — انزلاق ‎0.05×ATR‎ وعمولة ‎2‎ نقطة أساس **لكل
جهة**، ويقرؤه الأرشيف والتتبّع الحيّ معاً (نسختان تجعلان السجلّ يُقارَن
بأرشيفٍ يقيس شيئاً آخر — نفس علّة `plan.js`).

- **الانزلاق كسرٌ من ATR لا نسبةٌ من السعر**: سهمٌ يتحرّك ‎4%‎ يومياً
  يُنفَّذ أسوأ من سهمٍ يتحرّك ‎0.5%‎ عند نفس السعر.
- **والرسوم تُطرح مرتين** — دخولاً وخروجاً. طرحُها مرة يجعل الصفقة تبدو
  أرخص بالنصف.
- وأثرها **غير متناظر على الاستراتيجيات**: الصفقة القصيرة تدفع ما تدفعه
  الطويلة، فالتكلفة تضرب التداول السريع أشدّ — تمييزٌ لا يظهر بلا نموذج.

### قواعد محاكاة الخطة — كلٌّ تختار الأسوأ، وكلٌّ لها اختبار

1. **المسير من الشمعة التالية**: إشارةٌ تُقرأ عند الإغلاق لا تُنفَّذ داخله.
2. **الشمعة التي تلمس الوقف والهدف معاً وقفٌ لا هدف** — الشمعة اليومية لا
   تقول أيّهما أوّلاً، والافتراض المعاكس يمنح النظام صفقاتٍ رابحة لم تقع.
3. **الفجوة تُنفَّذ عند الفتح لا عند المستوى** — وهو بالضبط ما لا يحمي منه
   وقفُ الخسارة، فحسابه عند الوقف يخفي أسوأ ما يحدث فعلاً.
4. **الدخول المعلَّق يُملأ عند مستواه** لا عند الفتح الأرخص منه.
5. **ثلاثة مقامات لا واحد**: التفعيل مقامه كل الإشارات، والوقف والأهداف
   مقامها ما فُعِّل، والهدف الثالث مقامه الخطط ذات الأهداف الثلاثة.
6. **وثلاث سياسات خروج من محاكاةٍ واحدة** عبر `retAt`: الخروج عند T1 أفضل
   من T3 (‎−0.60‎ مقابل ‎−0.65‎) — والحدس يقول «دع الأرباح تجري».

> **وتغيير أيٍّ من هذا يُبطل الأرشيف** — أعِد بناءه (`node local/run.mjs
> backtest`، ~505 طلب، عشر سنوات). والمقارنة الذهبية شرطُ قبول: تشغيلٌ
> بـ`BT_RANGE=5y` بلا تكاليف يجب أن يعيد الأرقام السابقة بالضبط.

### الأرشيف يحكم على الشروط — وقد حكم على ثلاثة بالسقوط

قياسٌ على **482,418 شمعة** و499 رمزاً وخمس سنوات، والحافة تُقاس على خط
الأساس المطابق للإشارة (صعودي أو هبوطي):

| الشرط | الحالات | وسيط 20ي | النجاح | الحافة |
|---|---|---|---|---|
| تباعد صاعد ▲ | 4,114 | ‎+2.09%‎ | 59.8% | **‎+1.01‎** |
| قرب قاع 52 أسبوعاً | 13,551 | ‎+1.76%‎ | 59.0% | ‎+0.68‎ |
| تباعد هابط ▼ | 6,564 | ‎−0.59%‎ | 46.9% | ‎+0.49‎ |
| حجم غير معتاد | 27,836 | ‎+1.34%‎ | 56.9% | ‎+0.26‎ |
| تشبّع بيعي في اتجاه صاعد | 216 | ‎+1.03%‎ | 55.6% | ‎−0.05‎ |
| توافق الفريمات ▲ | 55,022 | ‎+0.80%‎ | 54.5% | ‎−0.28‎ |
| توافق الفريمات ▼ | 36,802 | ‎−1.50%‎ | 42.6% | ‎−0.42‎ |
| قرب قمة 52 أسبوعاً | 19,282 | ‎+0.64%‎ | 53.9% | ‎−0.44‎ |

- **أكثر الشروط تحقُّقاً أسوأُها حافةً.** «توافق الفريمات ▲» يُحقَّق 55 ألف
  مرة وحافتُه ‎−0.28‎، و«قرب قمة 52 أسبوعاً» ‎−0.44‎. وأفضلُها شرطان
  للارتداد لا للاستمرار. فمطاردةُ الزخم لا تُدفَع في هذا الكون — وهذه
  ليست رأياً بل قياساً، والواجهة تعرض الحافة السالبة ولا تخفيها.
- **ثلاثة شروط جُرّبت وسقطت — لا تُعَد تجربتُها** (مشروحةٌ في `scans.js`):
  «توافق مع قوّة اتجاه ▲» ‎−0.33‎ و«▼» ‎−0.62‎ و«انضغاط قبل الحركة» ‎−0.48‎.
  الفرضية كانت أن تقييد التوافق بـ ADX ≥ 25 يُبقي الحركة ذات الاتجاه
  وحدها، فجاءت النتيجة **عكسها**: التقييد يجعل الحافة أسوأ لا أفضل،
  وهو متّسقٌ مع سلبيّة شروط الاستمرار كلها.
- **الانضغاط لا يمكن التحقّق منه في هذا الإطار أصلاً**: تمهيدٌ لحركةٍ
  **بلا جهة**، والإطار يقيس عائداً باتجاه. وقرنُه بميلٍ صاعد يورّثه
  سلبيّةَ الميل — فرقمُه يقيس الميل لا الانضغاط. فبقي معلومةً معروضة
  ولم يبقَ إشارة.
- **قاعدة مستخلَصة**: قوّة الاتجاه تُعرَض ولا تُرشَّح بها الفرص. عرضُها
  يفيد القارئ، وترشيحُ السهم بناءً عليها يضرّه.
- **وتغييرُ `scans.js` أو تعريف النتيجة يُبطل الأرشيف** — أعِد بناءه
  (`node scripts/backtest.mjs --out data`، ~500 طلب) وإلا صار يقيس شرطاً
  غير الذي يُعرض.

### اختبارٌ يثبّت رقماً يُسقِط نفسه عند كل إضافة

`SCANS.length !== 8` و`eq(dn, ["alignDn"])` كانا يُسقطان الفحص عند إضافة
أيّ شرط. وهذا **يدفع إلى تخفيف الفحص بدل قراءته** — وهو أسوأ ما يمكن أن
يفعله اختبار. صارا يفحصان الخاصيّة: حدٌّ أدنى للعدد، ولا معرّف مكرّر،
وكلُّ شرطٍ وسمُه فيه ‎▼‎ يحمل `dir = −1` وكلُّ ما فيه ‎▲‎ لا يحمله.

### فحوصٌ بنيوية أُضيفت لأنها كشفت خللاً فعلياً

- **وسومُ السكربت مقابل `CORE` في `sw.js`.** أضفتُ `score.js` ثم
  `indicators.js` ونسيتُهما من قائمة ما يُخزَّن، فالعملُ بلا شبكة يعرض
  صفحةً بلا نصف شيفرتها — ولا يظهر ذلك على الشبكة أبداً. و`check-ui`
  يقارن القائمتين ويقرأ نسخة `V`.
- **لا اسم معرَّف في الصفحة وفي ملفٍ مشترك معاً.** السكربتات الكلاسيكية
  تتشارك نطاقاً واحداً، فالأخيرُ تحميلاً يغلب **بلا أيّ خطأ**: `pctRank`
  كانت في `indicators.js` بدلالة ‎(series, win)‎ وفي `index.html` بدلالة
  ‎(sorted, v)‎، فصار `analyze` يحسب انضغاط بولنجر بدالّة الأساسيات.
  أُثبت أن الفحص يُسقط نفسه بإعادة التعارض ثم رفعه. والاسم الآن
  `rankInWindow`.
- **وفحصُ النسخ المكرّرة صار يقرأ البصمة لا الاسم**: نسخةُ النتيجة كانت
  **مضمَّنةً بلا اسم** داخل `analyze` فمرّت من فحصٍ يبحث عن
  `function scoreFrom`.

### مصائد التعبيرات النمطية المبنيّة نصّاً

- **`"\s"` داخل نصٍّ جافاسكربتي حرفُ ‎s‎ لا صنفُ المسافات.** فـ
  `new RegExp("([\s\S]*?)")` مكتوبةً بشرطةٍ واحدة تصير `([Ss]*?)`،
  فتعيد كلَّ حقلٍ `null` **بلا أيّ خطأ**. الشرطتان إلزاميتان.
- **`<tag(?:[ >][^>]*)?>` تبتلع العنصر كاملاً.** عند
  `<transactionCode>S</transactionCode>` يطابق `[ >]` قوسَ الإغلاق ثم
  تمتدّ `[^>]*` على `S</transactionCode` ثم يطابق `>` القوسَ الثاني —
  فيصير «وسم الفتح» هو العنصر كلّه، ويبحث ما بعده عن الإغلاق **التالي**
  في العنصر التالي. والسمات تلزمها مسافة: `(?:\s[^>]*)?`.
  - **والأخطر أن الخلل لا يظهر في مستندٍ فيه عنصر واحد**: لا إغلاق ثانٍ
    فيفشل المسار الطامع ويرتدّ المحلّل إلى المطابقة الصحيحة. فمرّ على
    إيداعَين من أربعة — **ونجاحُ نصف العيّنة أخطر من فشلها كلّها**.
    فالمستند التجريبي في الفحص متعدّد العناصر بالضرورة.

### إيداعات SEC — المصدر الأوّلي

- **كل خلاصات الأخبار ثانوية.** قياس 2026-09-13: أحدث خبرٍ عمره 133 دقيقة
  والوسيط 28 ساعة. وEDGAR هو الحدث نفسه، يظهر لحظة قبول الإيداع.
- **`getcurrent` لا خلاصةٌ لكل شركة**: الثانية 18.6 ك.ب × 89 شركة =
  1.65 ميغابايت في كل دورة وأغلبها بلا جديد. والأولى طلبٌ واحد بـ74 ك.ب
  يحمل أحدث مئة إيداع في السوق، تُرشَّح على الكون. والمئة تغطّي **2.7
  ساعة** لـ8-K و3 ساعات لـForm 4 — ستّة عشر ضعف الدورة.
- **البند هو المعلومة**: ‎2.02‎ نتائجُ ربعٍ تحرّك السهم في دقائق، و‎9.01‎
  مرفقاتٌ لا حدثَ فيها. 22 بنداً بالعربية بوزن ‎1..3‎، و**الأهمّية أعلى
  البنود لا مجموعها** — إيداعٌ بأربعة بنودٍ شكلية كان يتقدّم على إيداع
  ببند ‎2.02‎ وحده.
- **الإيداع الواحد يحمل عشرات العمليات**: إيداعُ الرئيس التنفيذي لـ CRWD
  ستّة عشر بلوكاً مجموعُها 20,000 سهم، وقراءةُ أوّلها تقول 4,400 — رقمٌ
  صحيح لعمليةٍ وخاطئ للإيداع. فيُجمَع لكل رمزِ عمليةٍ عددُه، والسعرُ
  **مرجَّحٌ بالأسهم**، والرمزُ الغالب صاحبُ أكبر عدد أسهم لا أوّلُ بلوك.
- **الشراء وحده إشارة** (`sig`): البيع له عشرة أسباب مشروعة (ضرائب
  الاستحقاق، تنويع، خطّة ‎10b5-1‎ مبرمجة قبل شهور) والشراء له سببٌ واحد.
  والمنحُ (A) وتنفيذُ الخيار (M) وحجزُ الضريبة (F) ليست تداولاً أصلاً —
  عرضُها «شراء مطّلع» كذبٌ بحسن نيّة. و`sig` يقرّرها **الخادم** ويحفظها،
  فاشتقاقُها في الواجهة أيضاً يجعلها تسمّي عمليةً «شراء» والسجلَّ لا يعدّها.
- **العمليات المشتقّة لا تُعدّ مع العادية**: وحدةٌ مقيّدة لم تُستحقّ بعد
  ليست سهماً مملوكاً، وجمعُهما يخلط أداتين.
- **تراكميّ كـ`wide.json`** بمفتاح رقم القبول (accession) — لا الرابط
  (يتغيّر شكلُه بين الواجهات) ولا الزمن (إيداعان في الثانية نفسها عادي).
  والتنقيح **بالعمر** لا بالعدد: سقفُ عددٍ يحذف أقدمَ إيداعٍ لشركةٍ هادئة
  قبل أحدثِ إيداعٍ لشركةٍ ثرثارة.
- **بوابتان**: صفرٌ من التيار كلّه يعني خللَ شبكة لا سوقاً هادئة (ففي
  أهدأ ساعة تودع شركاتٌ أمريكية شيئاً)، والتقلّصُ إلى أقلّ من النصف يعني
  خطأ دمجٍ لا تنقيحاً. وحصّتُنا من التيار **قد تكون صفراً بحقّ** في عطلة
  نهاية الأسبوع فلا تُشترط.
- **خريطة CIK تُخزَّن أسبوعياً**: 600 ك.ب لعشرة آلاف رمز لا تُطلب كل عشر
  دقائق. وتغطيتُها 500 من 501 (الناقص `MMC` — وهو نفسه الرمز الذي لا
  يعطي ياهو له بياناتٍ، واتّفاقُهما يرجّح أن العلّة في الرمز لا فينا).

### التحليل المالي — نصفُ الاستجابة كان يُرمى

`quoteSummary` يعيد `financialData` و`defaultKeyStatistics` في **نفس
النداء** الذي يجلب المكرر والهامش، وكنّا نستخرج ‎26‎ حقلاً ونرمي الباقي.
صارت ‎51‎ حقلاً بكلفة **صفر طلبات** — الديون والنقد والتدفّق الحرّ
والهوامش التشغيلية و‎EV/EBITDA‎ و‎PEG‎ ونسبة التوزيع والمِلكية المؤسسية
والبيع على المكشوف ومدى هدف المحلّلين.

- **الملاءة بعدٌ رابع في النتيجة الأساسية، وكان غائباً كلّه.** كانت
  جودةً ونمواً وتسعيراً — وترتيبُ الشركات بالربحية وحدها يضع أكثرها
  استدانةً في القمّة، وهي أوّل من يسقط عند أي ضائقة. و**ROE مرتفعٌ
  بسبب الدَّين ليس جودة**: المقام صغيرٌ لا البسط كبير، ولذلك يُعرض
  ROA بجانبه — تباعدُهما هو أثر الرافعة معروضاً.
- **والديون تُقارَن بالقطاع كذلك**: نسبةٌ تقتل شركة برمجيات وتُعدّ
  متحفّظة في مرفقٍ عام.
- **`de` عند ياهو نسبةٌ مئوية**: ‎78.4‎ تعني ‎0.78×‎. عرضُها كما هي
  يضاعفها مئة مرة — نفس مصيدة `divY` بمقياسين.
- **البنك لا يُقاس بمقياس المصنع.** عشرة رموز مالية (JPM، BAC، WFC،
  MS، GS، AXP، C، SCHW، KKR، BX) لا تُعلن `fcf` ولا `de` ولا `cr` ولا
  `ebitda` — ميزانيتها مبنيّة على الإقراض لا على الإنتاج. فتُحذف صفوفها
  ويُعلَن أن الدرجة من ثلاثة أبعاد لا أربعة، ولا تُعطى صفراً كأنّها
  أسوأ من كل أقرانها.

#### ثلاث مصائد كلّفت تشخيصاً في هذه البطاقة وحدها

1. **`isFinite` العالمية — وقعتُ فيها رغم أنها موثّقة هنا.** كتبتُ
   `!isFinite(f.fcf)` في `fcfYield`، و`isFinite(null)` تعيد `true` لأن
   `null` تُحوَّل صفراً. فظهر لكل بنكٍ «عائد تدفّق حرّ ‎0.0%‎» — وهو
   **أسوأ من «—»** لأنه يُقرأ «لا نقد يدخلها» بدل «لا نعرف»، وأفسد
   وسيط القطاع المالي كلَّه معه. `Number.isFinite` دائماً.
2. **`freeCashflow` و`operatingCashflow` قد يُحسبان على فترتين مختلفتين**
   عند ياهو، فيخرج الحرُّ أكبر من التشغيلي وتصير «الحصّة الرأسمالية»
   سالبة — بوينج ‎−55%‎، وسبعة رموز من التسعين خارج المدى ‎0..100‎.
   و«حصّةٌ سالبة من التدفّق» بلا معنى في أيّ تعريف محاسبي. تُعرض حين
   تتّسق حسابياً وحدها.
3. **`EV/EBITDA` سالبة ليست رخيصة.** بوينج ‎−67.4‎: ترتيبُها مع الموجب
   يضع أخسرَ شركةٍ على رأس «الأرخص». `rel()` تشترط `v > 0` فتسقط، وتُعرض
   «—» بسببها المعلَن بدل رقمٍ يُقرأ فرصة. ونفس الشرط يحمي مكرر الربحية
   السالب.

- **والنسبة المتناهية الصغر تُقال صغيرةً لا صفراً**: هامش بوينج التشغيلي
  ‎0.004%‎ كان يُعرض «‎0.0%‎» فيُقرأ «لا ربح». والفرق بين «ضئيل» و«صفر»
  هو الفرق بين شركةٍ تتعادل وأخرى تخسر.
- **ومدى هدف المحلّلين يُقال مع متوسّطه**: هدفٌ متوسّطه ‎320‎ ومداه من
  ‎215‎ إلى ‎400‎ ليس توقّعاً بل **غيابَ توقّع**، والمتوسّط وحده يخفي
  خلافاً لا يصفه.
- **والبطاقة تُفرَّغ عند إخفائها لا تُخفى وحدها**: بطاقةٌ مخفيّة تحتفظ
  بأرقام السهم السابق، وأيُّ مسارٍ يُظهرها بلا إعادة رسم يعرض شركةً على
  شاشة أخرى — وهي نفس مصيدة «الأرقام تبدو صحيحة وتخصّ سهماً آخر».
- **ورموز الطبقة الواسعة بلا أساسيات عندنا**، والبطاقة تنسحب لها بصمت
  كما تنسحب للعملات الرقمية — غيابُها الحالة الطبيعية لا خطأ.

### مؤشّرات الحجم — الحقل الذي نملكه ولا نستعمله

كل مؤشّراتنا كانت دوالَّ في السعر وحده (متوسطات، RSI، MACD، بولنجر،
ADX)، والحجم محفوظٌ في كل شمعة منذ اليوم الأول ولا يقرؤه إلا شرطٌ واحد.
فصعودٌ يشتريه الناس وصعودٌ يتركونه يبدوان في قراءاتنا **واحداً**.

- **OBV** التراكم — الاتجاه هو المعلومة لا القيمة (بلا وحدة). و`obvDiv`
  حين يتعارض ميلُه مع ميل السعر بحركةٍ ذات شأن (‎>2%‎ و‎>5%‎) لا بأيّ فرق.
- **MFI** «RSI مرجَّحٌ بالحجم» — نطاقه ‎0..100‎ ويزن كل يوم بقيمة ما
  تُدووِل فيه.
- **الستوكاستك** موضع الإغلاق داخل مدى النافذة — أسرع من RSI فيسبقه إلى
  الانعطاف ويُخطئ أكثر منه.
- **POC** أكثر سعرٍ تُدووِل عنده — كل مستوياتنا الأخرى مشتقّةٌ من **نقاط**
  (قمّة، قاع، بيفوت، متوسّط) وكلٌّ تصف لحظة. وهذا يصف أين تراكمت المراكز.
  والحجم **يُوزَّع على مدى الشمعة** لا يُكدَّس عند إغلاقها.
- **وتسقط كلُّها بصمت حين لا يعطي المصدر حجماً** (Twelve Data وStooq،
  و`Math.round(x.v || 0)` تصفّره). الفرق بين «تراكمٌ صفر» و«لا نعرف» هو
  نفس قاعدة «اعرض ‎—‎ لا رقماً ملفّقاً». والستوكاستك سعريّ فيبقى.
- **مصيدة `sma` مع `NaN`**: مجموعٌ متدحرج، وأوّل `NaN` يدخله يبقى فيه إلى
  آخر السلسلة. فـ‎%D‎ خرجت فارغةً كلَّها و‎%K‎ سليمة — خانةٌ ناقصة في
  الواجهة بلا خطأ ولا استثناء. الحلّ حلقةٌ صريحة تتخطّى غير المنتهي.

### «لماذا تحرّك؟» — تجميعٌ لا جلب

كل مكوّناتها موجودة منذ شهور ومتفرّقة على خمس بطاقات: التغيّر في
الترويسة، والحجم في الجدول، والإيداعات في بطاقة، والأخبار في أخرى،
والقطاع في شاشةٍ أخرى تماماً. ومن يرى سهماً هبط ‎6%‎ يسأل سؤالاً واحداً
ولا شاشة تجيبه.

- **الحركة تُقاس بمقياس السهم نفسه**: ‎+5.61%‎ في ANET ليست حدثاً (مداها
  ‎4.01%‎)، و‎+4.37%‎ في CSCO حدثٌ (مداها ‎2.47%‎). العتبة ‎1.5×ATR‎.
- **وحركةٌ عادية لا تُفسَّر** — البطاقة تنسحب كلُّها. اختراعُ سببٍ ليومٍ لا
  شيء فيه هو ما تفعله مواقع الأخبار المالية، وهو تدريبٌ للمستخدم على
  تصديق التفسيرات الملفّقة.
- **تزامنٌ لا سببية**: النصّ يقول «ما تزامن معها» والنافذة معلنة (48
  ساعة). والترتيب بقوّة الدلالة: ‎8-K‎ ثم الأرباح ثم شراء المطّلعين ثم
  الأخبار. وحركةُ القطاع كلِّه **تنفي** الخصوصية عن السهم فتُقال كذلك.
- **والحجم تأكيدٌ لا سبب**: «تحرّك ‎1.8×‎ مداه بحجمٍ ‎0.7×‎ متوسّطه» تقرأ
  حركةً بلا مشاركة واسعة — وهي معلومةٌ مختلفة تماماً عن نفس الحركة بحجمٍ
  مضاعف.

### الكريبتو حين يُغلق الأمريكي

التطبيق كان ساكناً ستّ عشرة ساعة من كل يوم وطوال نهاية الأسبوع — نفس
الأسعار ونفس القوائم — وبيانات الكريبتو محدَّثة كل عشر دقائق طوال ذلك.

- **مشروطةٌ بالإغلاق** (`st.state !== "REGULAR"`): حين يفتح الأمريكي فهو
  المنتَج، والبطاقة تنسحب ولا تزاحمه.
- **إظهارٌ لما هو مخفيّ لا خلطٌ للإحصاءين**: أرقامها منفصلة وتقول ذلك في
  سطرها، ولا تدخل الاتساع ولا المزاج ولا القطاعات ولا قوائم الرابحين —
  وهي القاعدة نفسها التي استُبعد بها الكريبتو أصلاً.
- **بالوسيط لا المتوسط**: عملةٌ تقفز ‎40%‎ تزيح المتوسط ولا تزيح الوسيط.
- والشروط تُقيَّم بـ`scans.js` نفسه، فما يظهر هنا هو ما يقيسه الأرشيف.
- **مصيدة**: `renderCrypto(st)` كانت تأخذ الحالة وسيطاً من `renderNow`،
  و`st` معرَّفةٌ في `renderTop` لا فيها. تُحسب الحالة **داخل** الدالة:
  دالّتان تُنادَيان في مسارين مختلفين، وتمريرُها من إحداهما يُسقط الأخرى.

### بصمات «التصميم المولَّد» — ثلاثٌ أُزيلت، والرابعة الإيموجي الباقي

هذه ليست أذواقاً: كلُّ بندٍ منها نمطٌ يجعل الواجهة تُقرأ قالباً جاهزاً.

- **الإيموجي**: سبعةٌ في التبويبات و45 في العناوين. ولا تستعمله منصّةٌ
  مالية مهنية واحدة. التبويبات صارت أيقونات SVG أحادية الخط (عرض 1.6،
  شبكة 24، مجموعةُ رموزٍ في `<defs>` و`<use>`)، والعناوين **علامةً واحدة**
  لا 45 أيقونة: خمسةٌ وأربعون أيقونةً تعيد المشكلة نفسها، أمّا شارةٌ
  رأسية على `h2::before` فتقول «هذا عنوان قسم» ولا تحاول تصويره.
- **التدرّج الأزرق→البنفسجي على 135°**: أشهرُ نمطٍ يشي بقالب، وكان يمتدّ
  إلى اسم التطبيق بـ`background-clip:text` وهو أوضحُ ما يشي به. صار لونَ
  تمييزٍ واحداً أهدأ وتدرّجاً بدرجةٍ واحدة على 180°، والبنفسجي محصوراً في
  دلالته الوحيدة: ما قبل الافتتاح.
- **الزجاج المشبَع** `blur(18px) saturate(1.4)`: و`saturate` **ليست
  تجميلاً بريئاً** — تُشبع ما يمرّ تحت الشريط، فالرقم الأخضر يزيد خضرةً
  بحسب ما يصادف مرورَه خلفه، والرقمُ الذي يتغيّر لونُه بما تحته يفقد
  دلالته. صار سطحاً شبه معتم بضبابٍ خفيف بلا تشبيع.
- والزوايا من ‎15px‎ إلى ‎9px‎: الحافّة الأحدّ تجعل الجداول تُقرأ كشبكة
  لا كبطاقاتٍ طافية، وهي استدارةُ الأدوات لا التطبيقات الاستهلاكية.
- **ولا خطّ أيقوناتٍ خارجي**: قاعدة «بلا اعتماد خارجي» تمنعه و`check-ui`
  يفحصها، فالأيقونات مرسومةٌ مسارات.

- **والنصف المنظَّف أسوأ من غير المنظَّف.** نُظّفت التبويبات والعناوين في
  جولةٍ سابقة وبقي عشرون إيموجي في الأزرار والصفوف («⬇ تصدير»، «➕ إضافة»،
  «🔎 رتّب»، «🛡️ احسب»، «🟢/⚪» للأهداف، «★/☆» للمتابَعة). والواجهة
  المنظَّفة نصفَها تُقرأ **تردّداً في التصميم لا قراراً** — مجموعةَ
  قراراتٍ متفرّقة لا لغةً واحدة.
  - والإيموجي ليس ذوقاً: شكلُه يتغيّر بين ويندوز وأندرويد وiOS، ولونُه لا
    يتبع لوحة التطبيق، وحجمُه لا يتبع حجم النصّ — فالسطر يبدو مختلفاً على
    كل جهاز.
  - **ثلاثة بدائل حسب المعنى**، لا بديلٌ واحد: أيقونةٌ مرسومة حيث المعنى
    **فعل** (تصدير، إضافة، ترتيب)، ونقطةٌ ملوّنة `.dot` حيث المعنى
    **حالة** (بلغ/لم يبلغ — وهي مفرَّغة تصير مملوءة فيُقرأ الفرق بالشكل لا
    باللون وحده)، ومحرفٌ طباعيّ (‎▲▼⇄‎) حيث المعنى **اتجاه** — وهذا يرث
    لون النصّ وحجمه ولا يختلف بين المنصّات.
  - و`.ic` تقيس بـ`em` لا بالبكسل، فتتبع حجم النصّ ولونه تلقائياً ولا
    تحتاج ضبطاً في كل موضع.
  - **وعلامات Markdown في نصوص الواجهة تُطبع حرفياً**: `**بحجمٍ خلفه**`
    ظهرت بنجومها في بطاقة المؤشّرات. الشيفرة تكتب HTML لا Markdown.

### الدخول المزدوج — ومصيدةٌ تُفرغ التجربة من معناها

- **‎81.6%‎ من الخطط لم تُفعَّل قط.** من 38 خطة لها لقطة: 31 `wait` وسبعٌ
  دخلت. الدخول عند ارتدادٍ لا يأتي غالباً، فالأرشيف كان يقيس قاعدةً تُلغي
  أربعة أخماس إشاراتها — **ولا شاشة تقول ذلك**. الرقم يُطبع الآن في كل
  تشغيل ويُعرض في شاشة السجل.
- **مسارٌ ثانٍ يُقاس بالتوازي** (`planPair` · `snap.b` · `outB`/`openB`)
  بدل تغيير القاعدة برأي. والتعديل في `planFrom` **سطرُ الدخول وحده**
  بخيار `atMarket`؛ كل ما بعده يتدفّق عبر نفس الشيفرة.
- **المصيدة**: البديهي هو إزاحة الدخول والوقف والأهداف بالتوازي. وهو خطأ
  مزدوج — يخترع مستوياتٍ لا وجود لها (الأهداف **مقاومات حقيقية**، والمقاومة
  عند 106 تبقى عند 106)، **ويُبقي العائد كما هو في المسارين** فيتطابقان
  رياضياً ولا يُقاس شيء. الصحيح: نفس المستويات، مخاطرةٌ أكبر، هدفٌ قريب
  يسقط، عائدٌ أسوأ. قِيس: NVDA ‎1.85 → 1.77‎ · AAPL ‎1.66 → 1.53‎.
- **الفصل بين دورتَي الحياة إلزامي**: تشاركُ `open` يجعل وقفَ الأول يغلق
  تتبّعَ الثاني — انتهاءٌ لم يقع. و`stillLive` تُبقي السجلّ في
  `signals.json` ما دام أيُّ مسارٍ فيه يُقاس.
- **و«ب» غائبة عمداً حين لا حاجز قريب**: المساران يتطابقان، وتسجيلُهما
  يضاعف الصفقة نفسها في مقامين.

### الشارت — ما يُرسم وما يُقصّ
- **خطوط الخطة تدخل في حساب المدى، والمستويات لا تدخل.** هدفُ أبل على بُعد
  3% كان يقع خارج نافذة فريم الساعة (‎y = −2‎) فلا يُرسم — وخطةٌ لا تُرى
  تُفرغ الميزة من سببها. أما قاع 52 أسبوعاً فيبعد عشرات بالمئة، وضمُّه
  يسحق الشموع في شريط رفيع.
- **لا عربية في الكانفس** — الوسوم `E`/`SL`/`T1` لاتينية كما `EMA` و`R1`،
  ووسمٌ لا يسع الخانة اليمنى (62 بكسل) يسقط رقمُه ويبقى حرفُه: شيبا إينو
  تحتاج ثماني خانات.
- **الطبقات تُخزَّن مؤقتاً** لأن `hoverAt` تنادي `drawChart` عند كل حركة
  مؤشّر، ومفتاحُها يحمل السعر فينقض عند حركة السوق.
- **`chartKey` يعيد الزوم عند تبديل الرمز أو الفريم**: مسارات الفتح متعدّدة
  ونسيانُ واحدٍ يترك زوم سهمٍ سابق على سهمٍ آخر.

### مصائد متأخرة أخرى
- **`options.json.rows` مصفوفة لا خريطة.** `O[sym]` يعيد `undefined` لكل
  رمز بلا خطأ ولا استثناء — سطرٌ ناقص بصمت. الفهرسة تُبنى بـ`new Map`.
- **ضبطُ ARIA داخل `go` وحدها يفوّت الإقلاع**: الشاشة الأولى مُعلَّمة في
  الهيكل فلا تُنادى `go`، فيبقى الشريط بلا `role` في الحالة التي يفتح
  عليها المستخدمُ التطبيقَ فعلاً. `syncTabs` تُنادى من الموضعين.
- **قائمة منع النشر لا قائمة سماح**: السماح يُسقط أي ملف بيانات جديد
  **بصمت** (404 على الويب وحده). و`check-ui` يقابل القائمتين فيمنع أن
  يُحجب ملفٌ تقرؤه الواجهة. المستبعَد 629 ك.ب: `i18n.json` و`cik.json`.
- **الأداء يُقارَن بالنسبة لا بالسعر، والمحاذاة بالتاريخ**: سلسلتان تبدآن
  في تاريخين مختلفين تُقارَنان على فترتين مختلفتين فيبدو الأطولُ عمراً
  رابحاً دائماً.
- **تنبيه الشرط يُقيَّم بـ`scans.js` نفسه** وإلا نبّه التطبيق على شرطٍ غير
  الذي يعرضه ويقيسه الأرشيف. وشرطٌ أُزيل معرّفُه يبقى التنبيه ولا يُطلق.
- **`window.state` غير معرَّف** — `state` ثابتٌ في نطاق السكربت لا على
  `window`. فحصٌ يكتب `window.state` ينتظر إلى الأبد بلا سبب ظاهر.

### حالة قائمة (لم تُحلّ)
**جدولة GitHub Actions لم تشتغل ولا مرة** — صفر تشغيل من نوع `schedule`
رغم أن المهمة نشطة والمستودع عام وغير مشتقّ، وجُرّبت إزاحة الدقائق.
التشغيل المحلي عبر Task Scheduler يتجاوز هذا كلياً.

> تحديث 2026-09-03: فرع `data` صار يتلقّى دفعات من المهمة فعلاً (آخرها
> 19:45 UTC)، فالجدولة تعمل الآن على الأقل بشكل متقطّع. لم أستطع تمييز
> `schedule` من `workflow_dispatch` بلا `gh` على هذا الجهاز.

### بيانات السحابة ناقصة لا معطّلة — والنشر المحلي هو الحل
قياس 2026-09-03 على فرع `data`: **56 رمزاً من 70 قديم**، وشمعات NVDA
عمرها ستة أيام، بينما السعر سعر اليوم. السبب معروف: Finnhub يعطي السعر
من السحابة، وYahoo محظور، وميزانية Twelve Data تكفي 14 رمزاً لكل تشغيل.

النتيجة أسوأ من غياب البيانات لأنها لا تبدو خطأً — شارت ومؤشرات ومستويات
تُحسب من شمعات قديمة تحت سعر حديث.

`node local/run.mjs publish` يدفع `data/` المحلي إلى فرع `data` بنفس
طريقة المهمة (فرع يتيم بدفعة `-f`) وبنفس بوابة السلامة. مهمة GitHub
تسحب فرع `data` قبل كل تشغيل، فما تنشره من جهازك يبقى ولا يُداس.

---

### الاتجاه مرّةً أخرى — حارسٌ موجود لكنه ميت

بلّغ مستخدم عن `AVGO`: ترويسة «▼ خطة هبوط» بهدف ‎289.96‎، وتحتها «منذ
إشارة **تباعد صاعد ▲**». والمشروع يحمل حارساً لهذا بالضبط منذ جولات —
وكان لا يُطلق أبداً في الحالة التي بُني لها:

```js
if (!sc.dir || …) return false;      // ✗
```

و`scans.js` تنصّ أن **غياب `dir` يعني ‎+1‎**. فكل شرطٍ صاعد قيمته
`undefined` ⇒ `!sc.dir` صادقة ⇒ **يُستبعد قبل أن يُختبر**. فلم يكن
الحارس يُطلق إلا حين تكون الخطة صاعدة ويطابق شرطٌ هابط. نفس مصيدة
«`bandOf` تعيد ‎0..4‎ والصفر صالح فالفحص بـ`null` لا بالصدق» — وهنا
`undefined` تعني قيمةً افتراضية لا غياباً. **القاعدة: حقلٌ غيابُه له
معنى لا يُفحص بالصدق أبداً.**

- **و`planDir` خاصيّةٌ على الشرط لا قاعدةٌ عامة.** الأرشيف يمنع التعميم:
  فرضُ اتجاه الشرط يفيد «تباعد صاعد» (‎−2.05% → +0.24%‎) و**يضرّ**
  «تباعد هابط» (‎−0.32% → −0.78%‎). قاعدةٌ عامة تُصلح واحداً وتُفسد
  ثلاثة. و`plan.js` تبقى رياضياتٍ خالصة: تأخذ `forced` رقماً ولا تستورد
  تعريفات الشروط — وإلا استورد الأرشيفُ الواجهةَ كي يحسب خطة.
  > **عُدِّل شرطُ القبول لاحقاً** — انظر «الحارس يقول الحقيقة والخطة
  > تشير إلى الجهة الأسوأ» أدناه. «خاصيّةٌ لا قاعدة» باقية، والمتغيّر
  > أن المقارنة صارت **مفروضٌ مقابل الحالي** لا مقابل الصفر، فانضمّ
  > `low52` و`pullback` و`oversold`، ثم `vol` بقرارٍ صريح رغم أنه لا
  > يدّعي جهة. وبقي `divBear` وحده خارجها — الأرشيف يعترض عليه.
- **وبعد الفرض يبقى التحذير**، ونصُّه يتبدّل: «شراء لأن تباعد صاعد ▲
  يفرض اتجاه خطته … والنتيجة الفنية تقول اتجاه هابط قوي». الفرض يغيّر
  الخطة ولا يُخفي الحقيقة — ونصٌّ لا يتبدّل يصف **عكس** ما جرى.
- **وسجلٌّ بُني بسياسةٍ سابقة يُغلق ولا يُحوَّل** (`conv: "dir"`)، وكذلك
  سجلّات شرطٍ أُزيل من `SCANS` (`conv: "gone"` — كانت 23: `squeeze` و
  `alignAdx` و`alignAdxDn`). و`conv` تُفحص **بالصدق** لا بمساواة
  `"long"`، وإلا تسرّبت القيم الجديدة إلى الإحصاء.

### فريم 4 ساعات كان يقيس نصف ما يسمّيه

`e200` **معدومة في 96 رمزاً من 96**. فتسقط بوابتا الاتجاه الأمّ (‎4.0‎
من ‎8.5‎) ويصير الفريم يقيس المدى القصير وحده **ويُسمّى اتجاهاً**،
ومقامه ‎4.5‎ فأصغر خطوةٍ فيه ‎11.11‎ — عاجزٌ بنيوياً عن التعبير عن ميلٍ
ضعيف فيسقط من `allTF` وإن كان له جهة. `MSFT` (‎+11.1‎) و`V` (‎−11.1‎)
كانتا تسقطان بهذا وحده.

- **والسبب ليس شبكياً**: الساعة تُطلب بمدى **سنتين** فتعود بآلاف
  الشمعات، ثم تُقصّ إلى `KEEP`، ثم يُشتقّ 4h من **المقصوصة** —
  ‎260/4 = 65‎ وEMA200 تحتاج 200. الاشتقاق من الكاملة **قبل** القصّ
  يعطي 260 بكلفة **صفر طلبات**، والكاملة تُمرَّر ولا تُخزَّن.
- **ولذلك لم تُخفَّف عتبة `allTF`.** بعد إصلاح المصدر صارت الفريمات
  الأربعة بمقام ‎8.5‎ وخطوة ‎5.88‎، فالعتبة موحّدة أصلاً. **إصلاح
  البيانات أولى من تخفيف الفحص** — والتخفيف كان سيوسّع فجوة «المعروض
  غير المقيس» بلا مقابل.
- **ومصيدة النقل**: `4h` ليس في `frames` فلا يمرّ بحلقة الجلب **ولا
  يُنقل من `prev`**. فكان يُعاد اشتقاقه من الساعة المقصوصة في كل تشغيلٍ
  لا تُجدَّد فيه الساعة — يهبط من 260 إلى 65 ويضيع e200 الذي بُني قبل
  دقائق، فيتذبذب طولُه بين التشغيلات ومعه نتيجة الفريم. **لا شيء يبدو
  معطلاً.** ينتقل من `prev` حين لا تتوفّر الكاملة، و`shortDerived`
  تُعامل الساعة كقديمة حين يكون مشتقُّها أقصر من 200.

### الأوزان تُلغي بعضها فتخرج النتيجة صفراً حتمياً

وزن بوابتَي الاتجاه الأمّ (‎2.5 + 1.5‎) يساوي **بالضبط** وزن بوابات
المدى القصير (‎1.0 + 1.5 + 1.0 + 0.5‎). فالقائد الصاعد المتراجع تُلغي
بواباتُه بعضها وتخرج نتيجته **صفراً حتمياً لا احتمالياً**: 31 فريماً
بصفرٍ تامّ، منها 29 بهذه المعادلة وكلُّها بالبصمة `long=±4 / short=∓4`.

والصفر لا يمرّ `allTF` صعوداً ولا هبوطاً، فيسقط الرمز من «توافق
الفريمات» **بشقّيه** — وهما أكثر الشروط تحقّقاً. أصاب NVDA وAMZN وLLY
وRTX وIBM وAMGN وUNP دفعةً واحدة.

- **المعالجة شرطٌ يلتقط الحالة لا تعديلُ أوزان**: تعديلُ الأوزان يحرّك
  510 رمزاً ويُبطل كل قياسٍ سابق كي يعالج حالةً واحدة.
- **و`pullback` حافتُه ‎−0.04‎ داخل العيّنة وخارجها** على 38,497 حالة —
  منعدمة. يبقى معروضاً بحافته مكتوبةً، وهو مع ذلك أفضل من أربعةٍ من
  الثمانية. **يحلّ مشكلة الرؤية ولا يدّعي حافة.**

### الحارس يقول الحقيقة والخطة تشير إلى الجهة الأسوأ

بلّغ مستخدم عن «توصية شراء وأهدافها نزول». والمقيس: `NVDA` تحت شرط
**«ارتداد داخل اتجاه صاعد»** — وهو شرطٌ لا يُطلق إلا حين `p > e200`
و`e50 > e200` — وفوقها **«▼ خطة هبوط»**، ومعها تحذيرٌ يقول بالنصّ إن
توقّع المتناقضة ‎−0.99%‎ مقابل ‎−0.39%‎ للمتوافقة.

أي أن التطبيق كان **يعرف** أن الجهة المعروضة أسوأ بقياسه هو، ويعرضها.
تسعة عشر سجلاً مفتوحاً كذلك، تسعةٌ منها `pullback`.

- **السبب شرطُ قبولٍ يخلط سؤالين.** كانت القاعدة «لا يُفرض إلا إن خرج
  المفروض موجباً»، فلم يُفرض إلا «تباعد صاعد» (‎+0.24‎). وهي تخلط «هل
  هذا الشرط مربح؟» — ولا واحد منها مربح، وخط الأساس ‎−0.60‎ — بـ«الخطة
  ستُعرض على أي حال، فإلى أين تشير؟». والمقارنة الصحيحة **مفروضٌ مقابل
  الحالي** لا مقابل الصفر: تركُ الخطة تشير إلى الجهة الأسوأ لأن
  الجهتين خاسرتان ليس حياداً بل اختيارُ الأسوأ منهما.
- **الشرطان الجديدان مجتمعان**: موجَّهٌ **بتعريفه** (لا بوسمه)،
  والأرشيف لا يعترض (مفروض ≥ الحالي). فأُضيف `planDir: 1` إلى
  `low52` (‎−0.95 → −0.05‎ · 27,385 حالة) و`pullback` (‎−0.41 → −0.39‎)
  و`oversold` (بلا تغيّر، والفرض بنيويّ). وسقط `divBear` بالثاني
  (‎−0.40‎) — وهو ما يمنع التعميم.
- **و«حجم غير معتاد» حالةٌ ثالثة فُرضت بقرارٍ صريح: الأرشيف يؤيّد
  والتعريف لا يؤيّد.** ما يؤيّده أكبرُ عيّنةٍ بين المفروضات (57,193)
  وكلُّ المقاييس تتحرّك معاً — التوقّع ‎−0.72 → −0.34‎، وضربُ الوقف
  ‎71.7% → 67.8%‎، والربح ‎28.3% → 32.2%‎ — والشريحةُ المتناقضة أسوأُ
  شريحةٍ في الأرشيف كلّه (‎−1.42‎ على 17,088 بوقفٍ يُضرب ‎77.8%‎).
  وما لا يؤيّده أن الشرط **لا يدّعي جهة**: الحجم الشاذ يرافق الانهيار
  كما يرافق القفزة، فتُعرض خطةُ **شراء** على سهمٍ ينهار بحجمٍ مضاعف.
  التحفّظ يُسجَّل ولا يُمحى، و**الفرض يغيّر الخطة ولا يُسكت الحارس**:
  `ORCL` (نتيجة ‎−71.8‎، يُطلق `vol` و`alignDn` معاً) تعرض «▲ خطة صعود»
  وفوقها «ظهر بشرط توافق الفريمات ▼ … والخطة شراء لأن حجم غير معتاد
  يفرض اتجاه خطته … والنتيجة الفنية تقول اتجاه هابط قوي». ومن يقرأ
  التحذير يرى الحالة الواحدة لا الشريحة — وهذا هو ثمنُ الفرض ومقابلُه.
- **وتحفّظٌ قياسيّ يخصّ المفروضات الخمسة جميعاً**: الأرشيف لا يفصل خارج
  العيّنة **لمسارات الخطة** — `oos` عنده للعوائد وحدها. فكلُّ أرقام
  «مفروض» داخل العيّنة على عشر سنوات. حدُّ الأداة لا حدُّ شرطٍ بعينه.
- **ولا يحتاج الأرشيف إعادة بناء.** `planDir` لا يمسّ `test`/`btTest`،
  فمجموعة الإشارات والحوافّ والتركيبات كما هي. والمسار «ج» محسوبٌ
  بـ`of(sd)` وهو **نظير `own()` بعد الفرض حرفياً** — فالعمودان محفوظان
  معاً في كل تشغيل، والمتغيّر أيُّهما يُسمّى «التوقّع». وجدول الأرشيف
  صار يقرأ عمود السياسة النشطة ويسم الشرط المفروض، وإلا وصف سياسةً
  أُلغيت: رقمٌ صحيح يجيب سؤالاً لم يعد يُطرح.
- و`conv: "dir"` أغلق عشرة سجلات بُنيت بالسياسة السابقة، وتُعاد صحيحةً
  في الدورة التالية — المسار المصمَّم لهذا بالضبط.

### البوابة كانت قبل التقريب، والمحفوظ بعده

`snapFor` تفحص الخطة بـ`validatePlan` ثم تبني **ما يُحفظ فعلاً** بأرقامٍ
مقرَّبة — فأيُّ تشويهٍ يقع بعد الفحص يمرّ بلا اعتراض. وهو ما وقع:
`track-signals` كانت تقرّب بـ`r4` (خانات عشرية ثابتة)، وهو **التقريب
الذي يمحو الأصول الرخيصة** — موثَّقٌ منذ جولات ومُصلَحٌ في
`fetch-market` و`fetch-quotes` بـ`rp` (أرقام معنوية) ولم يُصلَح هنا.

فخرجت لقطة `SHIB-USD` (‎0.00000529‎) بدخولٍ **صفر** ووقفٍ صفر وATR صفر
وبلا هدفٍ واحد: **صفقةٌ محفوظة في السجلّ مخاطرتُها غير موجبة**، وكلُّ
نسبةٍ تُبنى عليها قسمةٌ على صفر. ولم يكشفها تشغيل — كشفها فحصٌ مستقلّ
مرّر اللقطات المحفوظة كلَّها على `validatePlan`.

- **القاعدة أعمّ من التقريب: ما يُفحص يجب أن يكون ما يُكتب.** فحصٌ على
  تمثيلٍ وسيط يشهد لشيءٍ لا يصل القرص. فالبوابة صارت تُعاد على اللقطة
  نفسها بحقولها المحفوظة — رخيصةٌ (مقارناتُ إشارة) وتُغلق الباب على كل
  مشوِّهٍ قادم لا على هذا وحده.
- **و`rp` صارت في `scripts/lib/round.mjs`** يستوردها الثلاثة. كانت
  مكرَّرةً عمداً في اثنين، وعلّةُ التكرار المعلنة أن اختلافهما يجعل
  السعر يقفز بين الدورتين — أي أن **التباعد هو الخطر المعروف** ثم تُرك
  بابُه مفتوحاً، فجاء ثالثٌ ووقع فيه. ولا تُستورد من `fetch-market`:
  استيرادُه يشغّل `main()` فيجلب الكون (357 طلباً قِيست).
- **واللقطة المكسورة تُغلق ولا تُصحَّح** (`conv: "snap"`): `snap` تُكتب
  مرّة ولا تُلمس، وإعادةُ بنائها بأسعار اليوم تجعل السجلّ يدّعي أنه رأى
  ما لم يره.
- والفحص يمرّ بسعرٍ حقيقي لا رمزيّ، ويفحص الطرفين: أن `rp` تُبقي
  المعنى، وأن **الفرق بين الدخول والوقف يبقى موجباً بعد التقريب** —
  وهو الشرط الذي انكسر. أُثبت أنه يُسقط نفسه بإعادة التقريب الثابت.

### الحارس أعمى تجاه شرطٍ واحد — `{ secMed: {} }`

حارسُ التناقض يعيد تقييم `SCANS` ليقول أيُّ شرطٍ يخالف الخطة، ويمرّر
خريطةَ وسائط القطاعات **فارغة**. و`cheap` وحده يقرؤها، فيعيد `false`
دائماً: لا خطأ ولا استثناء، فقط شرطٌ من أحد عشر لا يمكن أن يظهر في
التحذير أبداً. قِيس: `UBER` تحت «أرخص من قطاعه ▲» وخطةُ بيع بلا تحذير.

نفس عائلة «الحارس الميت»: موجودٌ ويُنادى ولا يُطلق في الحالة التي بُني
لها. والسياق الآن سياق شاشة الفرص نفسه (`fundScores().secMed`) كما في
التنبيهات — و`{ secMed: {} }` تبقى **صحيحة** في بطاقة الكريبتو وحدها،
لأن الكريبتو بلا قطاع ولا أساسيات أصلاً.

### `conv` كان يُفحص بمساواة قيمةٍ واحدة من ثلاث

القاعدة موثّقة («تُفحص بالصدق لا بمساواة `"long"`») ومطبَّقة في
`track-signals` وحده. أما الواجهة فكانت تسأل `conv !== "long"` في أربعة
مواضع — وقيمُ الحقل صارت ثلاثاً: `"long"` و`"dir"` (سجلٌّ بسياسة اتجاهٍ
سابقة) و`"gone"` (شرطٌ أُزيل من `SCANS`).

- **القياس: 24 من 37 صفقة مغلقة محسوبة كانت مُحوَّلة** — أي أن **ثلثي**
  مقام «نسبة النجاح» لا يصف ما يُعرض. عشرة لـ`squeeze` وثلاثة عشر
  لـ`alignAdx`/`alignAdxDn` (بلا تعريف اليوم فبلا وسمٍ ولا حافة)،
  وواحدٌ بُني باتجاهٍ معاكس فعائده مقلوب الإشارة.
- و`renderTrackedLevels` كانت بلا فحص `open` أصلاً، فتلتقط سجلاً مغلقاً
  محوَّلاً وتقيس منه المستويات.
- **القاعدة: حقلٌ مجموعةُ قيمه مفتوحة لا يُفحص بواحدةٍ منها.** وهي نفس
  عائلة «الصفر صالح فالفحص بـ`null`» و«`undefined` تعني ‎+1‎». و
  `check-ui` يمنع النمط عند جذره بدل انتظار القيمة الرابعة.

### الانسحاب أمام القفل نجاحٌ كاذب لا أثر له

المنسحبة تخرج بصفر عمداً (كي لا تُعلَّم المهمة فاشلة كل دورة متداخلة)،
فتجتمع ثلاث شهادات تقول إن كل شيء سليم — المهمة نشطة، وآخر نتيجة
‎0x0‎، ولا سطر في أي سجل — والبيانات تتقادم.

قياس 2026-09-14 **بعد** إصلاح الإزاحات: مهمة العقود ‎19:35‎ خرجت
‎rc=0‎ ولم تكتب، وآخر كتابةٍ ‎19:06‎ — دورةُ نصف الساعة صارت ساعة.
فالإزاحات تُباعد بين **البدايات** وحدها، وتفترض أن كل مهمة تنتهي قبل
بداية التالية: افتراضٌ يسقط كلما بطؤت الشبكة.

`.run.skips.json` تسجّل كل انسحاب بأطرافه الثلاثة — من انسحب، وأمام
من، وبعد كم ثانية — فيصير «لماذا تقادمت العقود؟» سؤالاً يُجاب في سطر.
آخر خمسين وحدها (تشخيصٌ لا أرشيف)، ومستبعدةٌ من النشر: الموقع المنشور
لا جدولة له.

### الطبقة الواسعة تُمسح بما يقدر عليها

`renderScreen` كانت تمرّ على `summary.rows` وحدها، فـ‎414‎ رمزاً خارج
كل شرط — و`CRWD` لم يكن يمكن أن يظهر إطلاقاً وهو ‎+15%‎ عند قمّته.

- **الوسم `wide` بالقياس لا بالظنّ**: أُجري كل شرط على الـ414 فعلياً،
  فمرّت `divBull` و`divBear` و`high52` و`oversold` (و`pullback` بعدها)،
  وخرج الباقي بصفر لأنه يقرأ `f.roe` أو `f.avgVol` أو الفريمات الأربعة.
  **وسمُ ما لا يعمل يملأ القائمة بلا شيء ويُقرأ عطلاً.**
- **والمقام ما مُسح فعلاً**: «17 من 510» لا «17 من 96».
- **والرمز المرقّى يخرج من `wide.json`** — الملف تراكمي فلا يخرج وحده،
  ولو بقي لظهر **مرّتين** في `allRows()`: صفٌّ بأربعة فريمات وآخر بفريم
  واحد. وبوابة التقلّص تقارن بما كان **بعد** استبعاده.
- **والترقية تضيف ولا تُزيح**: `top` من 70 إلى 71، وإلا خرج `UBER` من
  المرصودة وهو مرشّحٌ أساسي فليس في الواسعة — أي يختفي كلياً.

### اختبارٌ يثبّت رقماً — المصيدة تتكرّر

`SCANS.length !== 8` عولجت سابقاً، وبقي مثلها **اثنان**: `!== 90` في
`build-universe` و`fetch-market`. أيّ إضافةٍ تُسقط الفحص، وهذا **يدفع
إلى تخفيفه بدل قراءته**. يُفحص الخاصيّة: يكفون `top`، ولا معرّف مكرّر،
ولا رمز في الطبقتين معاً. **عند إضافة أي رمز أو شرط، ابحث عن رقمٍ
مثبَّت قبل أن تُخفّف فحصاً.**

### مهمّة معرَّفة ليست مهمّة مسجَّلة

`WebTrade-Filings` معرَّفة في `schedule.ps1` ولم تُسجَّل في المجدول قط —
لم يُعَد تشغيل السكربت بعد إضافتها. فالإيداعات عمرها **37.3 ساعة على
دورة عشر دقائق**، والمجدول يحمل أربع مهام لا خمس. `schedule.ps1` فيه
`Read-Host` فلا يعمل في جلسةٍ غير تفاعلية — تُسجَّل الناقصة وحدها
بنفس وسائطها. **بعد إضافة مهمة إلى `schedule.ps1` تحقّق من
`Get-ScheduledTask -TaskName WebTrade-*` فعلياً.**

### المنفذ 8080 قد لا يكون هذا المشروع

نسخةٌ ثالثة من المشروع تعمل في `D:\Ai\Gpt\trade-web-codex` بتاريخ git
مختلف تماماً، وتشغل 8080. فالفحص في المتصفح كان يقرأ شيفرةً **ليست
هذه** — `scans.js` بحجمٍ آخر بلا الحقول الجديدة. `autoPort` في
`.claude/launch.json` يعطي منفذاً حرّاً. **تأكّد أن ما يعرضه المتصفح هو
ما على قرصك قبل أن تشخّص «التعديل لم يُطبَّق».**

---

---

## ماسح الاستراتيجيات — عشر قراءاتٍ مستقلّة بدل نتيجةٍ واحدة

التطبيق كان يجيب «ما حال هذا السهم؟» بنتيجةٍ فنية واحدة، ولا يجيب
«كم طريقةً مستقلّة ترى نفس الجهة الآن، ومتى بدأت، وهل فات وقت
الدخول؟». والفرق بين إشارةٍ عمرها خمس دقائق وأخرى عمرها ثلاث ساعات
تحرّك فيها السهم ‎4%‎ هو الفرق بين صفقةٍ وخسارة.

`stocks/strategies.js` عشرُ استراتيجيات في أربع عائلات (جلسة · استمرار
· ارتداد · تقلّب وحجم)، و`stocks/consensus.js` يجمعها.

### المشغِّل ثم البوابات — فصلٌ مقصود
`side(c)` يقرّر الجهة أو `0` فلا تتفعّل الاستراتيجية أصلاً، ثم `gates`
تقيس **قوّة التأييد** لتلك الجهة (‎+1‎ تؤيّد · ‎−1‎ تناقض · ‎0‎ حائرة)
والنتيجة مجموعُها المرجَّح منقولاً إلى ‎0..100‎. وخلطُهما يعطي رقماً
متوسّطاً لا يُعرف أهو إشارةٌ ضعيفة أم لا إشارة — وهما قراران مختلفان.

### بوابةٌ واحدة بإيقاعين — لا نسختان من الرياضيات
كل بوابة موسومة `price` أو `ind`. دورة السوق (عشر دقائق) تحسب الكل،
ودورة الأسعار (دقيقتان) تحسب السعرية وحدها وتأخذ تصويت المؤشرية من
الدورة السابقة — **بنفس الدالّة** (`evalGates` بـ`only`). فلا مسارٌ
سريع يتباعد عن المسار الكامل.
- **وبوابةٌ مؤشرية بلا سابقة تُسقَط ولا تُعامَل محايدة**: «لا نعرف»
  ليست «متعادلة»، وإضافتها إلى المقام بصفرٍ تخفض كل نتيجةٍ بلا سبب.
- وشرطُ القبول مقارنةٌ ذهبية: بنفس المدخلات يعطي المساران نفس الرقم
  بالضبط. أُثبت أن الفحص يُسقط نفسه بكسر الآلية.
- ولماذا لا تُعاد المؤشّرات كل دقيقتين: القاعدة الموثّقة — مؤشّرٌ من
  سعرٍ واحد بلا شمعةٍ جديدة رقمٌ كاذب. تُجمَّد وتُعلَن طزاجتُها.

### الفريم الخامس يُحلَّل ولا يدخل النتيجة — حارسٌ صريح
`overallScore` و`tfScore` يدوران على `TFS`، و`allTF` في `scans.js`
تشترط `v.length === 4` **بالضبط**. فإضافة ‎5د‎ إليها تغيّر نتيجة كل
رمزٍ في الكون وتُبطل الأرشيف وتُسقط شرطَي «توافق الفريمات» **بصمت**.
لذلك `AN_TFS` قائمةٌ منفصلة في `fetch-market`: `an["5m"]` يُكتب ولا
يراه أحدٌ غير الماسح، وفحصٌ في `--check` يحرس ذلك ويسقط إن تسرّب.
- **وبلا `prePost`**: نطاق الافتتاح وVWAP يحتاجان الجلسة الرسمية وحدها،
  و260 شمعة تصير 3.3 يوم تداول بدل 1.8. قِيس: شمعةٌ واحدة بحجمٍ صفر من
  260 — فلا يمرّ ‎5د‎ بـ`tradingOnly` (تعريضٌ بلا مقابل، كالساعة واليومي).

### الوضع المُصغَّر يعمل لأن كل رمزٍ له ملفّ
**افتراضٌ خاطئ كلّف تشخيصاً**: ظننتُ أن رموز الطبقة الواسعة بلا ملفات
فمرّرتُ لها سجلاً فارغاً، فسقطت **العشرة** وظهر الرمز بلا تحليل. والحق
أن `data/sym/` فيه **510 ملفاً** كلٌّ بـ260 شمعة يومية ومؤشّراتها
كاملة — الناقص الفريمات اللحظية وحدها. فبالسقوط إلى اليومي (`iTf15`
و`sqTf`) تعمل خمسٌ: `brk` و`meanRev` و`rsiDiv` و`sqzExp` و`volSpike`.
- **والفريم المستعمل يُعلَن** (`tfUsed`) وتعرضه الواجهة: شرطٌ يقيس ١٥
  دقيقة على رمزٍ ويوماً على آخر يصف حالتين، وعدمُ قوله يجعل رقمين غير
  متقارنين يبدوان متقارنين.
- و«لا تباعد الآن» ليست «لا يمكن قياس التباعد»: الأولى حالةُ سوقٍ
  تُقال «لم يتفعّل»، والثانية نقصُ بياناتٍ يُقال `off`. خلطُهما يجعل
  الرمز السليم يبدو ناقصاً.

### الإجماع — ثلاث قواعد
١. **التعارض يُفحص قبل الاتجاه**: كتلةُ أقلّيةٍ فوق ‎35%‎ ⇒ `mixed`
   ولا خطة. «‎4‎ شراء و‎3‎ بيع» ليست شراءً ضعيفاً بل حالةٌ يُعرف فيها
   أنّا لا نعرف.
٢. **الوزن مقيسٌ أو محايد ولا شيء بينهما**: `edge: null` ⇒ وزن ‎1.0‎
   **بوسمٍ ظاهر**. ولا `REGIME_W` مكتوبةٌ بالحدس: وزن الحالة يُشتقّ من
   `byRegime` وهو **فارقُ حافّتها في هذه الحالة عن حافّتها العامة** —
   السؤال «هل قِيس أنها تعمل هنا أفضل من عادتها؟» لا «أيّها يليق
   بالنطاق؟».
٣. **الثقة ليست احتمالاً** ولا تُعرض نسبةَ نجاح.

- **والإشارة المنفردة لا تُسمّى إجماعاً — قِيس فعلاً.** استراتيجيةٌ
  واحدة تتّفق مع نفسها تعطي «اتفاق ‎100%‎»، فكان `KO` بإشارةٍ منفردة
  يتصدّر القائمة فوق رمزٍ اتّفقت عليه ثلاثٌ من عائلتين. فالعدد وتنوّع
  العائلات شرطان في الثقة، و**مفتاح الترتيب `mass`** (الكتلة المرجَّحة)
  لا `sc` — والمتوسّط لا ينمو بالعدد فيضع واحدةً بـ‎95‎ فوق ثلاثٍ بـ‎85‎.
- **والتوافق الاستثنائي يشترط عائلتين متمايزتين**: ثلاث نكهاتٍ من فكرةٍ
  واحدة تتّفق دائماً، فاتّفاقُها تكرارٌ لا تأكيد. ومعها طزاجةٌ وجودة
  دخول — توافقٌ تامّ على حركةٍ وقعت قبل ساعات معلومةٌ تاريخية لا فرصة.

### لحظةُ البدء من الخادم والنتيجةُ من المتصفح
المتصفح يملك الشمعات فيحسب كل شيء بالسعر الحيّ، لكنه **لا يملك
الماضي** — لا يعرف متى انقلبت الجهة لأنه لم يكن يعمل حينها. فـ`at`
و`px0` من `strategies.json` ولا يُخترعان، وغيابُهما يُقال «منذ هذه
الدورة» بدل تأريخٍ ملفّق.
- `at` تُعاد **عند تبدّل الجهة وحدها**: النتيجة تتحرّك كل دورة، وإعادةُ
  التأريخ عند كل حركة تجعل كل إشارةٍ «جديدة» أبداً — عكس السؤال تماماً.
- و`px0` لا يُلمس: `entryQuality` تقيس كم تحرّك السعر **منذ الإشارة**،
  فتحديثُه يجعل الجواب صفراً دائماً.

### سكّانان منفصلان في ملفّين — والدوالّ مشتركة
إشارات الماسح في `strat-signals.json`/`strat-history.json` لا في
`signals.json`: خلطُهما يجعل «نسبة النجاح» تصف سكّانَين ويُبطل مقارنة
الأرشيف. و`updateOutcome` و`guardTotal` **تُستوردان** من
`track-signals.mjs` — ولذلك شكلُ `snap` هنا هو شكلُه هناك بالحرف.

### الاستيراد كان يشغّل الملفّ — حارس `IS_MAIN`
`track-signals.mjs` و`backtest.mjs` كانا ينفّذان `main()` بمجرّد
استيرادهما: استيرادٌ بـ`--check` في `argv` يُجري فحصَ الملفّ المستورَد
ثم `process.exit` **قبل أن يبدأ المستورِد**، وبدونه يجلب الكون. عولجت
عند جذرها بـ`IS_MAIN` فصارا قابلين للاستيراد بأمان — بدل قاعدة «لا
تستوردهما» الموثّقة سابقاً.

### الأرشيف اللحظي — ونافذته المعلنة
`backtest-strategies.mjs`: ستون يوم تداول لـ‎5د/15د‎ وسنتان للساعة،
‎~4‎ طلبات لكل رمز. و**ستون يوماً حدُّ المصدر لا اختيارُنا** (أقصى ما
يعطيه ياهو لفريمٍ لحظي)، وتغطّي حالةَ سوقٍ واحدة غالباً — يُكتب بجانب
كل رقم، **ولا يُقارن برقم الأرشيف اليومي** (عشر سنوات) كأنهما سواء.
- `simulatePlan` و`summarizePlans` و`COSTS` **مستوردة** لا معادة
  الكتابة: قواعد المحاكاة الستّ نسخةٌ واحدة، وإلا قِيس الأرشيفان
  بمسطرتين.
- **الكلفة كانت تربيعية ووقعتُ فيها رغم أنها موثّقة**: نداءُ `analyze`
  على شريحةٍ متنامية عند كل شمعة جعل **رمزاً واحداً يتجاوز دقيقتين**
  (تسعون رمزاً ≈ ثلاث ساعات). الحلّ `seriesOf` مرّةً لكل فريم ثم `anAt`
  بالفهرس، ومؤشّرٌ متقدّم لكل فريم بدل بحثٍ خطّي. وشرطُ القبول مقارنةٌ
  ذهبية: `anAt` يطابق `analyze` حتى ‎1e-9‎ عند شمعاتٍ مختارة.
- والتباعد وحده يُحسب على نافذةٍ محدودة عند الشمعة المرشَّحة: مسحُ قممٍ
  لا سلسلةٌ متدحرجة، ونافذتُه ‎60‎ شمعة أصلاً.
- **`tfAlign` لا يمكن قياسه لحظياً**: يشترط الفريمات الخمسة، و‎5د‎ لا
  يغطّي إلا ستين يوماً من تاريخٍ يوميٍّ طوله خمس سنوات — فيخرج بصفر
  إشارة. ونظيرُه الرباعي مقيسٌ في الأرشيف اليومي (‎−0.28‎/‎−0.42‎)
  ويُذكر في شرحه نصّاً بلا ادّعاء أنه رقمُه هو.

### عائلتا الاستمرار تُعرضان بحافّتهما السالبة
الأرشيف على 482 ألف شمعة: هذا الكون **يدفع على الارتداد ويعاقب على
الاستمرار**. فالمتوقَّع أن تخرج حافّة `momo` و`tfAlign` و`brk` سالبة —
وتبقى معروضةً بأرقامها. حذفُها لأن قياسها سيّئ إخفاءٌ للقياس، وعرضُها
بلا رقمها كذبٌ بالصمت.
- و`sqzExp` **بناءٌ مختلف** عن `squeeze` الساقط (‎−0.48‎): ذاك يأخذ
  جهته من الميل فيرث سلبيّته (أي أن رقمه يقيس الميل لا الانضغاط)، وهذا
  يأخذها من **الكسر نفسه**. فرضيةٌ أخرى تُقاس من جديد.

### VWAP بلا حجمٍ رقمٌ ملفّق
`sessionVwap` تعيد `null` حين يكون مجموع الحجم صفراً — Twelve Data
وStooq لا يعطيان حجماً و`Math.round(x.v || 0)` تصفّره، ومتوسّطٌ حسابيّ
يحمل اسم VWAP رقمٌ يبدو صحيحاً. نفس قاعدة إسقاط OBV وMFI.
- و**الانحراف مرجَّحٌ بالحجم** لا حسابياً: الشريطان يصفان أين تُدووِل.
- و`openingRange` تعلن `complete: false` ما دام النطاق يتكوّن —
  ونطاقٌ قيد التكوّن لا يُكسَر. و`complete` تُحكم **بوجود شمعةٍ بعد
  النافذة** لا بمرور الوقت: السلسلة قد تتأخّر دورةً كاملة.
- و`orb`/`vwapRec` تنسحبان خارج `REGULAR` وحين يصف `period` يوماً
  سابقاً — المصيدة الموثّقة «الفترات المحفوظة تصف يوم جلبها».

### حجم الشمعة الجارية جزئيّ
بوابات الحجم تقرأ الشمعة **المكتملة** الأخيرة (`k[n-2]`): الجارية
تتراكم، فمقارنةُ حجمٍ نصفِ مكتمل بوسيط شمعاتٍ كاملة تقول «الحجم ضعيف»
أول الشمعة و«قوي» آخرها — تذبذبٌ مصدرُه القياس لا السوق.

### فريم الاكتشاف غير فريم الصفقة
نطاق الافتتاح يُكتشف على ‎5د‎ (ATR أبل فيها ‎0.44‎ دولار) والصفقة
تُحمل ساعاتٍ إلى يومين — ووقفٌ على بُعد ‎0.13%‎ يُضرب بالتذبذب الطبيعي
قبل أن يصحّ التحليل أو يخطئ. فـ`atrTf` في `levels()` أبطأ من فريم
المشغِّل، و**هو نفسه يُمرَّر إلى `etaFor`** (مصيدة خلط الوحدتين).

### مستويات الاستراتيجية تُحقَن في المجمّع ولا تحلّ محلّه
البديهيّ أن تحسب كل استراتيجية دخولَها ووقفَها بنفسها، وهو خطأ مزدوج:
يخترع مستوياتٍ لا وجود لها، ويجعل عشر نسخٍ من رياضيات الخطة تتباعد
بأول تعديل. فحافةُ النطاق وVWAP تُحقَن في مجمّع `levelsFrom` ثم تتولّى
`planFrom` كل ما بعدها — نفس درس `planPair` (سطر الدخول وحده).

### `check-ui` كان أعمى تجاه تعارض الملفّات المشتركة فيما بينها
كان يقارن الصفحة بكل ملفّ **ولا يقارن الملفّات ببعضها**، فمرّ `volX`
معرَّفاً في `scans.js` و`strategies.js` معاً. وهنا لم يمرّ بصمت بل رمى
`Identifier 'volX' has already been declared` فأسقط **كل** الشيفرة —
صفحةٌ ساكنة بلا رسالة. ولو كان أحدهما `var` لمرّ بلا خطأ وبقيت الدالّة
الخاطئة تعمل، وهي مصيدة `pctRank` بالحرف. صار الفحص يقابل كل ملفّين.

### شمعة 4h كانت تُبنى بفهرس المصفوفة فتزحف شبكتُها ساعةً كل يوم

بلّغ مستخدم أن سهماً يُعطي «اتجاه صاعد ‎96‎» ثم «‎67‎» بعد ثلاث دقائق
وفريماتُه تنقلب — وهو أخطر ما ظهر بعد علّة الاتجاه، لأن **لا شيء يبدو
معطلاً**: السعر حيّ والشارت يتحرّك والأرقام في مداها.

`aggregate` كانت تقسّم بالفهرس (`i += factor` من الصفر)، وهو يفترض أن
الصفر ثابت. والافتراض يسقط **دائماً**: سلسلة الساعة تُطلب بمدى سنتين
فتتدحرج نافذتُها (تسقط أقدمُ شمعة وتُضاف أحدثُ)، وفيها فجواتُ الليل
وعطلةُ الأسبوع ونصفُ الجلسات. فكلّ جلبٍ يزيح حدودَ المجموعات، وتقع
**نفس ساعات السوق في مجموعاتٍ مختلفة** بين تشغيلٍ وآخر.

- فتتغيّر شمعة 4h فتحاً وأعلى وأدنى وإغلاقاً، وتتغيّر معها EMA وMACD
  وRSI على الفريم، فتنقلب بوابةٌ وزنُها ‎1.5‎ فتتحرّك النتيجة الكلية
  ‎~11‎ نقطة — **بلا أن يتحرّك السعر أصلاً**.
- المقيس: `CRM` تتذبذب ‎72.94 ↔ 61.96 ↔ 72.94‎ بين ‎257.46‎ و‎257.61‎،
  و`AAPL` تتغيّر نتيجتُها ‎88.24 ↔ 94.12‎ والسعر **مطابقٌ بالبايت**
  (‎326.57‎ في أربع لقطات متتالية). والتذبذب **دوريّ بمقياس ‎4‎** لأن
  الإزاحة `الطول % 4` — فيتنقّل بين حالاتٍ معدودة لا عشوائية.
- ودليلُ الزحف مباشر ولا يحتاج تفسيراً: `t % 14400` لشمعات 4h كان
  ‎5400‎ يوماً و‎9000‎ يوماً تالياً و‎12600‎ بعده.

**المفتاح الآن `floor(t / bucket)`** — نفس اللحظة تقع في نفس المجموعة
مهما تغيّر طولُ المصفوفة أو بدايتها. وطولُ شمعة المصدر يُشتقّ
**بالوسيط** لا بالفرق الأول: الفجوات تجعل بعض الفروق ستّ عشرة ساعة،
والوسيط لا يتحرّك بها.

- والقاعدة أعمّ من 4h: **أيُّ تجميعٍ على سلسلةٍ متدحرجة يجب أن يكون
  مفتاحُه الزمنَ لا الموضع.** الموضع يصف مكانَ الشمعة في مصفوفةٍ عابرة،
  والزمن يصف اللحظة التي وقعت فيها.
- وفحصٌ يُثبت الثبات: نفس السلسلة بأربع بداياتٍ مختلفة يجب أن تعطي نفس
  الشمعات في الذيل، وإضافةُ شمعةٍ جديدة لا تعيد تشكيل ما قبلها. أُثبت
  أنه يُسقط نفسه بإعادة التقسيم بالفهرس. وقِيس بعد الإصلاح: **صفر
  تأرجح على 40 رمزاً** مقابل ‎33‎ نقطة قبله.
- **والبيانات المخزَّنة لا تُصلَح بنفسها**: 4h يُعاد اشتقاقها حين
  تُجدَّد الساعة وحدها (صلاحيتُها 55 دقيقة)، وما عداها تُنقل من `prev`
  كما هي. فبعد أي تعديلٍ في `aggregate` **تُقدَّم ساعةُ الفريم الساعي
  إلى الوراء** في ملفات الرموز كي يُعاد الجلب والاشتقاق فوراً، وإلا
  بقيت الشبكة القديمة ساعةً كاملة.

### ترتيب الكون — الكتلة لا المتوسّط
قائمةٌ لكل رمزٍ في الكون مرتَّبةً بتوافق الاستراتيجيات، **بثلاث طبقات**:
الموجَّهة بكتلتها، ثم المتعارضة، ثم الخالية من أي إشارة.
- المفتاح `mass` (الكتلة المرجَّحة) لا `sc`: المتوسّط لا ينمو بالعدد
  فيضع إشارةً منفردة بـ‎95‎ فوق أربعٍ بـ‎85‎ — وهو عكس سؤال القائمة.
- **والمتعارضة قبل الخالية**: تعارضُ إشارتين معلومةٌ تُقرأ (السهم
  يتحرّك وقراءاتُه تختلف)، وخلوُّ الرمز ليس معلومةً بل غيابُها. ودفنُ
  المتعارضة تحت ثلاثمئة صفٍّ فارغ يُخفي أنفعَ ما في القائمة بعد قمّتها.
- ويُبنى من `strategies.json` وحده بلا جلب 510 ملفّ رمز — وهو **لقطةُ
  الخادم** لا حسابٌ لحظي، ويُقال ذلك في سطر المصدر بدل الإيهام.
- و`.btn` الأساسية `width:100%`: وضعُها في `flex` مع `flex:none` يُبقيها
  على عرض الحاوية كاملاً فتفيض ‎13‎ بكسل على الهاتف. `width:auto` صراحةً.

### الجديد في الملفات
```
stocks/strategies.js       العشر + evalGates + buildCtx + planFor (مشترك)
stocks/consensus.js        الإجماع والحالة والترجيح والتعارض (مشترك)
scripts/track-strategies.mjs   الحالة والتسلسل والسجلّ (بلا شبكة)
scripts/backtest-strategies.mjs الأرشيف اللحظي 60 يوماً (~360 طلباً)
scripts/check-strategies.mjs    الفحص الذاتي — المقارنة الذهبية وغيرها
data/strategies.json       الحالة الآنية + at/px0 (يُقرأ عند فتح التبويب)
data/strat/{SYM}.json      التسلسل الزمني — كسول، للرمز المبحوث وحده
data/strategy-edge.json    الحوافّ المقيسة أو `null`
data/strat-signals.json · data/strat-history.json   السجلّ الحيّ
```

```bash
node scripts/check-strategies.mjs        # المقارنة الذهبية للإيقاعين
node scripts/check-opportunity-list.mjs      # قائمة الفرص: لا تتبدّل داخل الشمعة
node scripts/check-opportunity-stability.mjs  # CONFIRMED مقابل LIVE + إثبات صفر فريمات ممنوعة
node scripts/track-strategies.mjs --check
node scripts/backtest-strategies.mjs --check
node scripts/market-direction.mjs --check # توجّه السوق: التثبيت والأوزان
node local/run.mjs strategies            # الحالة والتسلسل + توجّه السوق (بلا شبكة)
node local/run.mjs mdir                  # توجّه السوق وحده (بلا شبكة)
node local/run.mjs stratbt               # الأرشيف اللحظي (~450 طلباً على 15د)
```

---

## الجلسة الممتدة — ثلاثُ عللٍ جمّدت التطبيق نصفَ اليوم

بلّغ المستخدمون أن الموقع «ينتظر السوق الرسمي فيفوّت فرصاً تبدأ قبله».
والقياس الحيّ (2026-09-15 الساعة ‎07:28 ET‎، أي بعد ثلاث ساعات ونصف من
بدء ما قبل الافتتاح) فصّل السبب إلى **ثلاث عللٍ مستقلّة**، ولا واحدة
منها في الجدولة ولا في الواجهة — المجدول يعمل ‎24/7‎ أصلاً بلا أيّ
شرط ساعة:

| العلّة | القياس |
|---|---|
| **السعر مجمَّد على إغلاق أمس** — `applyQuotes` تقرأ `regularMarketPrice`، وياهو لا يحرّكه قبل ‎09:30‎. وسعرُ ما قبل الافتتاح يُكتب في `ext` **للعرض وحده** | `MSFT.p = 505.41` (إغلاق أمس) و`ext.PRE = 500.11` — فارقُ ‎1.05%‎ لا يراه أيُّ ماسحٍ أو خطة |
| **كلُّ شمعات الجلسة الممتدة تُحذف قبل أن تُحفظ** — `tradingOnly` تُسقط ما حجمه صفر، وياهو يعطي حجم الجلسة الممتدة **صفراً حرفياً** | ملفّ MSFT: **صفر شمعة** منذ ‎04:00 ET‎ رغم تحديثه قبل ‎7‎ دقائق. وعلى MSFT/TSLA/AAPL/SPY عبر خمسة أيام: ‎0‎ في كل شمعةٍ ممتدة |
| **فريم ‎5د‎ — أسرع فريم في الماسح — يُطلب بلا `prePost`** | آخر شمعة ‎5د‎ كانت إغلاق أمس ‎16:00‎ |

ومترتّبٌ عليها أن `orb` و`vwapRec` كانتا تشترطان `sess === "REGULAR"` —
وهو شرطٌ **كان صحيحاً بالبيانات القديمة**: السلسلة الرسمية لا تحوي
شمعةً واحدة قبل الافتتاح، فنطاقُ الافتتاح عليها فارغٌ حتماً.

> **وأخطر ما في الثلاث أنها لا تبدو عطلاً**: الموقع يعرض سعراً وشارتاً
> ونتيجةً وخطةً — كلُّها متّسقة وكلُّها تصف أمس. نفس مصيدة «الأرقام
> تبدو صحيحة وتخصّ سهماً آخر» مطبَّقةً على **الزمن**.

### المرساة حدثٌ لا رقمُ ساعة
الطلب كان «يبدأ ‎11:00‎ صباحاً بتوقيت السعودية»، وهو يساوي ‎04:00‎
نيويورك **في التوقيت الصيفي وحده**. شتاءً يصير ‎11:00‎ الرياض =
‎03:00‎ نيويورك: ساعةٌ كاملة قبل وجود شمعةٍ واحدة. فالمرساة
`SCAN_START_ET = 04:00` تُحسب بـ`Intl` وتُعرض ‎11:00‎ صيفاً و‎12:00‎
شتاءً — نفس اللحظة بوجهين. والسعودية لا تطبّق التوقيت الصيفي، فالفارق
يتحرّك من جهةٍ واحدة.

### `stocks/session.js` — نسخةٌ واحدة لثلاث نسخ
كان منطق الجلسة في ثلاثة مواضع: `lib/session.mjs`، ودالّةٌ مكرّرة في
`index.html`، وشرطٌ داخل `strategies.js`. صار ملفّاً مشتركاً واحداً
(بعطلات NYSE وأنصاف الأيام)، و`lib/session.mjs` غلافٌ فوقه يترجم
`AFTER` ⇄ `POST` **في موضعٍ واحد** فلا ينكسر مستدعٍ قديم.
- والنوافذ تُحسب من **تقويم نيويورك** لا من `rec.period` المحفوظ —
  فتسقط مصيدة «الفترات المحفوظة تصف يوم جلبها» من جذرها، وتصحّ
  النوافذ لأيّ يومٍ في الماضي (وهو ما يحتاجه Replay بالضبط).

### `tfx` — سلسلةٌ موازية لا سلسلةٌ مدموجة
دمجُ شمعات ما قبل الافتتاح في `tf["15m"]` يغيّر EMA وRSI وATR وبولنجر
لكل رمزٍ في الكون، فيُبطل الأرشيف (‎1,084,574‎ شمعة) **بصمت**. فـ`tf`
تبقى الجلسة الرسمية بايتاً ببايت، و`tfx` تحمل الممتدة وتقرؤها
استراتيجياتُ الجلسة الممتدة وحدها — نفس منطق `AN_TFS` مع فريم ‎5د‎.
- و`tradingOnly` صارت تُرشّح **بالجلسة ثم بالحجم**. الترتيب ليس
  تجميلاً: اليوم يُسقط شرطُ الحجم الشمعاتِ الممتدة تلقائياً لأن حجمها
  صفر، فحين يصل مزوّدٌ يعطي حجماً ممتداً حقيقياً كانت ستتسرّب إلى
  السلسلة الرسمية وتغيّر كل مؤشّرٍ في الكون بلا رسالة.
- و**المقارنة الذهبية شرطُ قبول**: بعد التغيير طابقت ‎1770‎ سلسلة
  رسمية، واختلفت ‎69‎ على ‎15د‎ وحدها — وكشف الفحصُ **خللاً كامناً**:
  ياهو يُرفق مطبعةَ الإغلاق بختم ‎16:00‎ بالضبط (ABBV: ‎18‎ يوماً من
  ‎60‎ بحجم ‎1.45‎ مليون، وMSFT صفر). وهي بتعريف الجلسة «ما بعد
  الإغلاق»، فكانت السلسلة الرسمية تختلف بين رمزٍ وآخر وبين يومٍ وآخر.
  الأثر بعد التوحيد: ‎13‎ رمزاً تغيّرت نتيجته (وسيط ‎1.77‎ نقطة) و
  **صفر تغيّر في الأوسمة**. وطُبّق نفس الترشيح على `backtest-strategies`
  كي لا يقيس الأرشيف سلسلةً غير المعروضة.

### طبقة المزوّد — الاختيار بالقدرة لا بالاسم
`scripts/providers/` بواجهةٍ واحدة (`getQuotes`/`getCandles`/
`getCandlesBatch`/`listAssets`/`subscribe`) و`caps` تُستعلَم:
`extendedVolume`، `extendedVolumeDelayMs`، `batchSize`، `websocket`.
فالبوابة التي تحتاج حجماً تسأل القدرة لا `src === "alpaca"`.
- **ولا يستورد أيُّ محوّلٍ شيئاً من `strategies.js` أو `scans.js`** —
  الاتجاه واحد: المنطق يقرأ البيانات، والبيانات لا تعرف المنطق.
- و`lib/env.mjs` يُستورَد من `providers/index.mjs` نفسه: تحميل `.env`
  كان في `local/run.mjs` وحده، فالسكربت المُشغَّل مباشرةً يعمل بلا
  مفاتيح ويطبع «لا حجم للجلسة الممتدة» — سطرٌ صحيح لسببٍ خاطئ.

### Alpaca: تغذيتان لا واحدة — قياسٌ غيّر التصميم
| المقياس | `iex` (لحظية) | `sip` (مجمَّعة) |
|---|---|---|
| التأخير | صفر | **‎15‎ دقيقة بالضبط** (مقيس) |
| تغطية ما قبل الافتتاح | **رمزٌ واحد من ‎149‎** في أربع ساعات | ‎139‎ من ‎149‎ (‎93%‎) |
| حجم MSFT قبل الافتتاح ‎09/14‎ | ‎54,841‎ | ‎1,696,732‎ (‎3.2%‎ منه) |

فالفرض الأول («Alpaca تعطي حجم الجلسة الممتدة») صحيحٌ ناقص: اللحظيّة
تغطيتُها لا تكفي، والمجمَّعة متأخّرة ربعَ ساعة. والتقسيم يتبع السؤال:
**السعرُ اللحظي من ياهو** (مجمَّع، بلا تأخير، كل الرموز)، و**الحجمُ
الممتد من Alpaca SIP**، و**تاريخُ الـReplay من SIP** (لا تأخير على
الماضي). والتأخير يُعلَن في `caps` ولا يُخفى.
- و**الكون كلُّه في طلبٍ واحد**: ‎149‎ رمزاً في ‎1.7‎ ثانية. فالتوسّع
  إلى آلاف الرموز مسألةُ ستّة طلبات لا مئات.
- ومصائد قِيست: `sip` ترفض أيّ `end` داخل نافذة التأخير بـ‎403‎ **ولو
  كان على الحدّ بثانية** — وحذفُها لا يُرفض (الخادم يقتطع بنفسه).
  ورمزٌ واحد بصيغةٍ خاطئة (`BRK-B` عندنا و`BRK.B` عندهم) يُسقط الدفعة
  كلَّها بـ‎400‎ — فالترجمة في المحوّل، والرمز المرفوض يُستبعد وتُعاد
  المحاولة بدل فقدان خمسين رمزاً سليماً معه.

### حجمُ ما قبل الافتتاح يختلف ثلاثة أضعاف بين مصدرين محترفين

قياسٌ مباشر على `MSFT` يوم ‎2026-09-14‎ (الجلسة الممتدة ‎04:00–09:30 ET‎):

| المصدر | الحجم | السعر |
|---|---|---|
| Alpaca SIP | ‎1,696,732‎ | مطابق |
| IBKR (عبر موصّل الحساب) | ‎561,033‎ ‎(−67%)‎ | مطابق |
| ياهو | ‎0‎ | مطابق |

**السعر يتطابق إلى القرش عند الثلاثة** — أوّل شمعةٍ تغلق ‎492.65‎ عندها جميعاً.
والحجمُ وحده يتباعد، لأن كلَّ مصدرٍ يرى مجموعةً مختلفة من منصّات التداول قبل
الافتتاح (وحصّةُ كلٍّ تتغيّر بالاشتراك وبالمنصّة).

**والنتيجة قاعدةٌ ملزمة**: أيُّ عتبةٍ **مطلقة** للحجم قبل الافتتاح («أكثر من
نصف مليون سهم») بلا معنى — تتغيّر ثلاثة أضعاف بتغيير المزوّد وحده. والمقارنة
الصالحة نسبةٌ **بسطها ومقامها من نفس التغذية**، وهي ما تفعله `volRatio`: حجمُ
الشمعة مقسوماً على وسيط شمعاتٍ من نفس نوع الجلسة ونفس المصدر — فحصّةُ المنصّة
تُختصر. ولهذا يوجد `caps.volumeScope`: الرقم يُعرض بمصدره مكتوباً بجانبه، لا
مجرَّداً كأنه حجم السوق.

> وموصّل IBKR **لا يصلح مصدراً للمنتَج**: هو أداةٌ داخل جلسة المحادثة، ومهامُّ
> الجلب مجدولةٌ تعمل بلا حضور. يصلح للتحقّق المستقلّ — وهو ما استُعمل فيه هنا.

### `replay.mjs` — منعُ النظر إلى المستقبل بنيويّ
ساعةٌ افتراضية تخطو ‎5د‎ من ‎04:00 ET‎، وكلُّ قراءةٍ للسلاسل تمرّ
بـ`lastIdx` التي تشترط أن تكون الشمعة قد **اكتملت** (`t + barMs <=
vnow`) — فشمعةُ ‎09:30‎ لا تُقرأ قبل ‎09:35‎. والفحص يحقن شمعةً
مستقبلية ويشترط ألّا يتغيّر الناتج بحرف.
- ومحرّكان من **نفس البيانات ونفس المحاكاة**: `--engine=old` يحاكي
  قيود ما قبل الإصلاح، فالمقارنة تقيس التغيير لا اختلاف الأدوات.
- وتسخينان لا واحد: ‎5د‎ يحتاج ‎200‎ شمعة (‎12‎ يوماً)، واليوميّ يحتاج
  ‎200‎ **يوم تداول** (‎400‎ يوماً تقويمياً). وخلطُهما أعطى ثماني شمعات
  يومية فسقط الكونُ كلُّه بـ«بلا بيانات كافية».
- وتهدئةٌ ثماني شمعات: بدونها تُعدّ الإشارة الواحدة عشراتِ المرّات
  (‎1262‎ إشارة على ‎40‎ رمزاً في يوم — رقمٌ يصف اهتزاز العتبة لا السوق).
- و**القفل لا يشمل `replay` و`audit`**: يقرآن ويكتبان في مجلّديهما
  وحدهما، فإخضاعهما له يجعل تحليلاً يدويّاً ينتظر دورةَ جلبٍ ثم ينسحب
  — فيُقرأ فشلاً في التحليل وهو ازدحامٌ على قفل.

### ما قِيس فعلاً — خمسة أيام، ‎149‎ رمزاً

| اليوم | فرصٌ ممتدة | اكتُشفت قبل الجرس (جديد) | (قديم) | إيجابيات كاذبة |
|---|---|---|---|---|
| 09-08 | 13 | ‎10‎ (‎76.9%‎) | ‎0‎ | ‎92.4%‎ |
| 09-09 | 7 | ‎3‎ (‎42.9%‎) | ‎0‎ | ‎97.6%‎ |
| 09-10 | 7 | ‎3‎ (‎42.9%‎) | ‎0‎ | ‎95.8%‎ |
| 09-11 | 7 | ‎3‎ (‎42.9%‎) | ‎0‎ | ‎95.7%‎ |
| 09-14 | 37 | ‎33‎ (‎89.2%‎) | ‎0‎ | ‎83.1%‎ |

- **القديم صفرٌ بنيويّ لا بالصدفة**: لا يملك إشارةً واحدة قبل الجرس
  لأن سعره إغلاقُ أمس وشمعاتُه الممتدة محذوفة.
- **وMSFT — الحالة التي بلّغ عنها المستخدم**: الجديد يكتشف ‎11:05‎
  بتوقيت الرياض (`meanRev ▲` عند ‎492.65‎، بلغت ‎+3.51%‎)، والقديم
  ‎16:45‎ — فارقُ **خمس ساعاتٍ وأربعين دقيقة**. ومع ذلك أطلق الجديد
  ثلاث إشارات ‎▼‎ خاطئة في نفس الساعة، وهذا يُقال ولا يُخفى.

### الإيجابيات الكاذبة هي المشكلة الباقية — ورقمان يوجّهان الحلّ
على ‎2,948‎ إجماعاً قبل الافتتاح في أربعة أيام، أصاب ‎8.1%‎ منها حركةً
بحجم ‎1.5×ATR‎. والترشيح:

| المرشّح | العدد | أصابت حركة |
|---|---|---|
| الكل | 2,948 | ‎8.1%‎ |
| عائلات ‎≥2‎ | 1,092 | ‎14.5%‎ |
| **عائلات ‎≥3‎** | 220 | **‎19.5%‎** |
| النتيجة ‎≥80‎ | 726 | ‎6.2%‎ |
| **النتيجة ‎≥90‎** | 177 | **‎1.1%‎** |

- **تنوّعُ العائلات هو المرشّح الوحيد الذي يعمل** (‎2.4×‎ خط الأساس).
- **والنتيجة المعروضة كـ«ثقة» معكوسةُ الدلالة قبل الافتتاح**: ‎≥90‎
  تعطي ‎1.1%‎ — أسوأ من الإطلاق العشوائي. متّسقٌ مع الدرس الموثّق أن
  هذا الكون يدفع على الارتداد ويعاقب على الاستمرار، ويجب أن يُعرض لا
  أن يُدفن.
- والحدّ المعلن: أربعة أيام وحالةُ سوقٍ واحدة، و‎220‎ حالة تبلغ حدّ
  العيّنة عدداً **ولا تُقسَم خارج العيّنة**. دليلٌ موجِّه لا حافّة
  مثبَتة.

### ما لم يُبنَ بعد — والسبب
طُلب «بثّ + قاعدة بيانات + كلّ السوق»، وبُني منه ما يخدم الاكتشاف
المبكّر فعلاً. والباقي مؤجَّلٌ بقرار:
- **البثّ اللحظي**: `subscribe` جاهزةٌ في محوّل Alpaca ولم تُشغَّل.
  والسبب مقيس: تغطية IEX قبل الافتتاح **رمزٌ واحد من ‎149‎** — فالبثّ
  اللحظيّ المتاح مجاناً لا يحمل ما نحتاجه، ودفعةُ REST تعطي الكون في
  ‎1.7‎ ثانية. البثّ يفيد داخل الجلسة الرسمية، وهي ليست المشكلة.
- **قاعدة البيانات**: `node:sqlite` مدمجٌ وجُرّب (يعمل بلا راية ولا
  اعتمادية). وقيمتُها الحقيقية خطُّ أساسٍ للحجم بـ«نفس دقيقة الجلسة»
  عبر أيام — وهو ما يحتاجه `pmRVol`. تُبنى مع الاستراتيجية التي
  تستهلكها لا قبلها.
- **توسيع الكون إلى آلاف الرموز**: `listAssets` جاهزة، والحدّ ليس
  الجلب (ستّة طلبات) بل **النشر**: أربعة آلاف ملفّ رمز على GitHub
  Pages. يحتاج فصل مخزن الخادم عن عقد المتصفّح أولاً.

---

## قواعد العمل

- **لا يُعلَن إصلاحٌ إلا بعد دفعه والتحقّق منه على الرابط المنشور.** هذا
  شرطٌ ملزم لا عادةٌ حسنة: ثلاث عللٍ متتالية أُعلن إصلاحُها بناءً على
  فحصٍ محلّي ناجح، والمستخدم يفتح الموقع فيراها قائمة — لأنها كانت
  تعيش في الفجوة بين القرص والصفحة المنشورة. فالتسلسل الملزم:
  `فحوص محلّية` ← `git push` ← **انتظار وصول النشر** ← `node
  scripts/check-live.mjs` ← ثمّ الإعلان. وفشلُ الأخيرة يعني أن العمل لم
  يتمّ مهما نجح ما قبلها.
  - والرابط: <https://alkongrs2014.github.io/Alkongrs2014/stocks/>
  - و«الشيفرة على الخادم صحيحة» ليست تحقّقاً: عاملُ الخدمة يعترض قبل
    ترويسات HTTP، فقد يقدّم للمستخدم خليطاً من نسختين. `check-live`
    يلغيه ويمسح ذاكراته قبل أن يقيس.

- **الفحص الذاتي أولاً:** `--check` يمرّ بلا شبكة في ثانية. شغّله قبل أي دفع.
- **تحقّق من البيانات لا من «نجح التشغيل»:** ثلاثة أخطاء حقيقية اختبأت
  خلف تشغيلات ناجحة. افحص `data/summary.json` و`data/meta.json` فعلياً.
- **بوابة السلامة مقدّسة:** لا تُكتب بيانات فوق بيانات سليمة عند الفشل
  التام (`if (!rows.length) throw`). لا تُضعفها لتمرير تشغيل.
- **`indicators.mjs` مشترك** بين الخادم والمتصفح — لا تكرّر منطقه بنسختين
  وإلا اختلفت الأرقام بين القائمة والشارت.
- **التعليقات تشرح السبب لا الفعل** — الشيفرة تقول ماذا، والتعليق لماذا.
  الملفات كلها بالعربية؛ حافظ على ذلك.

---

## المفاتيح

محلياً في `.env` (انظر `.env.example`)، وعلى GitHub في
`Settings → Secrets and variables → Actions`:

| المفتاح | الأثر عند غيابه |
|---|---|
| `FINNHUB_API_KEY` | لا أسعار لحظية (تُشتق من إغلاق آخر شمعة) |
| `TWELVEDATA_API_KEY` | لا شموع سحابياً · **غير مطلوب محلياً** |

**لا ترفع `.env` إطلاقاً.** ولا تضع أي مفتاح في ملف يصل المتصفح.

---

## الانسحاب صار انتظاراً، والكريبتو صار من Binance

### «مشكلة الثلاث دقائق» كان لها ثلاثة أسباب لا واحد

بلّغ المستخدمون أن الماسح «فيه بيانات متأخّرة أو غير صحيحة» و«مشكلة
بالثلاث دقائق». والقياس (2026-09-15) فرّقها إلى ثلاثة أسباب منفصلة،
وواحدٌ منها فقط كان يستحقّ مالاً:

| السبب | القياس | العلاج |
|---|---|---|
| ‎164‎ من ‎311‎ صفٍّ في الماسح على شمعة يومية عمرها ~‎18‎ ساعة | **كلُّ** الطبقة الواسعة — رموزها لا تأخذ إلا اليوميّ | توسيع المرصودة |
| ‎33%‎ من دورات الأسعار تسقط | ‎37‎ من ‎~113‎ في ‎3.8‎ ساعة | الطابور — **مجاناً** |
| السعر لقطةٌ كل دقيقتين لا بثّ | بالتصميم | خارج النطاق |

- **والخلط بينها هو الخطر**: مزوّدٌ مدفوع بـ‎$199‎/شهر يحلّ الأول
  والثالث ولا يمسّ الثاني — فتُدفَع فلوسٌ على بياناتٍ يرميها باگٌ عندنا.
  **قِس أيَّ مشكلةٍ بالضبط قبل أن تشتري حلاًّ.**

### الانتظار بدل الانسحاب — والانسحاب حالةٌ قصوى

`acquireLock` كانت تنسحب فوراً أمام قفلٍ حيّ وتخرج بصفر. والغرض الأصلي
للقفل — ألّا تعمل مهمّتان معاً — **يتحقّق بالانتظار كما يتحقّق
بالانسحاب**، والفارق أن الانتظار ينفّذ العمل.

- السقف من دورة المهمة نفسها و**أقلّ منها دائماً**: انتظارٌ أطول من
  الدورة يجعل تشغيلين ينتظران نفس القفل فيتراكمان.
- القفل يُؤخذ بـ`fs.openSync(LOCK, "wx")` — **كتابةٌ ذرّية**. فحصٌ ثم
  كتابةٌ يترك فجوةً يأخذه فيها تشغيلان معاً.
- و`alive(pid)` يُفحص في **كل دورة استطلاع** لا مرّةً عند الدخول:
  العملية قد تموت أثناء انتظارنا.
- و`.run.skips.json` صار يحمل `outcome`: `queued` (انتظر ونفّذ) أو
  `gave-up` (استنفد السقف). **بلا هذا التمييز لا يُعرف هل تحسّن شيء.**
- **وانسحابٌ أمام `market` ليس فجوة بيانات**: `fetch-market` يجلب أسعار
  كل الرموز في دورته، فالسعر يتجدّد من المهمة الحاجبة نفسها. الذي
  يستحقّ الانتباه انسحابٌ أمام مهمةٍ **لا تُسعّر** (`filings`، `backtest`).

### Binance مصدر الكريبتو — وأرخص من ياهو بمراتب

| المقياس | ياهو | Binance |
|---|---|---|
| أسعار كل الكون | دفعةٌ لكل 40 رمزاً + crumb + كوكي | **طلبٌ واحد** (‎3702‎ زوج في ‎1.6‎ ثانية) |
| الحصّة | غير معلنة، و429 عند ~1500 طلب | وزنيّة: ‎6000‎/دقيقة وطلب الشمعات ‎2‎ |
| الأزواج | ‎26‎ مكتوبة يدوياً | ‎744‎ زوج USDT |

- **ويزيل مصيدتين موثّقتين من مصدرهما**: `ARB-USD` عند ياهو أصلٌ ميت
  مجمَّد على ‎0.000629‎ بحجم صفر — وعند Binance أربيتروم الحقيقية بسعر
  ‎0.1353‎ وحجم ‎95‎ مليوناً. و«أسعار ما قبل الإدراج» (عائد ‎1,573,986%‎
  في يوم) لا وجود لها في زوج تداولٍ فعليّ.
- **والمستقرّة والرافعة المرمَّزة تُستبعدان**: `USDCUSDT` تصدّرت قائمة
  الحجم وسعرها ثابتٌ عند الواحد فكلُّ مؤشّرٍ عليها ضجيجٌ حول خطٍّ
  مستقيم، و`BTCUP`/`ETHDOWN` مشتقّاتٌ تتآكل يومياً ولا تتبع الأصل.
- **الرموز تبقى بصيغة التطبيق** (`BTC-USD`) لا بصيغة المنصّة
  (`BTCUSDT`): السجلّ والأرشيف والتسلسل كلُّها مفهرسة بها، وتغييرُ
  الصيغة ييتّم تاريخها كلَّه. و`ALIAS` يترجم لواحق ياهو القديمة.

### طبقتان للكريبتو — نفس معمارية الأسهم

‎744‎ زوجاً، و**‎74‎ فقط** تتجاوز خمسة ملايين دولار يومياً. والباقي يُنتج
إشاراتٍ من ضجيج — فمصيدة `ARB-USD` ليست حالةً فردية بل **فئة**.

- المرصودة (‎>$5M‎): الفريمات الأربعة كل عشر دقائق.
- الواسعة: تدخل `cfg.wide` نفسها بشمعةٍ يومية. **طبقةٌ ثالثة تعني مسار
  جلبٍ ثالثاً يتباعد عن الاثنين بأول تعديل.**

### يوم الكريبتو يبدأ ‎00:00 UTC‎ — لا جلسة نيويورك

`orb` و`vwapRec` تشترطان `sess === "REGULAR"`، والحالة تُشتقّ من السوق
الأمريكي — فكانتا **معطَّلتين على الكريبتو دائماً** رغم أنه ‎24/7‎، وهو
بالضبط ما يُراد تشغيله حين يُغلق الأمريكي. `buildCtx` تركّب للكريبتو
فترةً من حدّ يوم UTC، وشمعات Binance اليومية مرتكزة على نفس الحدّ
(`t % 86400 == 0` مقيس) فليس اصطلاحاً مخترَعاً.

- **والفريم اللحظي يُختار بالتغطية لا بالتفضيل**: ‎260‎ شمعة من ‎5د‎
  تساوي ‎21.7‎ ساعة، ويوم الكريبتو ‎24‎ — فمن ‎21:40 UTC‎ فصاعداً تخرج
  شمعاتُ الافتتاح من النافذة ويعود `openingRange` بـ`null` **بلا خطأ**،
  فيسقط `orb` صامتاً في آخر ساعتين من كل يوم. `covers()` تختار أصغرَ
  فريمٍ يصل إلى بداية الجلسة. وجلسةُ نيويورك ‎6.5‎ ساعات فتُغطّى بـ‎5د‎
  دائماً — أي أن سلوك الأسهم لم يتغيّر.

### `top` صار يساوي عدد المرشّحين — فسقطت بوابةُ الترتيب

`fetch-daily` كانت تشترط `ranked.length >= cfg.top` **بالتساوي التامّ**.
وبعد التوسيع صار `cfg.top = 151` و`MMC` وحده لا يعطي بيانات عند ياهو
(موثَّق) — فـ‎149‎ من ‎151‎ أسقطت الترتيب كلَّه إلى ترتيب الأمس (‎71‎
رمزاً). أي أن **توسيع الكون لم يُنفَّذ ولا شيء يقول ذلك**: الملفّ مكتوب،
والتشغيل ناجح، والكون القديم يعمل.

البوابة تحرس من **انهيار الشبكة** لا من رمزٍ بلا قيمة سوقية، فصارت
نسبيّة (‎90%‎). **بوابةٌ بالتساوي التامّ تنكسر عند أول توسّع.**

### ثلاثة بنودٍ طُلبت وكانت مبنيّةً أصلاً

«رزنامة الأرباح» و«لماذا تحرّك؟» و«عرض الحافّة السالبة» كلُّها موجودة
منذ شهور. وكتابةُ `function renderEarnings` ثانية كادت تمرّ: `function`
يرفع تعريفه فالأخير يغلب **بلا خطأ ولا تحذير** — البطاقة الجديدة لا
تُرسم أبداً والقديمة تُنادى من مكانٍ لم يقصدها.

- **`check-ui` كان أعمى تجاه هذا**: يقارن الصفحة بالملفات المشتركة،
  والمشتركة ببعضها، **ولا يقارن الصفحة بنفسها**. أُضيف الفحص وأُثبت أنه
  يُسقط نفسه. نفس عائلة `pctRank` و`volX`، والفارق أن الملفّ واحد.
- **فتّش قبل أن تبني**: `grep` على اسم الميزة أرخص من ميزةٍ مكرّرة لا
  تعمل.

### المنيفست Blob لا يُثبِّت شيئاً

كان يُولَّد بـ`URL.createObjectURL` — يبدو سليماً في أدوات المطوّر ولا
يُثبِّت: كروم يشترط منيفستاً من **نفس الأصل**، و`start_url`/`scope`
النسبيّان يُحلّان مقابل عنوان الـBlob فيخرجان خارج النطاق. صار
`manifest.json` و`icon.svg` ملفّين في `CORE` مع رفع `V`.

- وزرّ التثبيت يُبنى من `beforeinstallprompt` لا العكس: زرٌّ ثابتٌ يكون
  **معطَّلاً في أغلب الحالات** — على iOS الذي لا يدعم الحدث، وداخل
  التطبيق المثبَّت نفسه.

### النصّ الذي يخترع سبباً أسوأ من الصمت

«سقط إلى اليومي — الفريم اللحظي غير متاح» كان ثابتاً كلّما اختلف
`tfUsed` عن `tf`. وهو خطأ من وجهين: الفريم قد ينزل إلى ‎15د‎ لا إلى
اليومي، و**أكثرُ الاختلافات ليست سقوطاً أصلاً** — `iTf15` تُفضّل ‎15د‎
بالتصميم في استراتيجياتٍ فريمُها المعلَن ساعة. فكان يقول «ساعة غير
متاح» ولملفّ الرمز شمعاتُ ساعةٍ كاملة: عذرٌ مخترَع عن قرارٍ مقصود.
الشرط الآن أن يكون الفريم المعلَن **غائباً فعلاً** من `ctx.k`.

### عمر الشمعة يُعرض، ويُحسب من السياق المعروض

الماسح كان يعرض **اسم** الفريم ولا يعرض **عمره**، فصفٌّ محسوبٌ على شمعة
أمس يبدو لحظياً تماماً كجاره. والعمر يُحسب من `ctx` لا من صفّ الخادم:
البطاقة تُعيد التقييم في المتصفّح بالسعر الحيّ، فقراءةُ زمنٍ محفوظ من
دورةٍ سابقة تصف حساباً آخر غير المعروض.

### حارس الطزاجة يصف النظام لا الرمز

`r.stale` تصف رمزاً تخلّف عن إخوته. ولم يكن شيءٌ يقول إن **التطبيق
كلّه** توقّف: حين ينام الجهاز أو ينقطع النت يبقى الموقع يعرض سعراً
ورسماً وخططاً تبدو سليمة وكلُّها من أمس. والعتبة **نسبةٌ من دورة كل
مهمة** لا رقمٌ واحد — تأخّرُ خمس دقائق كارثةٌ في دورة الدقيقتين وطبيعيٌّ
في دورة نصف الساعة. وغيابُ `meta.json` يترك الشريط مخفيّاً: «لا أعرف»
ليست «متوقّف»، وإنذارٌ كاذب يُدرَّب المستخدم على تجاهله.

### الأرقام المكتوبة في نصوص الواجهة تتقادم

«مسح آلي على **السبعين**» و«متوسط نتائج الـ**70** سهماً» و«مرتبة مئوية
بين **السبعين**» — والكون صار ‎1155‎. أربعةُ نصوص كذبت دفعةً واحدة بتغييرٍ
في ملفِّ إعدادات. **العدد يأتي من البيانات أو لا يُذكر.**

---

## أربعة فريمات لا خمسة — و‎5د‎ أُزيل من المشروع كلِّه

كان ‎5د‎ يعيش في طبقةٍ موازية (`AN_TFS` الخامس و`tfx["5m"]`) يقرؤها ماسحُ
الاستراتيجيات وحده، بينما `TFS` الأربعة تحكم النتيجة الفنية. وكلفتُه
طلبٌ ثانٍ لكل رمز وفرعٌ ثانٍ في كل مسار اختيارِ فريم — **بلا أن يظهر
رقمُه في شاشةٍ واحدة**.

- **`TFS` لم تُمسّ**، فالنتيجة الفنية والأرشيف اليومي (‎1,084,574‎ شمعة)
  و`allTF` كما هي بايتاً ببايت. المتغيّر إسقاطُ الطبقة الموازية وحدها.
- **استراتيجيتان فقط تأثّرتا**: `orb` و`vwapRec`. صارتا على ‎15د‎ بنافذة
  نطاقٍ **ثلاثين دقيقة** — وهي ليست اختياراً جديداً بل المسار الذي كان
  يسلكه كلُّ فريمٍ غير ‎5د‎ أصلاً (`iTf === "5m" ? 15 : 30`).
- **و«الستّ الأخيرة» في `vwapRec` تُقاس بالشمعات لا بالدقائق**، فصارت
  ساعةً ونصفاً بدل نصف ساعة. تخفيفٌ مقصود: عدٌّ أصغر (شمعتان) يجعل
  الشرط لا يتحقّق تقريباً.
- **`tfAlign` صار رباعياً** ويبقى منفصلاً عن `allTF`: تلك تقرأ `tfScore`
  المحفوظ بعتبة ‎±15‎، وهذه تقرأ `an` لحظياً. دمجُهما يربط الماسح
  بالأرشيف اليومي فيُبطله أيُّ تعديلٍ في أحدهما.
- **`replay.mjs` صارت خطوتُه ‎15د‎** وتسخينُه ‎30‎ يوماً (‎26‎ شمعة جلسة
  في اليوم × ‎8‎ أيام تداول). ودقّةُ لحظة الاكتشاف تنزل من ‎5‎ دقائق إلى
  ‎15‎ — خطأٌ في اتجاهٍ واحد يُظهر الاكتشاف **أبطأ** لا أسرع.
- **وإعادة بناء `strategy-edge.json` إلزامية**: حافّتا `orb` و`vwapRec`
  كانتا مقيستين على ‎5د‎، و`weightFor` تزن بهما الإجماع. حافّةٌ تخصّ
  فريماً لم يعد يُعرض أسوأ من غياب حافّة.

### حارسان ميتان كشفهما التغيير

١. **`orb` لم يكن يفحص الجلسة أصلاً.** الشرط `c.sess !== "REGULAR"`
   أُزيل عمداً كي تعمل في الجلسة الممتدة، ولم يوضع مكانه شيء — وكان
   الفحص الذاتي يمرّ لأن نطاق ‎5د‎ بنافذة ربع ساعة لم يكن يكتمل في تلك
   اللحظة. وبالنقل إلى ‎15د‎ اكتمل النطاق فظهر أن «كسر نطاق افتتاح»
   يُطلق **والسوق مغلق**. الحارس الآن `c.sess === "CLOSED"` وحدها:
   `PRE` و`AFTER` جلستان لهما افتتاح، و`OPEN24` يومٌ كامل.

٢. **فحصُ المنطقة الميتة كان يخلط صفرين.** كان يرفض أيّ انتقالٍ من ‎0‎
   إلى ‎±1‎ — وصفرُ `cmp(a,b,atr)` «طرفاها متقاربان» غيرُ صفرِ بوابةٍ
   ثلاثية الدرجات بحدودٍ مقصودة (`vwDist`: ‎x≤1‎ قريب · ‎x≤2‎ متوسط).
   ومرّ لأن السعر لم يكن يقع على حدٍّ من النوع الثاني. الثابت الصحيح
   **ألّا تنقلب بوابةٌ من ‎+1‎ إلى ‎−1‎** بإزاحةٍ دون العتبة، ويُفحص
   من الجهتين.

> **القاعدة**: كلُّ حارسٍ يمرّ دون أن تُختبر الحالةُ التي بُني لها هو
> حارسٌ مفترَض لا محروس. وأدقُّ ما يكشفه تغييرٌ في البيانات التي كان
> ينجح عليها بالصدفة.

### فحصٌ يختار عيّنته بالأبجدية ليس فحصاً

أوّل صيغةٍ لحارس تسرّب ‎5د‎ قرأت أربعين ملفاً من `data/sym` ومرّت. وترتيبُ
المجلّد أبجديّ وأوائلُه كلُّها كريبتو (`0G-USD`…) ولم تحمل ‎5د‎ أصلاً،
بينما أربعةُ ملفّاتٍ أبعدَ في الترتيب كانت تحمله. يُفحص **الكون الحيّ
كاملاً** بمسحٍ نصّي قبل التحليل (‎0.6‎ ثانية لـ‎1154‎ ملفاً).

- **والمقام هو الكون الحيّ لا المجلّد**: فيه ملفّاتٌ يتيمة لرموزٍ خرجت
  من `symbols.json` (كريبتو بأسماء ياهو قبل Binance) لا تُعاد كتابتها
  أبداً ولا يقرؤها التطبيق. تُستثنى ويُعلَن عددُها.

### `stocks/confluence.js` — درجة التوافق، مصدرٌ واحد لثلاثة مستهلكين

```js
net   = (up − dn) / (up + dn)            // ∈[−1,1] — التعارض يخفضه
cover = min(1, (up + dn) / SCS_FULL)     // SCS_FULL = 3
str   = |net| × (SCS_FLOOR + (1−SCS_FLOOR) × cover)
scs   = sign(net) × str                  // ∈[−1,+1]
```

- **`SCS_FULL = 3`** ليس ذوقاً: هي عتبة `act.length >= 3` التي تشترطها
  `consensusOf` للثقة العالية.
- **`SCS_FLOOR = 0.35` قيمةٌ صُحّحت بالقياس.** أوّل قيمة (‎0.6‎) أعطت
  إشارةً منفردة ‎0.73‎ فخرجت بـ‎+11‎ نقطة، وخمسُ استراتيجيات بينها خلافٌ
  يسير بـ‎+9‎ — فقرأ الصفّان **مقلوبين** على الشاشة (`SWKS` بواحدة فوق
  `BRK-B` بخمس). الرقم كان صحيحاً حسابياً وكاذباً دلالةً: يقيس **نقاء**
  الاتفاق ولا يقيس **اتّساعه**. وبـ‎0.35‎: منفردةٌ ‎0.57‎ · اثنتان
  ‎0.78‎ · ثلاثٌ ‎1.0‎. وفحصٌ يثبّت الترتيب `منفردة < اثنتان < ثلاث`.
- **ثلاثة أصفار لا تُخلط**: `scs === null` «لا نعرف» · `scs === 0` مع
  `mixed` «قِيس فخرج متعارضاً» · `scs` صغيرٌ «جهةٌ ضعيفة». خلطُها يجعل
  رمزاً بلا بيانات يُقرأ محايداً.
- **و«منفردة» لا «توافق 1»** في العرض: استراتيجيةٌ واحدة تتّفق مع
  نفسها، وتسميةُ ذلك توافقاً هي بعينها ما تمنعه قاعدة «الإشارة المنفردة
  لا تُسمّى إجماعاً».

### أثرُ التوافق على الفرص — رتبةٌ مئوية لا قيمةُ المقياس

`scan.rank` تعيد **وحداتٍ مختلفة**: سعراً في مسحٍ وRSI في آخر ومضاعفَ
حجمٍ في ثالث، وكلَّها تصاعدياً. فضربُها أو جمعُها بمعاملِ توافقٍ خلطُ
وحدات. الرتبة المئوية بلا وحدة وتصلح لكل المسوح بصيغةٍ واحدة:

```js
base = 1 − percentile(scan.rank)          // 0..1 الأفضل 1
oppQ = clamp01(base + 0.15 × scs × sd)    // ±15 نقطة على مئة
```

- **جمعٌ لا ضرب**: الضرب يجعل الأثر يتناسب مع الرتبة فيُحابى صدرُ
  القائمة ويكاد لا يمسّ ذيلها.
- **الأساس يبقى المسح**: الإزاحة ‎±15%‎ من طول القائمة تفرّق بين
  المتقاربين ولا تقلبها.
- **وبلا `strategies.json` لا تعديل** — و«الترتيب بالمسح وحده» يُقال في
  سطر العدد بدل أن يُخفى.

### بطاقة «الاستراتيجيات» — واللقطة تحفظ ما له جهةٌ فقط

`track-strategies` يتخطّى ما لا جهة له (`if (!r.dir) continue`)، فبناءُ
البطاقة من صفوف `strategies.json` أعطى **أربعة صفوف من عشرة** — ويُقرأ
«الستّ الباقية غير موجودة» بدل «لم تُطلق». القائمة تُبنى من `STRATEGIES`
ويُدمج فوقها المحفوظ.

- **والغائب يُسمّى «بلا إشارة» لا «متعذّر»**: اللقطة لا تفرّق بين
  «قُيّم فلم يُطلق» و«تعذّر تقييمه»، والادّعاء بأحدهما كذبٌ في
  الحالتين — ويُقال ذلك في سطر المصدر.
- **`cardStrat` و`stratOut` كانا مأخوذَين** لـ«باني الاستراتيجيات» في
  شاشة العقود. والمعرّف المكرّر يعيد أوّلَ عنصرٍ في المستند لكل
  `$("#…")` فتُرسم بطاقةٌ في شاشةٍ أخرى بلا خطأ. البطاقة الجديدة
  `cardScans`/`scansOut`، و`check-ui` كشف التكرار في أوّل تشغيل.

### `state.fund.rows` — خريطةٌ لا وجود لها

مقارنةُ المحرّكين في بطاقة الماسح كانت تقرأ `state.fund.rows`، و
`fundamentals.json` يحمل **`f`** لا `rows` (ستةَ عشرَ موضعاً في الصفحة
تقرأ `f` وموضعٌ واحد يقرأ `rows`). فالخريطة تخرج فارغةً دائماً ويقرأ
كلُّ شرطٍ أساسيّ `undefined` — لا خطأ ولا استثناء، فقط شروطٌ لا يمكن
أن تظهر في المقارنة أبداً. نفس عائلة `{ secMed: {} }`، وحُلّت بتوحيد
المسار في `oppDirOf` المشتركة.

---

## قسم «توجه السوق» — مستقلٌّ لا يؤثّر في شيء

`scripts/market-direction.mjs` → `data/market-dir.json`، في دورة السوق
بعد `track-strategies`، **بلا شبكة**.

- **الفريمات ثلاثة: ساعة · ‎4‎ ساعات · يومي. لا ‎15د‎ بقرار** — السؤال
  عن الميل لا عن اللحظة.
- **إحدى عشرة طريقة**: الاستراتيجيات العشر **مثبَّتةً** على الفريم
  (`c.tfPin`)، ومعها محرّك الفرص ممثَّلاً بـ`an[tf].score` بعتبة
  ‎±15‎ — **نفس عتبة `allTF`** لا رقمٌ جديد.
- **خادميّ لا لحظيّ**: ‎16‎ أصلاً × ‎3‎ فريمات × ‎11‎ طريقة = ‎528‎
  تقييماً، كلٌّ يحتاج ملفَّ شمعات. يُقال «لقطةُ خادم» في الشاشة.

### تثبيتُ الفريم `tfPin` — خيارٌ في السياق لا فرعٌ في الشيفرة

`sqTf`/`iTf15`/`divTf`/`moSlow`/`pbTf` تقرؤه كلُّها، فيبقى المسار
الحسابي واحداً. ولو فُرِّعت الشيفرة لنسختين لتباعدتا بأول تعديل — نفس
درس `planFrom(inputs, d)` و`evalGates(only)`.

- **وما لا يقبل التثبيت يُعلَن `off` ولا يُحشى محايداً**:
  `orb`/`vwapRec` استراتيجيتا جلسةٍ داخل اليوم، و`tfAlign` حكمٌ عابرٌ
  للفريمات، و`pbTrend` يحتاج فريماً أعلى فيسقط على اليوميّ (لا أسبوعيّ
  عندنا). العمود اليوميّ يخرج بسبعٍ عاملة من إحدى عشرة — **ويُقال
  العدد**.
- **حارسُ دائرية في `dayAgree`**: بوابة «موافقة الاتجاه اليومي» كانت
  مكتوبةً **ستَّ مرّات بنصّها**. وعند التثبيت على اليوميّ تسأل «هل
  يوافق اليوميُّ اتجاهاً مشتقّاً من اليوميّ نفسه؟» — الجواب ‎+1‎
  دائماً، فتُضاف نقطةٌ مجّانية إلى كل استراتيجية في العمود. تُسقَط
  (`null`) فتخرج من المقام. وهي نفس قاعدة «بوابةٌ بلا سابقة تُسقَط ولا
  تُعامَل محايدة»: الإضافة بصفرٍ تخفض كل نتيجة، وبواحدٍ ترفعها.
- **و`momo` كانت ترقن `"1h"` و`"15m"` في أحد عشر موضعاً**، فتثبيتُها
  على ‎4‎ ساعات كان يعطي عموداً عنوانه «‎4‎ ساعات» وأرقامُه من الساعة.
- **`tfAlign` يُحسب مرّةً على مستوى الأصل** لا في عمود: استنساخُه في
  الأعمدة الثلاثة يَعُدّه ثلاث مرّات.

### الأوزان — ولا واحد منها مخترَع

| الوزن | القيمة | من أين |
|---|---|---|
| فريم الساعة | ‎1.0‎ | `TF_WEIGHT` في `score.js` — **تُستورد ولا تُنسخ** |
| فريم ‎4‎ ساعات | ‎1.5‎ | نفسها |
| الفريم اليومي | ‎2.0‎ | نفسها |
| `tfAlign` (ساق الأصل) | ‎1.0‎ | أدنى وزنٍ في الجدول — حافّته المقيسة سالبة |
| المؤشّر مقابل السهم | ‎3×‎ | يمثّل خمسمئة شركة لا شركة |
| وزن كل استراتيجية | `weightFor` | حافّتها المقيسة ‎0.4..1.8‎ — لا وزنٌ جديد |

- **والمقام ما شارك فعلاً**: فريمٌ بلا قراءة يخرج من البسط والمقام معاً.
  مقامٌ ثابت يجعل أصلاً بفريمٍ واحد يبدو **أضعف** من أصلٍ بثلاثة وهو
  ليس كذلك — هو أقلُّ معلوميةً، ويُقال ذلك بعدد السيقان لا بخفض الرقم.
- **ونسبةُ التوافق العام مقامُها ذواتُ الجهة** لا الكلّ: أصلٌ محايد لا
  يوافق ولا يخالف، وعدُّه في المقام يخفض النسبة كلّما هدأ السوق — فتقيس
  الهدوء لا الاتفاق.

### أكبر ‎15‎ شركة — الدمج بالهوية لا بالاسم

`ranking.top` فيه `GOOGL` و`GOOG` معاً: فئتا أسهمٍ لشركةٍ واحدة. وأخذُ
أعلى خمس عشرة حرفياً يعطي **أربع عشرة شركة** إحداها مرّتين، ويزيح شركةً
حقيقية خارج القائمة. الدمج بـ`cik.json` — رقمُ الشركة عند SEC **هويّةٌ**
لا تخمين، ويحلّ `BRK-A`/`BRK-B` و`FOX`/`FOXA` و`NWS`/`NWSA` بنفس
الحقل. والبادئة النصّية بديلٌ **معلَن** حين يغيب الملف.

### `SPY` يُجلب ويُخزَّن — ولا صفَّ له في الملخّص

`cfg.indices` كانت تُجلب أسعارُها ولا تُحفظ شمعاتُها، فلم يملك المشروع
سلسلةً واحدة لـ S&P 500. صار `fetch-market` يكتب `data/sym/SPY.json`
(وQQQ وDIA وVIXY) بالفريمات الأربعة.

- **ولا يُضاف صفٌّ إلى `summary.rows` بقرار**: شرطُ `mkt !== "crypto"`
  مكرَّرٌ في **اثني عشر موضعاً** (الاتساع، القطاعات، الرابحون،
  الخاسرون، البحث، القوائم، الارتباط). وطبقةٌ ثالثة تفرض مراجعتها
  كلَّها، ونسيانُ واحدةٍ يُدخل المؤشّر في «اتساع السوق» فيُحسب داخل
  نفسه. الملفّ يُكتب ويُقرأ بالاسم، ويُحفظ صفٌّ مصغَّر في `market.bench`.
- **وسعرُه المعروض مستوى المؤشّر** من `market.indices` لا سعر الصندوق:
  من يقرأ «إس آند بي 500» ينتظر ‎7,585‎ لا ‎757‎.
- وحارسٌ في `--check` يفحص **البيانات المكتوبة** لا النيّة: لا بديل في
  `summary.rows` ولا في الرابحين/الخاسرين.

### `renderMarketDir` لا `renderTrend`

`renderTrend` **مأخوذة** لتسلسل النتيجة في بطاقة السهم. وتعريفٌ ثانٍ
بنفس الاسم يرفع ويغلب بلا خطأ ولا تحذير — مصيدة `renderEarnings`
الموثّقة، ويفحصها `check-ui` منذ ذلك.

### وسمُ التبويب «السوق» لا «توجه السوق»

قِيس على الهاتف: ‎41.7‎ بكسل لكل تبويب، و«توجه السوق» تلتفّ سطرين بين
ثمانيةٍ بسطرٍ واحد فتكسر إيقاع الشريط. واسمُ القسم كاملٌ داخله.

```bash
node scripts/market-direction.mjs --check
node local/run.mjs mdir                  # بلا شبكة
```

### حافّةٌ معروضة لنظامٍ لم يعد قائماً — والصفرُ يُصاح به لا يُبتلع

إعادةُ بناء الأرشيف اللحظي بعد نقل الفريم أخرجت `orb` و`vwapRec`
بـ**صفر حالة** على ‎1,154,403‎ شمعة، بعد أن كانتا ‎45,481‎ و‎28,673‎.

والسبب **ليس** تغيير الفريم: `ctxAt` في `backtest-strategies` لا تضع
`win` قط، واستراتيجيتا الجلسة تشترطانها (`if (!c.win) return …`).
فكانتا تخرجان `off` عند كل شمعة. والحارس `!c.win` أُضيف في دفعة
‎13:27 UTC‎ والأرشيف القديم بُني ‎06:50‎ — أي أن **قياسهما صار مستحيلاً
منذ ذلك الكومّت**، والملفّ احتفظ بالأرقام القديمة و`weightFor` تزن
بهما الإجماع.

- **رقمٌ صحيحٌ يوماً ما يصف نظاماً لم يعد قائماً أخطرُ من غياب رقم**:
  لا شيء يبدو معطلاً — الملفّ موجود وحقولُه مملوءة ومداها معقول.
- ولم يكشفه تشغيلٌ ولا فحص، بل **إعادةُ بناءٍ** فرضها تغييرٌ آخر.
- **صفرُ حالةٍ في مليون شمعة سياقٌ مكسور لا ندرةُ سوق**: السطر الختامي
  كان يعدّ «المقيسة» و«ما تحت حدّ العيّنة» ويمرّ على الصفر صامتاً. صار
  يصيح بأسماء ما خرج بصفر.
- **وثلاثةُ حقولٍ ناقصة لا واحد**: بعد إصلاح `win` بقيتا على صفر —
  `orb.side` تقرأ `c.ian` (مؤشّرات السلسلة الجارية) وهي غائبة،
  و`undefined[...]` ترمي فيُبتلع الاستثناء وتخرج «بلا جهة» عند كل
  شمعة. ثم كشف الفحصُ `sessOf` ثالثةً — وبها يقارن `volRatio` الحجمَ
  بخطّ أساسٍ من نفس نوع الجلسة.
- **والقائمة المكتوبة بيدٍ تنسى ما نسيه كاتبُها.** أوّل صيغةٍ للفحص عدّت
  الحقولَ المطلوبة في مصفوفةٍ مكتوبة — فأغفلت `ian` بالضبط كما أغفلتها
  الشيفرة، ومرّت. **فهي تعيد إنتاج الخطأ لا تكشفه.** الصيغة الصحيحة
  تشتقّ القائمة من `buildCtx` **وقتَ التشغيل**: أيُّ حقلٍ يُضاف هناك
  يصير مطلوباً هنا تلقائياً، ولا يمكن أن يُنسى في الاثنين معاً. أُثبت
  أنه يُسقط نفسه بإزالة `win` ثم `ian`.
- و`tfAlign` تخرج بصفرٍ **بحقّ** لا بخلل: تشترط الفريمات الأربعة،
  و‎15د‎ لا يغطّي إلا ستين يوماً من تاريخٍ يوميٍّ طوله خمس سنوات — وهو
  حدٌّ موثَّق من قبل.

> **القاعدة**: أيُّ سياقٍ يُبنى في مكانين (الحيّ والأرشيف) يجب أن
> يُفحَص تطابقُ حقوله آلياً. والفرق بينهما لا يظهر خطأً بل **صمتاً**.

---

## الاتجاه كان يُشتقّ في أربعة مواضع — و`stocks/direction.js` وحّدها

بلّغ مستخدم عن `AVGO`: الفرصة «Call / صعود»، والقراءة الفنية ‎−71%‎،
والفريمات الأربعة هابطة. والقياس أعطى السبب بالضبط: السهم يُطلق
**شرطين متعاكسين في آن** — `alignDn` (▼، اتجاهه من النتيجة ‎−68.8‎)
و`divBull` (▲، يفرض `planDir: 1`). و`oppDirOf` كانت تأخذ **أوّل** اتجاهٍ
مفروض تجده بلا أن تسأل هل يعارضه اتجاهٌ راسخ.

والأربعة لم تكن نسخاً من دالّة واحدة بل أربع طرق مختلفة:

| الموضع | كان يشتقّ الاتجاه من |
|---|---|
| `planDirOf` في `plan.js` | النتيجة الفنية الخام |
| `oppDirOf` في `index.html` | أوّل شرطٍ مفروض بلا فحص تعارض |
| `track-signals` | لكلّ شرطٍ على حدة — فسجلّان متعاكسان للحظة الواحدة |
| `tradePlan` عبر `forcedFor` | **أقدم إشارةٍ مفتوحة في السجلّ** |

- **والرابع هو الذي يفسّر شكوى «الفرصة Call والتفاصيل Put» حرفياً**:
  بطاقة الفرصة تقرأ `oppDirOf` وبطاقة التفاصيل تقرأ سجلّاً مفتوحاً قد
  يكون بُني بسياسةٍ سابقة. وجدتُه **بعد** أن ظننتُ الإصلاح تامّاً —
  كشفه فحصٌ في المتصفّح لا فحصٌ في Node.

### الثبات يُورَّث من `band` ولا يُخترع له آليةٌ جديدة
`bandStable` تمنع تغيّر النطاق إلا بتجاوز حدّه بـ‎3‎ نقاط، والخادم وحده
يحسبها لأنه وحده يملك الدورة السابقة. فاشتقاق الاتجاه من **النطاق
المثبَّت** بدل النتيجة الخام يعطي الهيستريسس مجّاناً — وهو جواب شكوى
«تتحوّل من ‎74%‎ Call إلى ‎90%‎ Put خلال خمس دقائق». وقِيس: صفر تغيّر
اتجاه عند الاستبدال، أي أنه إضافةُ ثباتٍ لا تغييرُ سلوك.

### التعارض الجوهري — حدُّه النطاق المتطرّف لا كلُّ خلاف
`conflictOf` تُطلق حين يعاكس الفرضُ نطاقاً ‎0‎ أو ‎4‎ (حدود `labelOf`
نفسها ‎∓45‎، لا عتبةٌ مخترَعة). والميل العادي (‎1‎ و‎3‎) يبقى للفرض:
إلغاؤه كلَّه يهدم حافّةً مقيسة (`vol` ‎−0.72 → −0.34‎) لعلاج حالةٍ
متطرّفة.

- **والحجب للشرط المتناقض لا للرمز كلّه.** حجبُ `AVGO` كلَّها يضيّع
  `alignDn` وهي إشارةٌ هابطة **صحيحة** توافق النطاق. فاتجاهُ الرمز
  واحد، ثم يُسقَط ما لا يوافقه. القياس: ‎131‎ من ‎138‎ رمزاً تبقى بفرصة
  متّسقة مقابل ‎116‎ لو حُجبت الرموز كاملةً، و**‎20‎ صُحّح اتجاهها**
  (كلُّها بنفس البصمة: `alignDn` + شرطٌ يفرض الصعود).

### مصائد كلّفت تشخيصاً في هذه الجولة
- **فحصٌ في المتصفّح يمرّ لأن شيئاً لم يُنفَّذ.** أوّل صيغةٍ خرجت
  «صفر مخالفة» و`بلا_خطة = 125` مساويةً لعدد الفرص — أي أن كلّ صفٍّ
  خرج قبل التأكيدات. والسبب `typeof levelsInputs === 'function'` وهي
  **غير موجودة أصلاً** فابتلع الحارسُ الفشل. **اطبع عدد التأكيدات
  المنفَّذة** لا عدد المخالفات وحده: صفرٌ من صفر ليس نجاحاً.
- **عامل الخدمة يقدّم `index.html` القديمة حتى مع `cache: 'no-store'`**
  — اعتراضُه يسبق ترويسات HTTP. فبدا أن الإصلاح «لم يُطبَّق» والقرص
  والخادم متطابقان بايتاً ببايت (‎561942‎). يُلغى تسجيلُه وتُحذف
  الذاكرات ثم يُعاد التحميل. وهي مصيدة «الزيارة الثانية» الموثّقة
  ظاهرةً في التطوير لا في الإنتاج.
- **السجلّ يُحكم عليه ببياناته هو لا ببيانات اليوم.** هجرة `conv:
  "unidir"` تقرأ `sig.sc` (نتيجة السجلّ **لحظة إشارته**) لا نتيجة
  الرمز الآن — الحكم ببيانات لاحقة هو بعينه النظرُ إلى المستقبل.
  أُغلق ‎29‎ سجلاً.

```bash
node scripts/check-direction.mjs    # اختبار القبول — 10 فحوص على الكون الحيّ
```

---

## الفرصة كانت تنتظر هدفاً ثالثاً لا وجود له

`reached >= 3` رقمٌ مثبَّت يفترض ضمناً أن كلّ خطة ثلاثية الأهداف.
والقياس: **‎141‎ من ‎399‎** سجلاً مفتوحاً له أقلّ من ثلاثة (‎50‎ بلا
هدف · ‎43‎ بهدف · ‎48‎ بهدفين). فخطةٌ بهدفين تبلغ هدفيها **كليهما**
وتبقى «جارية» حتى ينتهي أفقُها بعد ‎28‎ يوماً.

- **والأثر ليس إحصائياً**: `openKey` يمنع أيّ إشارة جديدة لنفس
  `الرمز|الشرط` ما دامت مفتوحة — فتُحجب الشركة عن تحليلٍ جديد شهراً
  كاملاً بينما فرصتُها المعروضة انتهت فعلاً.
- والمقام لم يتأثّر: `hitRate` يشترط `tgts(s).length > i` أصلاً، فخطةٌ
  بهدفين خارج مقام الهدف الثالث قبل التعديل وبعده.
- **وهو نفس نمط «اختبارٌ يثبّت رقماً»** المعالَج مرّتين قبلاً
  (`SCANS.length !== 8` و`!== 90`) — مطبَّقاً هنا على **منطق التشغيل**
  لا على فحص. عند رؤية رقمٍ مثبَّت اسأل: ما الذي يفترضه ضمناً؟

### حالةٌ قائمة لم تُغيَّر بقرار: ‎23%‎ من الفرص بلا هدفٍ واحد
‎30‎ من ‎129‎ خطة معروضة تخرج بدخولٍ ووقفٍ و**صفر أهداف** — رمزٌ عند
طرفٍ (قمّة أو قاع) لا مستوى أمامه. وهي لا تكتمل أبداً ولا تُقاس إليها
نسبة. **لم تُعالَج**: علاجها إمّا توسيع بحث المستويات أو حجبها،
والثاني يمسّ `validatePlan` فيُبطل الأرشيف (‎1.08‎ مليون شمعة). قرارٌ
يحتاج صاحب المنتَج.

---

## Binance لا يعطي أسهماً أمريكية — يعطي **رموزاً مرمَّزة**، وكانت تتسرّب

`AAPLBUSDT` و`SPYBUSDT` و`NVDABUSDT` موجودةٌ فعلاً وحالتُها `TRADING`
وسعرُها يتتبّع السهم ضمن ‎0.1%‎. ولذلك بدت صالحةً بديلاً — وليست:

| المقياس | السهم الحقيقي | المرمَّز عند Binance |
|---|---|---|
| حجم MSFT اليومي | ‎35,360,121‎ | **‎207‎** (‎0.0006%‎) |
| حجم AAPL | ‎54,182,922‎ | ‎3,028‎ (‎0.0056%‎) |
| حجم NVDA | ‎131,906,400‎ | ‎31,011‎ (‎0.023%‎) |
| شبكة الشمعات | جلسة نيويورك | **‎24/7‎** (‎24‎ ساعة ET مميّزة، صفر شمعة فارغة) |

فكلُّ بوابة حجم (`volX` · OBV · MFI · POC · `volRatio`) تصير ضجيجاً،
وكلُّ حسابِ جلسة (VWAP · نطاق الافتتاح · البيفوت) يُبنى على شبكةٍ
خاطئة. **المصدر لم يُغيَّر** — القرار: Binance للكريبتو وحده كما هو.

### وكان ‎34‎ منها داخل كون الكريبتو عندنا
أي أن `AAPL` في التطبيق مرّتين: سهماً حقيقياً و«عملة» اسمها
`AAPLB-USD`. وأخطرُ ما فيها أنها **تبدو سليمة تماماً** — سعرٌ صحيح
وشارتٌ متّسق.

- **والنمط النصّي `/B$/` لا يصلح**: `BNB` و`SHIB` و`ARB` و`TRB` و`CKB`
  و`DGB` عملاتٌ حقيقية تنتهي بالحرف نفسه — وحذفُها يمحو بايننس كوين
  وشيبا إينو وأربيتروم. من ‎87‎ مرشّحاً بالنمط، ‎34‎ فقط مرمَّزة.
- **المميّز بالهوية السعرية**: المرمَّز يتتبّع سهمه. والفجوة المقيسة
  بين أقصى مرمَّز (‎0.43%‎) وأدنى كريبتو (‎99.91%‎) واسعةٌ بحيث لا
  التباس — وهو مبدأ «الدمج بالهوية لا بالاسم» الذي حلّ `GOOGL`/`GOOG`
  بـ`cik.json`.
- و`stockPx` اختيارية: بلا أسعار أسهم لا يُستبعد شيء **ويُعلَن ذلك** —
  مرشِّحٌ صامتٌ عاجز أسوأ من غيابه.

---

## الفرصة كانت تُثبَّت بسجلٍّ لا يُقاس — `NEAR-USD`

بلّغ مستخدم أن `NEAR-USD` ثالثةُ الفرص «ومنذ ‎8‎ أيام» وقد حقّق مستوياته
الثلاثة (‎+1%‎ و‎+2%‎ و‎+5%‎). والقياس فصل العلّة إلى شقّين لا واحد:

### ١) `109` سجلاً من `370` مفتوحة **بلا لقطة** — كلُّها بعمر ‎8.5‎ يوم
`updateOutcome` تردّ فوراً عند غياب `snap`، فلا `out` ولا دخول ولا وقف
ولا هدف: يبقى `open` حتى ينتهي أفقُه بعد ‎28‎ يوماً. ودفعةٌ واحدة أُنشئت
حين لم تكن ملفّات الشمعات متوفّرة كانت **مصدر كلّ «منذ 8 أيام» في
القائمة** (NVDA · BRK-B · TSLA · QCOM …).
- **ولا يبدو شيءٌ معطلاً**: الصفّ يُعرض والشرط يُطلق والسعر حيّ — والمرساة
  وحدها متحجّرة. نفس مصيدة «الأرقام تبدو صحيحة» مطبَّقةً على **الزمن**.
- تُغلق بـ`conv: "nosnap"` ولا تُرمَّم (بناء لقطةٍ اليوم يجعل السجلّ
  يدّعي أنه رأى ما لم يره)، **ولا يُنشأ سجلٌّ بلا لقطة أصلاً** بعد اليوم.
  و‎75‎ من ‎79‎ رمزاً تنجح لقطتُه الآن فيُعاد بناؤه بسعر يومه.

### ٢) المستويات المعروضة لم تكن تُنهي الفرصة — `oppSpent`
`marks = [1,2,5]` تُقاس من **سعر المسح المثبَّت**، وأهدافُ الخطة تُقاس من
**دخولٍ قد يكون ارتداداً لم يُلمس**. فيمكن أن تُبلَغ الثلاثة وخطةُ الرمز
ما زالت `wait`: قِيس **‎19‎ من ‎37‎** سجلاً بلغ المستويات الثلاثة ولم
تُفعَّل خطته قط.
- **ولذلك لا تُغلق المستهلَكة**: إغلاقُها يبتر قياس الخطة ويسجّل انتهاءً
  لم يقع. تُرفع عن **بوابة `openKey`** فحسب، فيُنشأ سجلٌّ جديد بسعر اليوم
  وخطةٍ جديدة — والقديم يُكمل قياسه. قِيس: ‎13‎ من ‎40‎ مستهلَكة تجدّدت في
  أوّل دورة.
- و`TRACK_MARKS` في `plan.js` **مشتركة**: رقمان مختلفان بين العرض والحكم
  يجعلان الشاشة تقول «بلغت الثلاثة» والسجلَّ يُبقيها نشطة — وهي العلّة
  بعينها.
- والمرساة تأخذ **الأحدث** من غير المستهلَكة لا الأقدم: بعد الاستهلاك
  يُنشأ سجلٌّ جديد، وهو الذي يصف الفرصة الآن.

**النتيجة المقيسة**: `NEAR-USD` من «🟠 ‎8‎ يوم · متأخرة» إلى «🟢 ‎5‎ دقيقة ·
عند ‎$2.61‎ · جديدة» بخطةٍ جديدة (دخول ‎2.597‎ · وقف ‎2.3978‎)، و**صفر
مرساة أقدم من سبعة أيام** على مستوى النظام بعد أن كانت ‎109‎ عند ‎8.5‎ يوم.

---

## تصنيف `bStocks` — الجواب الموثَّق عن أسهم Binance

**الفحص الأوّل كان ناقصاً**: بحثتُ بالبادئة في `exchangeInfo` فوجدتُ
الرموز، ولم أبحث عن **التصنيف**. والتصنيف موجود فعلاً:
`www.binance.com/bapi/asset/v2/public/asset-service/product/get-products`
يعيد ‎1372‎ منتجاً بـ‎27‎ وسماً، منها **`bStocks` بـ‎77‎ منتجاً**
(و`tCommodities` بـ‎12‎).

وBinance نفسها تسمّيها **«NVIDIA (bStocks)»** و«Apple (bStocks)»، والأصل
`NVDAB` لا `NVDA`، والمقابل `USDT` لا `USD`. والقياس على
AAPL/NVDA/MSFT/SPY:

| | bStocks | المصدر الحالي |
|---|---|---|
| السعر | مطابق ضمن ‎0.01–0.12%‎ | — |
| حجم MSFT بالدولار | ‎0.1‎ مليون | **‎17.4‎ مليار** (‎0.0006%‎) |
| حجم AAPL | ‎1.0‎ مليون | ‎18.1‎ مليار (‎0.0055%‎) |
| حجم NVDA | ‎7.0‎ مليون | ‎28.4‎ مليار (‎0.0246%‎) |
| شموع ‎30‎ يوماً | **‎8‎ منها عطلة أسبوع** | ‎0‎ |

فهو سوقٌ يعمل ‎24/7‎ بحجمٍ يساوي واحداً من كلّ عشرة آلاف — **لا يصلح
مصدراً ولا احتياطاً**، وكلُّ بوابة حجم وكلُّ حسابِ جلسة عليه ملفَّق.

- **والمرشِّح صار يقرأ الوسم الرسمي** (`listTaggedStocks`) بدل الاستدلال
  السعري، والاستدلالُ بديلٌ حين تتعذّر النقطة (خارج `api.binance.com` فقد
  تتغيّر) — ويُعلَن أيُّهما عمل. وصفرُ منتجٍ موسوم يعني **تغيّر شكل الردّ
  لا اختفاء القسم**، فلا يُقبل بصمت.
- ويسري عند إعادة بناء الكون: التشغيل الجافّ أكّد «استُبعد ‎77‎ سهماً
  مرمَّزاً — المصدر: وسم bStocks الرسمي».

---

## الفرصة كانت تنقلب صعوداً↔هبوطاً خلال دقائق — CONFIRMED مقابل LIVE

بلّغ مستخدم أن فرصة تظهر رقم ١ صعوداً بثقة ‎80-90%‎ ومتوافقة مع ٤-٥
استراتيجيات، وبعد ٥-١٠ دقائق تنقلب رقم ١ هبوطاً بثقة قريبة من ‎90%‎ —
دون أي إغلاق شمعة. والتشخيص (لا افتراضٌ عام) أثبت أن **الفريمات نفسها
سليمة**: 4h مبنيّة بحاوية زمنية لا فهرس، ولا فريم دون ‎15د‎ يدخل الحساب
في أي مكان (مفحوصٌ ذاتياً من قبل)، وتوجّه السوق ثابتٌ بنيوياً (‎1h/4h/1d‎
فقط، دورة عشر دقائق، لا يلمسه جلبُ الأسعار).

### السبب: `side()` يُعاد حسابه كل دقيقتين بسعرٍ لحظي بلا هيستريسس
`local/run.mjs`: دورة `quotes` (كل دقيقتين) تُشغّل
`track-strategies.mjs --only-price`، وهذه تستدعي `evalStrategy` لكل
الاستراتيجيات العشر بالسعر **اللحظي** — بما فيها `side(c)` نفسها، الدالّة
التي تقرّر الاتجاه الأساسي (‎+1‎/‎−1‎/‎0‎) لا قوّته فقط. ومقارناتها
(`c.px` مقابل EMA أو VWAP أو حدّ نطاق افتتاح أو قناة دونشيان) تملك
منطقةً ميتة ثابتة العرض (`tolOf`/`DEAD_ATR`) **بلا أي ذاكرة حالة** —
بخلاف `sBandStable` في نفس الملفّ التي تُثبّت **قوّة** النتيجة (٠..٣)
لا **جهتها**. فسعرٌ يتذبذب عند حافّة مستوى يقلب `dir` كل دورتي دقائق،
وبما أن عدّة استراتيجيات تُقاس بمستوياتٍ مترابطة قرب نفس السعر اللحظي
(EMA، VWAP، فتح اليوم)، تنقلب دفعةً واحدة — وهو ما يفسّر بالضبط «٤-٥
استراتيجيات صاعدة» تصير «عدّة هابطة» خلال دقائق.

- **ولم يكن الخلل في الفريمات فيُصلَح بتضييقها.** كان في **أيّ سعرٍ**
  يُقارَن بمستويات تلك الفريمات — لحظي بلا ذاكرة، أم إغلاق شمعةٍ مغلقة.
  نفس مصيدة «الأرقام تبدو صحيحة» مطبَّقةً على مصدر السعر لا الفريم.

### الحلّ: تقييمان لا نسخة، وسياقٌ مختلف لا مسارٌ مختلف
`confirmCtx(st, c)` في `stocks/strategies.js` تستبدل `c.px` بإغلاق آخر
شمعةٍ **مغلقة** (`k[length-2].c` — نفس اصطلاح `donch`/`volRatio`/
`follow` أن الأخير قد يكون جارياً) لفريم الاستراتيجية المُستعمَل
(`tfOf`/`tfUsed`)، ثم تُستدعى `evalStrategy` **نفسها بلا نسخ** على هذا
السياق. لا حاجةَ لتجميدٍ يدويّ ولا لمقارنةٍ بدورةٍ سابقة: مدخلُها
الوحيد (إغلاق الشمعة) لا يتغيّر بين دورتي أسعار ما لم تُغلَق شمعةٌ
جديدة فعلاً، فهي مستقرّةٌ تلقائياً — وهذا ما أثبته `check-opportunity-
stability.mjs` بسيناريو دونشيان اصطناعي: سعرٌ لحظي بين ‎10‎ و‎200‎ لا
يحرّك CONFIRMED بحرف، وإغلاقُ شمعة اختراقٍ فعلية يحرّكه فوراً.

- **`track-strategies.mjs` يكتب المجموعتين معاً**: `dir`/`sc`/`band`
  في `strategies.json` هي CONFIRMED الآن (ما يقرأه التوافق والترتيب في
  الفرص عبر `consFromRows`)، و`ldir`/`lsc`/`lband` LIVE — بادجٌ تكميليّ
  فقط، لا يُنشئ صفّاً بمفرده ولا يدخل السجلّ الحيّ ولا الترتيب.
- **البوابة (`if (!rConfirmed.dir...) continue`) صارت على CONFIRMED**:
  إشارةٌ يخلقها سعرٌ لحظيّ عابر ولا تصمد أمام إغلاق الشمعة **لا تظهر في
  الفرص أصلاً** الآن، بدل أن تظهر ثم تختفي.
- **`evalAllConfirmed` نظيرٌ لـ`evalAll`** — إضافةٌ لا استبدال: أدوات
  التشخيص تستعملها، ولم تُمَسّ `evalAll` في مستهلكيها الحاليين
  (`market-direction.mjs`، `check-strategies.mjs`، شاشة البحث الحيّة في
  الواجهة) كي لا يُكسر سلوكٌ عامل لإصلاح آخر.

### علّةٌ ثانية مستقلّة: `band()` في شاشة تفاصيل السهم بلا هيستريسس
`index.html` كانت تعيد حساب نطاق تسلسل الاتجاه بعتبةٍ خام
`[-45,-15,15,45].filter(...)` **بلا** هامش `bandStable`، فتُظهر «انقلب
الاتجاه منذ ٥ دقائق» على تذبذبٍ دون نقطة واحدة رغم أن `row.band`
المستقرّ في الخادم لم يتغيّر. الحلّ يمشي على نقاط التسلسل من الأقدم
للأحدث مطبِّقاً `bandStable` (المشتركة من `score.js`) تراكمياً، بدل
إعادة اشتقاقٍ مستقلٍّ لكل نقطة — نفس ما يفعله الخادم دورةً بعد دورة.

### نسبة «الثقة/التوافق %» ليست احتمال نجاح
`scsFrom` في `confluence.js` تحسب نقاءَ توافق الاستراتيجيات × اتساعه
(`SCS_FLOOR`/`coverage`) مُطبَّعاً ‎0..100‎ — وليس معدّل نجاحٍ تاريخي
مقيس ولا احتمالاً إحصائياً. عُلِّق ذلك صراحةً في الكود، وأُضيف توضيحٌ
في كل مكانٍ يُعرض فيه الرقم (بطاقة الاستراتيجيات، توجّه السوق) كي لا
يُقرأ «سهمٌ بـ‎90%‎ أرجح ٩٠ مرّة من مئة».

### لوحة تشخيص — `?` زرّ «تشخيص» في تبويب الاستراتيجيات
تعرض لكل رمزٍ CONFIRMED مقابل LIVE على الفريمات الأربعة معاً (وقت
الشمعة، مغلقة/جارية، كل استراتيجيةٍ ومساهمتها)، والاتجاه/النتيجة/الثقة
النهائية (من CONFIRMED)، وسطر إثباتٍ محسوبٍ من البيانات الفعلية لا نصّاً
ثابتاً: `forbiddenTfUsage(ctx)` يفحص مفاتيح `c.k`/`c.an`/`c.bw` الفعلية
لا استدعاءً يُعترَض — فريمٌ ممنوع لا يمكن أن يظهر إن لم يكن أصلاً في
البيانات التي يبنيها `buildCtx` من `rec.tf`/`rec.an` الخادميين.

```bash
node scripts/check-opportunity-stability.mjs
```

---

## `GS` صاعدة فوق أربعة فريماتٍ هابطة — تعارضٌ لم يكشفه `conflictOf`

بلّغ مستخدم عن `GS`: فرصة «صعود» فوق ‎15د‎/ساعة/‎4‎ساعات/يومي هابطة
بالإجماع. وهي **علّةٌ مستقلّة** عن تذبذب CONFIRMED/LIVE أعلاه — لم تمسّ
هذه الفريمات ولا منطقها، والخلل في المسار المنفصل تماماً:
`oppDirOf` ← `conflictOf` ← `resolveOpp` في `stocks/direction.js`.

القياس: نتيجة GS الفعلية ‎−45.88‎ (نطاقها الحقيقي ‎0‎ «هابط قوي»)، لكن
`row.band` **المحفوظ** بقي ‎1‎ لأن `bandStable` (هيستريسس ‎3‎ نقاط) لم
يكن قد لحق بعد — يحتاج ‎−48‎ ليهبط من ‎1‎ إلى ‎0‎. فشرطا `vol` و
`pullback` (كلاهما `planDir: 1`) مرّا من `conflictOf(1, band=1)` لأنها
تختبر ‎band === 0‎ حصراً، بينما `alignDn` الهابط السليم أُسقط — وهذا هو
بالضبط ما صُمِّم له `conflictOf`: يمنع الفرض ضدّ **نطاقٍ متطرّف**، ولم
يكن يفحص إجماع الفريمات الأربعة مباشرة قط.

- **`conflictOf` تقرأ مصدراً متأخّراً بطبيعته.** `band` يحمل هيستريسس
  عمداً (وسمٌ مستقرّ عبر الدورات)، فلحظةَ سقوطٍ حادّ في النتيجة يتأخّر
  النطاق عنها خطوةً كاملة — وهو بالضبط الفارق بين `−45.88` الفعلية و`1`
  المحفوظة. الفريمات الأربعة (`row.tfScore`) بلا هذا التأخّر: كلُّ فريمٍ
  رقمٌ مباشر من نفس اللحظة، فإجماعها **أقوى** من أي قراءةٍ مشتقّة من
  نطاقٍ لم يلحق بعد.
- **الحلّ دالّتان جديدتان في `direction.js`**: `allTfDir(tfScore)` تقرأ
  الفريمات الأربعة مباشرة بنفس عتبة `allTF` في `scans.js` (‎±15‎ — لا
  رقمٌ جديد)، و`tfConflict(dir, tfScore)` نظيرُ `conflictOf` بمصدرٍ
  مختلف. `resolveOpp` تستبعد أيّ فرضٍ يعاكس الإجماع التامّ، وحين تتّفق
  الفريمات الأربعة كلُّها **يُفرض اتجاهها نهائياً** — يعلو حتى على
  `base` المشتقّ من النطاق المتأخّر، لا على الفرض وحده.
- **ولا تُطلق على الاختلاف**: فريمٌ واحد يخالف الثلاثة الباقية
  (`allTfDir` تعود صفراً) يبقي القرار لمنطق `conflictOf`/النطاق تماماً
  كما كان — التعارض الجزئي معلومةٌ حقيقية (اختلاف آجال) لا يُنقض، ولا
  علاقة له بالتغيير.
- **نقاط الاستدعاء الثلاث** (`stocks/index.html`، `scripts/track-signals.mjs`،
  `scripts/check-direction.mjs`) تمرّر `tfScore` الآن إلى `resolveOpp` —
  نفس `row.tfScore` المحفوظ الذي يقرأه `allTF` أصلاً، لا حسابٌ موازٍ.
- **`conflictOf` نفسها لم تُمَسّ**: لا تزال تخدم غرضها الأصلي (النطاق
  المتطرّف)، والدالّة الجديدة تتحقّق من مصدرٍ مختلف تماماً بجانبها لا
  بدلاً منها.

### الاختبار — لا يعتمد على بيانات السوق
خمسة اختباراتٍ في `check-direction.mjs` بمدخلاتٍ اصطناعية بحتة (تبقى
صالحةً ولو لم يتكرّر إجماع GS بعينه لاحقاً): إجماعٌ هابط تامّ لا يخرج
صعوداً مهما فُرض، إجماعٌ صاعد تامّ لا يخرج هبوطاً، فريماتٌ متعارضة تترك
القرار لمنطق الفرض/النطاق كما كان بالحرف، نفس المدخلات عشر مرّاتٍ تعطي
نفس الاتجاه دائماً، وسيناريو GS بعينه محسوماً هابطاً مع سقوط `vol` و
`pullback`. والمقارنة الذهبية: نفس اختبار الإجماع الحيّ (كان موجوداً من
قبل ويفشل على GS فعلياً) يمرّ الآن على بيانات السوق الحيّة.

### لوحة تشخيص — «اتجاه الفرصة» داخل زرّ «تشخيص»
تُضاف تحت لوحة CONFIRMED/LIVE في نفس الزر: كل فريمٍ ووسمُه من
`row.tfScore`، إجماع الفريمات (`allTfDir`)، `row.score`/`row.band`،
`oppDirOf.base`/`forced`، `conflictOf`/`tfConflict`، ما بقي وما سقط من
`resolveOpp`، والاتجاه النهائي وسببه بجملةٍ مشتقّة من البيانات نفسها لا
نصّاً ثابتاً — نفس مبدأ `forbiddenTfUsage` أعلاه.

```bash
node scripts/check-direction.mjs
```

---

## التأكيد كان يستبدل السعر وحده — والسياق كلُّه كان لحظياً

بلّغ مستخدم بدليلٍ قابل للتكرار: `VLO` «الاستراتيجيات متعارضة» و`TMO`
«توافق ٢ استراتيجيات» عند ‎17:48‎، ثم بعد **ستّ دقائق** `VLO` «توافق ٣»
و`TMO` بلا وسمٍ أصلاً — ولا إغلاق شمعة ١٥د في الفارق ولا ما فوقها.
والاستنتاج الذي قدّمه (أن الحساب يجري على فريمٍ غير معلَن: دقيقة أو
ثلاث أو خمس) **خاطئ، والرصد صحيح**: `forbiddenTfUsage` يفحص مفاتيح
`c.k`/`c.an` الفعلية ويخرج بصفر، ولا فريم دون ‎15د‎ في الملفات المخزَّنة.

السبب الحقيقي أن `confirmCtx` كانت تنسخ السياق وتضع فيه **سعر الإغلاق
وحده** وتمضي — والسياق يحمل طبقةً مشتقّةً كاملة تتحرّك أسرع من الشمعة:

| المسرَّب | كيف يتحرّك دون إغلاق شمعة |
|---|---|
| `lv.L` | `levelsFrom` تقسّم المستويات فوق/تحت بـ`P0` = **السعر اللحظي**، فعبورُه مستوىً يبدّل `supAll[0]`/`resAll[0]` دفعةً واحدة |
| `an`/`ian` | محسوبةٌ خادمياً على السلسلة **بما فيها الشمعة الجارية**، فتتبدّل كل دورة سوق (عشر دقائق) وسط ربع الساعة |
| `lv.vwap` | `sessionVwap` تضمّ الشمعة الجارية — وهي أساس `vwapRec` |
| `bw` | آخرُ قيمةٍ فيها عرضُ الشمعة الجارية — وعليها يقوم `sqzExp` |

- **والقياس هو الذي فصل السببين.** محاكاةُ دورتَي خادم على الكون الحيّ
  (٢٢٣ رمزاً): تشويهُ الشمعة الجارية **بلا** إعادة حساب المؤشّرات يعطي
  ‎68‎ اختلافاً (كلُّها `lv.L`)، ومع إعادة حسابها — وهو ما يفعله
  `fetch-market` فعلاً في كل دورة — تقفز إلى **‎803‎ اختلافاً منها ‎474‎
  انقلابَ اتجاه**. أي أن نصف المشكلة كان يختبئ خلف محاكاةٍ ناقصة:
  **حاكِ مسار الخادم كاملاً وإلا قِست نصف العلّة وظننتها كلَّها.**
- والأثر المرئي بالضبط هو المُبلَّغ عنه: قفزةُ نتيجةٍ تعبر `ACT_MIN`
  تُدخل الاستراتيجية في الإجماع أو تُخرجها — `EUR-USD` من ‎50‎ إلى ‎67‎
  بحركة ‎0.4%‎ — فيتبدّل «توافق ٢» إلى «توافق ٣» أو إلى «متعارضة».

### الحلّ: سياقٌ بلا شمعةٍ جارية، من `buildCtx` نفسها
`closedRec` تحذف آخر شمعة من كل سلسلة وتعيد `analyze` من المقصوص، ثم
`confirmBase` تبني السياق بـ`buildCtx` **نفسها** — فتُعاد المستويات
وVWAP ونطاق الافتتاح وعرض بولنجر كلُّها من شمعاتٍ مغلقة، بمسارٍ حسابيّ
واحد لا نسختين. وكلُّ استراتيجيةٍ تأخذ إغلاق فريمها هي (`tfUsed`)،
وتُعاد `levelsFrom` بسعره وحده لأن التقسيم فوق/تحت يتبع السعر.

- **ولماذا تُحذف الشمعة الأخيرة بلا سؤال الساعة**: كلُّ شمعةٍ لها خليفةٌ
  في السلسلة مغلقةٌ بالضرورة — قاعدةٌ سليمة بلا تقويم ولا منطقة زمنية،
  وهي نفس اصطلاح `k[length - 2]` في `volRatio`/`donch`/`follow`. والبديل
  (اشتقاق «هل أُغلقت؟» من ساعة الحائط) **يكسر الفريم اليومي قبل
  الافتتاح**: شمعةُ أمس يقع «الآن» داخل مدّتها فتُقرأ جارية فتُحذف،
  فتصير المؤشّرات اليومية من أول أمس. والثمن المعلَن شمعةٌ من الطزاجة
  حين يكون السوق مغلقاً.
- **وبعد الحذف يصير آخرُ عنصرٍ هو الشمعة المغلقة الأخيرة**، فتقرأه
  البوابات بنفس دلالاتها بلا تعديل حرفٍ واحد فيها. ولهذا اختير الحذف
  على «سلسلتين متوازيتين»: تلك كانت ستترك `k.slice(-13,-1)` في `sqzExp`
  يشمل الشمعة التي تُقاس ضدّها، فلا يخترق السعرُ نطاقاً هو جزءٌ منه —
  علّةٌ صامتة تُسقط الاستراتيجية كلَّها.
- **`src` على السياق** هي وسائط البناء نفسها، وبها وحدها تستطيع
  `confirmBase` إعادةَ البناء. ولا تُقرأ في أيّ بوابة. وفي سياق الأرشيف
  `src: null` عمداً: الأرشيف يقيس عند إغلاق كل شمعة فلا شمعةَ جارية فيه،
  والتصريح بالغياب يجعل أيّ نداءٍ للتأكيد هناك يُعلن التعذّر بصوتٍ عالٍ
  بدل أن يتراجع صامتاً.
- **وفريمٌ غائبٌ ليس تأكيداً متعذّراً**: رمزُ الطبقة الواسعة بلا سلسلةٍ
  ساعيّة، و«متعذّر التأكيد» عنه عذرٌ مخترَع عن حالةٍ تقولها `ready`
  بدقّة. قِيس: ‎3,728‎ تقييماً كان يُوسَم تعذّراً، وصار صفراً بأسبابها
  الحقيقية.
- **وشاشة «الماسح» كانت تعرض `evalAll` اللحظية** بينما يكتب الخادم
  CONFIRMED — فتهتزّ بين دورتَي أسعار **وتخالف لحظات البدء** (`at`)
  الآتية من الخادم وهي مبنيّةٌ على CONFIRMED. صارت تعرض المؤكَّد،
  وLIVE يبقى محسوباً ويُعرض في لوحة التشخيص جنباً إلى جنب.
- **وسطرُ المصدر يقول ختم الشمعة**: «النتائج محسوبة على إغلاق شمعة
  ‎18 سبتمبر 10:30 م‎». فمن يرى رقمين مختلفين بفارق ستّ دقائق يقارن
  ختمَيهما بنفسه — إثباتٌ يقرؤه المستخدم لا وعدٌ نصّيٌّ بالثبات.

### ما لم يُغيَّر، وسببه
النتيجةُ الفنية (`an` المحفوظة و`score` و`tfScore` و`band`) **لم تُمسّ**:
تغييرُ تعريفها يُبطل الأرشيف (‎1,084,574‎ شمعة) ويستلزم إعادة بنائه.
وهي تتحرّك على دورة السوق (عشر دقائق) لا دورة الأسعار، و`bandStable`
تمنع وسمَها من الاهتزاز أصلاً. فالمُصلَح هنا هو محرّك الاستراتيجيات
وإجماعُه — وهو ما يعرضه وسم «توافق N استراتيجيات» المُبلَّغ عنه.

### الإثبات
`check-opportunity-stability.mjs` صار يشمل **اختبار القبول**: يمسح الكون
الحيّ بثلاث دوراتٍ داخل نفس ربع الساعة (سعرٌ لحظيّ متحرّك، وشمعةٌ جارية
مشوَّهة، وساعةٌ تتقدّم دقيقتين ثم تسعاً) ويشترط **صفر اختلاف**. ومعه
اختبارٌ ثانٍ يعيد سلوكَ ما قبل الإصلاح حرفياً ويشترط أن يُرصد مختلفاً —
فحصٌ يمرّ مهما كانت المدخلات ليس فحصاً.

واختبارٌ من طرفٍ إلى طرف على مسار الخادم الفعلي: دورتا `track-strategies`
بسعرٍ يختلف ‎1.2%‎ وشمعةٍ جارية متحرّكة أخرجتا `strategies.json` بـ‎949‎
صفّاً و**صفر اختلاف** في `dir`/`sc`/`band`/`act`، بينما اختلف `ldir`/`lsc`
في ‎189‎ صفّاً — أي أن المدخلات تغيّرت فعلاً والمؤكَّد وحده لم يتحرّك.

```bash
node scripts/check-opportunity-stability.mjs
```

---
## عقود الخيارات توقّفت أربع ساعات — `fetch` بلا مهلة تعلَّق ولا تفشل

بلّغ مستخدم بشارة «توقّف التحديث · عقود الخيارات · 4 ساعة و1 دقيقة» على
الموقع المنشور. والقياس: `optionsUpdated` في `meta.json` المحلي كان
طازجاً (‎5.6‎ دقيقة)، ودفعة `data` على GitHub وصلت لحظة الفحص — أي أن
العلّة انتهت من تلقاء نفسها **قبل** أن أبدأ التشخيص. فالسؤال لم يعد «ما
الحل» بل «ما الذي عطّل تسع دوراتٍ متتالية (‎09:35→14:05 UTC‎) بلا أن
يترك أثراً واحداً؟» — `data/.run.skips.json` صفرٌ من سجلّات `options`،
وسجلّ اليوم (`data/logs/*.log`) صفرُ سطرٍ يذكرها، وسجلّ Task Scheduler
التشغيلي **معطَّلٌ افتراضياً** (`Get-WinEvent -ListLog … | IsEnabled` ⇒
`False`) فلا شاهد نظامي عليه أيضاً.

غياب أي أثر هو الدليل نفسه: لو فشلت المهمّة (شبكة، 429، استثناء) لكانت
كتبت سطر خطأ أو دخلت مسار `gave-up` في القفل. صمتٌ تامّ يعني أنها **لم
تنتهِ من الأساس** — عالقة، لا فاشلة. `getSession()` في
`scripts/lib/yahoo.mjs` تطلب `https://fc.yahoo.com` بـ`fetch` الخام
**بلا** `AbortSignal` — الملفّ الوحيد في كل نداءاته الأخرى الذي يمرّ عبر
`req()`/`curlGet` بمهلة ‎20‎ ثانية، وهذا النداء وحده أُغفل. اتصالٌ يتعلّق
(لا يرفض ولا يرمي، فقط لا يردّ) يجعل `await` ينتظر إلى الأبد —
`.catch(() => null)` تصطاد الرفض السريع ولا تصطاد التعليق، فلا شيء
يوقفه غير `ExecutionTimeLimit` الخارجي (‎25‎ دقيقة)، ثم يتكرّر عند كل
مشغّل تالٍ ما دامت العلّة الشبكية قائمة — تسع دوراتٍ × ‎25‎ دقيقة ≈ نفس
الفجوة المقيسة بالضبط.

- **نفس الثغرة بالحرف كانت في `sec.mjs` (تُستدعى كل عشر دقائق عبر
  `fetch-filings.mjs`) و`fetch-events.mjs` (يوميّاً، BLS والفدرالي)** —
  كلاهما `fetch` خام بلا `signal`. أُضيفت مهلة ‎20‎ ثانية للثلاثة.
  `binance.mjs` و`finnhub.mjs` و`twelvedata.mjs` و`news.mjs` كانت محميّة
  أصلاً؛ لم تُمسّ.
- **القاعدة العامة**: كل `fetch` في كود مجدول يجب أن يحمل مهلة صريحة.
  غيابها ليس خطأ صامتاً بل **غياب أي خطأ على الإطلاق** — وهو أخطر من
  الفشل الصريح لأنه لا يترك أثراً يُشخَّص، ويناقض «الفشل السريع أفضل»
  الموثّق أعلاه من جذوره: التراجع الطويل موثَّقٌ كخطأ، والتعليق اللانهائي
  أسوأ منه.
- **ولم يُصلَح `build-universe.mjs`** (`fetch(SP500_CSV)` بلا مهلة
  كذلك): أداة صيانةٍ تُشغَّل يدوياً لا مجدولة، فتعليقها لا يُنتج نفس
  العرَض الصامت المتكرّر — يبقى ملحوظاً لصيانةٍ لاحقة لا عاجلة.
- **سجلّ Task Scheduler التشغيلي يستحقّ التفعيل** (`wevtutil sl
  Microsoft-Windows-TaskScheduler/Operational /e:true`، يحتاج صلاحيات
  مسؤول) — بلا هذا لا شاهد نظامي على متى قُتلت مهمّةٌ ولماذا، ونعتمد
  حالياً على غياب الأثر وحده للاستدلال.

---

## قائمة الفرص كانت تُحسب على شمعةٍ لم تُغلق — العلّة الجذرية

بلّغ المستخدم بثلاث صورٍ متتابعة (‎07:46‎ · ‎07:47‎ · ‎07:51‎ بتوقيت
الرياض) أن تبويب «الفرص» يتبدّل داخل شمعة الربع ساعة: ترتيبٌ يُقلب،
ورموزٌ تدخل وتخرج، و«88 نقطة» تصير ‎85‎. وقال إن العلّة قائمة منذ بداية
الموقع وإن كلَّ إصلاحٍ سابق أُعلن حلّه ولم يمسّها.

**وكان محقّاً.** الإصلاح السابق (`confirmCtx`/CONFIRMED) ثبّت **محرّك
الاستراتيجيات** وحده، وهو يعدّل درجة الفرصة ‎±15‎ نقطة فحسب. أمّا
**عضوية القائمة وترتيبها والرقم المعروض** فتأتي من مكانٍ آخر لم يُمَسّ:

| المدخل | من أين | كم يتحرّك |
|---|---|---|
| `r.score` · `r.tfScore` | `analyze` على السلسلة **بما فيها الشمعة الجارية** | كل دورة سوق (عشر دقائق) |
| `r.p` | السعر اللحظي من دورة الأسعار | كل دقيقتين |
| `r.vol` | حجمُ جلسةٍ **يتراكم** طوال اليوم | كل دقيقتين |

وهي مدخلات الشروط كلِّها: `allTF` عضويةً و`-r.score` رتبةً في «توافق
الفريمات»، و`r.p` في «قرب قمة/قاع 52 أسبوعاً» و«ارتداد»، و`r.vol` في
«حجم غير معتاد».

**القياس على الكون الحيّ (218 رمزاً)**: بحذف الشمعة الجارية وحدها
تتغيّر نتيجةُ **‎85.3%‎** من الرموز، وسيطُ الفرق ‎5.88‎ نقطة وأقصاه
‎34.71‎، و**‎28‎** رمزاً تنقلب عضويتُه في شرطَي «توافق الفريمات»، و**أوّل
ثمانية في القائمة يختلفون بالكامل**.

- **ولا شيء يبدو معطلاً**: السعر حيّ والشارت يتحرّك والأرقام في مداها.
  نفس مصيدة «الأرقام تبدو صحيحة» مطبَّقةً على **الشمعة** بدل الرمز.
- **ولا يُبطل الإصلاح الأرشيف بل يوافقه**: `backtest.mjs` يقيس عند كل
  شمعة بـ`p: c[i]` — إغلاقُ شمعةٍ **مكتملة**. فكان الأرشيف يقيس المغلق
  والواجهة تعرض الجاري، وصارا يقيسان الشيء نفسه.

### `closedBars` — تعريفٌ واحد لـ«الشمعة المغلقة الأخيرة»
في `stocks/indicators.js`، يقرؤه `fetch-market` حين يحسب `an`،
و`closedRec` حين يبني سياق التأكيد، والفحوص. ثلاث إجاباتٍ متقاربة أسوأ
من واحدة: تتباعد بأول تعديل فيصف المؤكَّد في مكانٍ شمعةً وفي مكانٍ
أخرى، بلا خطأٍ ولا أثر.

- **الفريمات اللحظية**: طولُ الشمعة معلوم، فهي جاريةٌ ما دام
  `t + طولها > now`. دقيقٌ بلا تقويم ولا منطقة زمنية.
- **الفريم اليومي**: لا طول ثابت له — الجلسة ‎6.5‎ ساعات لا ‎24‎، و
  `t + 86400000` يعطي جواباً خاطئاً **دائماً** (شمعة أمس مختومة ‎13:30‎
  تُقرأ «جارية» حتى ‎13:30‎ اليوم وهي مغلقة منذ ‎20:00‎ أمس). فالمقياس
  **اليوم نفسه**: جاريةٌ إن كان تاريخُها تاريخَ اليوم بتوقيت UTC — وهو
  نفس منطق `pivotBar` في `plan.js` ولنفس السبب. أُثبت على بياناتٍ حيّة:
  NVDA اليوميّة ‎09-18T13:30‎ تُقرأ مغلقةً بعد منتصف الليل، وبيتكوين
  ‎09-19T00:00‎ تُقرأ جارية.
- **والحذف مشروطٌ لا مطلق.** كان `dropLast` يحذف الأخيرة دائماً، وعلّتُه
  المعلنة أن «لكل شمعةٍ خليفةً مغلقةً بالضرورة». والقاعدة صحيحة
  ونتيجتُها مكلفة: حين يكون السوق مغلقاً تكون الأخيرة **مغلقةً هي
  نفسها**، فحذفُها يقرأ الأسهم الأمريكية مساءً على شمعةِ **أمس**
  اليومية — عمىً كاملٌ عن جلسةٍ انتهت، طوال الليل وعطلة الأسبوع، وهو
  أغلب وقت استعمال التطبيق من الرياض. والمشروط يعطي الثبات نفسه
  (المعيار لا يتبدّل إلّا عند إغلاق شمعةٍ فعلاً) بلا هذا الثمن.

### `scanRow` — الصفّ المؤكَّد، مدخلُ الشروط
`pc` (إغلاق أدقّ فريمٍ مغلق) و`volc` (حجم آخر شمعةٍ يومية مغلقة) و`cbar`
(ختمُ شمعة التأكيد) تُكتب في صفّ الملخّص **بجانب** `p`/`vol` لا بدلاً
منهما: `p` هو السعر المعروض في كل شاشة، وسعرٌ متأخّرٌ ربعَ ساعة في
الترويسة خللٌ ظاهر. والفصل بين «ما يُعرض» و«ما يُقاس عليه» هو نفس فصل
LIVE عن CONFIRMED في الاستراتيجيات.

و`scanRow` في `scans.js` لا عند كلّ مستهلك: ثلاثة يقرؤون الشروط — شاشة
الفرص، وحارس الاتجاه، وسجلّ الإشارات — وثلاثُ نسخٍ من «أيّ سعرٍ يُقاس
عليه» تتباعد فيصير السجلّ يقيس غير ما تعرضه الشاشة. والأرشيف يمرّ بها
بلا أثر: صفوفُه مبنيّةٌ من شمعاتٍ مغلقة أصلاً فلا `pc` فيها.

**و`cbar` يُعرض في سطر عدد الفرص**: «على إغلاق شمعة ‎08:15‎». من يرى
ترتيبين مختلفين بفارق خمس دقائق يقارن ختمَيهما بنفسه — إثباتٌ يقرؤه
المستخدم لا وعدٌ نصّيٌّ بالثبات.

### الرجوع الصامت إلى السعر اللحظي — كشفته المقارنة الحيّة لا الفحص
`scanRow` تعيد الصفّ كما هو حين يغيب `pc`. وصفوف الطبقة الواسعة
المكتوبة قبل هذا الحقل كانت تقع في ذلك: `GMEB-USD` من «‎96%‎ من النطاق»
إلى «‎97%‎» بين دورتَي خادمٍ حقيقيّتين تفصلهما سبع دقائق **وشمعةُ
تأكيدهما واحدة** — بلا خطأ ولا سطر، صفٌّ واحد يهتزّ وسط عشر قوائم
ثابتة.

- والصفر هو القيمة الصحيحة دائماً بعد أوّل تحديثٍ لكل طبقة: الرمز
  الجديد يُجلب قبل أن يدخل الملفّ فيحمل `pc` منذ صفّه الأول. فبقاءُ
  أيّ صفٍّ بلا الحقل يعني ملفّاً لم يُجدَّد بعد تغيير التعريف.
- و`wide.json` **تراكمي**، فلا يُصلح نفسه إلّا بدورة تدوير كاملة
  (‎931‎ صفّاً على ‎60‎/تشغيل ≈ يوم). يُفرض تحديثٌ واحد
  (`WIDE_PER_RUN=1200 node local/run.mjs market`) — وهي نفس هجرة
  «تُقدَّم ساعةُ الفريم الساعي إلى الوراء بعد تعديل `aggregate`».
- وفحصٌ يرفع الصمت: `check-opportunity-list.mjs` يفرّق بين الطبقتين لأن
  إيقاعهما مختلف بنيوياً — المرصودة تُجدَّد كل عشر دقائق فصفٌّ بلا `pc`
  فيها خللٌ في الكاتب يُسقط الفحص فوراً، والواسعة تُجدَّد بالتدوير على
  صلاحيةٍ يومية فتبقى صفوفٌ بالشكل القديم حتى يأتي دورها. والشرط
  المنضبط: **أيُّ صفٍّ جُلب بعد أوّل صفٍّ يحمل `pc` يجب أن يحمله** —
  يمرّ أثناء التدوير، ويسقط فورَ توقّف الكاتب عن كتابته.
- **والمجدول مشبَع فلا تُتاح لحظةٌ بلا قفل**: دورة السوق تستغرق أطول من
  دورتها (قِيس ‎23‎ دقيقة على دورة عشر دقائق)، و`.run.skips.json` مليءٌ
  بـ`gave-up`. فوسمُ الصفوف بـ`u = 0` يدوياً يُداس من مهمّةٍ قرأت الملفّ
  قبل الوسم وكتبته بعده — ثلاث محاولات ضاعت هكذا. الوسم يُعاد دورياً
  حتى تقرأه دورةُ سوقٍ فعلاً، أو يُترك للصلاحية اليومية تُتمّه وحدها.

### الاختبارات كانت تفترض «الأخيرة تُحذف دائماً» — ثلاث مصائد فيها

١. **شمعاتٌ مرساها ختمٌ ثابت في 2023** (`t0 = 1_700_000_000_000`):
   كلُّها في الماضي فآخرُها **مغلقة**، فاختبارٌ يُفسد `k[n-2]` ليقول
   «الشمعة المغلقة الأخيرة بلا إغلاق» كان يقيس افتراضه لا الشيفرة.
   المرساة الآن ربعُ الساعة الجاري.

٢. **`Date.now()` مرساةً للمسح**: الاختبار يدّعي «داخل نفس ربع الساعة»
   ثم يقفز تسع دقائق من لحظةٍ عشوائية — فيعبر حدَّ شمعةٍ في أغلب
   الأحيان، ويُقرأ **إغلاقٌ صحيح** خللاً في الثبات. المرساة الآن لحظةُ
   كتابة الملفّات، والخطوات كلُّها دون ‎900‎ ثانية منها.

٣. **تشويهُ الشمعة الأخيرة مطلقاً**: شمعةٌ **مغلقة** لا يعيد المصدر
   كتابتها، وتشويهُها محاكاةُ فسادِ بياناتٍ لا محاكاةُ دورة. التشويه
   الآن على الجارية وحدها (`isLiveBar`)، وهو تمييزٌ ماديّ يُطبَّق على
   المسارين المقارَنين معاً — وإلّا قُورن مدخلان مختلفان وسُمّي الفرقُ
   أثراً للشيفرة.

٤. **وصفُّ الملخّص كان مثبَّتاً في محاكاة الخادم** بينما يعيد الخادم
   بناءه كلَّ دورة، فأيُّ تسرّبٍ يمرّ عبر `row.tfScore` كان مخفيّاً.
   نفس الدرس الموثّق: **حاكِ مسار الخادم كاملاً وإلّا قِست نصف العلّة
   وظننتها كلَّها.**

### الختم كان يتخلّف عن المحرّك — فيبدو الإغلاق المشروع خللاً

بعد الإصلاح بقي عرَضٌ يُقرأ كالعلّة تماماً: القائمة تتغيّر والختمُ ثابت.
والسبب أن **الشمعة تُغلق بمرور الوقت لا بوصول بيانات**:

| | من يكتبه | دورتُه |
|---|---|---|
| `cbar` في صفّ الملخّص | `fetch-market` | عشر دقائق |
| شمعةُ التأكيد الفعلية | `track-strategies` بساعته هو | دقيقتان |

فعند ‎09:00‎ تصير شمعة ‎08:45‎ مغلقةً ويتقدّم المحرّك إليها **بلا أن
يتغيّر ملفّ الرمز أصلاً** (قِيس: ختم الملفّ ‎08:53‎ وشمعة التأكيد تقدّمت
من ‎08:30‎ إلى ‎08:45‎ عند ‎09:00‎)، بينما `cbar` يبقى على ‎08:30‎ حتى
دورة السوق التالية. **الفارق المقيس: ‎15‎ دقيقة كاملة.**

- والأثر ليس تجميلياً: الختم هو الإثبات الوحيد الذي يفرّق للمستخدم بين
  «تغيّرَ لأن شمعةً أُغلقت» و«تغيّرَ بلا سبب». فختمٌ متخلّف يجعل الإغلاق
  المشروع يُقرأ خللاً — ويُبطل الغرض من عرضه.
- `track-strategies` يكتب `confBar` (ختم الشمعة التي حُسب عليها
  CONFIRMED)، والواجهة تعرض **الأحدث** من الاثنين: فلا يتقدّم رقمٌ بلا
  ختمٍ يشرحه.
- **وقياسُ الثبات يجب أن يستعمل القاعدة نفسها لا موضعاً ثابتاً.** أوّل
  مِقياسٍ أخذ `c[c.length - 2]` فقال «تغيّرت CONFIRMED وشمعتها ثابتة»
  في ‎70‎ صفّاً — وكان المقياس هو الخاطئ لا النظام. والقياس بـ`closedBars`
  نفسها أعطى الصورة الصحيحة: `BTC-USD|orb` ثابتٌ عند ‎1/93‎ في أربع
  كتاباتٍ متتالية على شمعة ‎08:30‎، ثم ‎1/85‎ عند ‎09:00‎ حين أُغلقت
  ‎08:45‎. **أداةُ القياس تُختبر قبل أن يُستنتج منها.**

### النشر كان يصل نصفَه — والمستخدم يرى العلّة وقد أُصلحت

فُحص الموقع المنشور بعد الدفع، فكانت `scanRow` و`closedBars` **غير
معرَّفتين** في الصفحة الحيّة رغم أن الملفّات على الخادم مطابقةٌ للقرص
بايتاً ببايت (أربعتها بـ`md5`). والسبب عامل الخدمة: «قديمٌ ثم يُجدَّد»
يحدّث كلَّ ملفٍّ **على حدة**.

- القياس: احتاج **ثلاث** عمليات تحميل ليكتمل. وفي الثانية منها كانت
  `scans.js` جديدة و`index.html` **قديماً** — حالةٌ مختلطة تشغّل المسار
  القديم، فتظهر العلّة وقد أُصلحت فعلاً. وهذا بالضبط ما يجعل المستخدم
  يقول «جرّبتُ ولم يتغيّر شيء» وهو محقٌّ فيما يرى.
- ولا يكفي أن يكون الملفّ صحيحاً على الخادم: **إصلاحٌ لا يصل ليس
  إصلاحاً**. والتحقّق من `curl` وحده يشهد للخادم لا للمتصفّح.
- **رفعُ `V` يجعل الانتقال ذرّياً**: عاملٌ جديد يثبّت الهيكل كلَّه من
  الشبكة دفعةً واحدة ثم يحلّ محلَّ القديم، فالتحميل التالي يقرأ نسخةً
  متّسقة. وكان التعليق في `sw.js` يقصر الشرط على **إضافة ملفٍّ** إلى
  `CORE`، والصواب أنه شرطُ أيّ تغيّرٍ في محتوى أيّ ملفّ هيكل.
- و`SHELL_SHA` في `sw.js` يُلزم بذلك: `check-ui` يحسب بصمة ملفّات
  `CORE` الأربعة عشر ويقارنها، فيسقط حتى تُرفع النسخة وتُحدَّث البصمة.
  أُثبت أنه يُسقط نفسه بلمس ملفٍّ واحد. **القاعدة تُنفَّذ ولا تُترك
  للتذكّر** — نُسيت فعلاً في أوّل دفعةٍ لهذا الإصلاح.

### ما يُثبت الإصلاح فعلاً — دورتان حقيقيتان لا محاكاة
المحاكاة تُثبت الخاصيّة، والتشغيل الحقيقي يُثبت أنها تصف النظام. أُجريت
ثلاث دوراتِ `fetch-market` حقيقية:

| المقارنة | شمعة التأكيد | النتيجة |
|---|---|---|
| ‎05:44‎ مقابل ‎05:45‎ | ‎05:15‎ مقابل ‎05:30‎ | اختلفت — **وهو الصواب**: أُغلقت شمعة بينهما |
| ‎05:45‎ مقابل ‎05:52‎ | ‎05:30‎ مقابل ‎05:30‎ | ‎10‎ من ‎11‎ قائمة متطابقة حرفياً · والحادية عشرة كشفت الرجوع الصامت |

**والمقارنة الأولى شرطُ قبولٍ كالثانية**: نظامٌ لا يتحرّك أبداً ليس
ثابتاً بل معطَّلاً. ولذلك يُفحص الطرفان معاً — لا يتغيّر داخل الشمعة،
ويتغيّر عند إغلاقها.

```bash
node scripts/check-opportunity-list.mjs        # القائمة نفسها: عضوية وترتيب ورقم معروض
node scripts/check-opportunity-stability.mjs   # المحرّك تحتها: CONFIRMED مقابل LIVE
```

---

## الاتجاه كان بلا هيستريسس — والانقلاب يعود أدراجه خلال ساعة

سأل المستخدم: «هل إزالة ‎50%‎ أو أكثر من الشركات تحلّ انقلاب النقاط
والتوافق؟» والجواب المقيس **لا**، والعلّة في مكانٍ آخر تماماً.

### ما قِيس أولاً — وما سقط من الفرضيات
ثلاث فرضياتٍ جُرّبت وسقطت، **لا تُعَد تجربتُها**:

1. ❌ «شمعة الساعة تصل متأخّرة لأن ‎55‎ لا تقسم ‎60‎». الشمعة تُختم
   **ببدايتها**: بار ‎23:00‎ يُغلق عند ‎00:00‎، فسلسلةٌ تنتهي بـ‎23:00‎
   عند ‎00:26‎ صحيحةٌ لا متأخّرة. والحساب يثبت أن الجلب يسبق الإغلاق
   دائماً، والقياس يؤكّده: ‎5‎ رموز من ‎655‎ ينقصها شمعة، وكلُّها
   يتيمةٌ ميتة (`TON-USD` متأخّر ‎82‎ يوماً).
2. ❌ «ATR يصغر في الرموز الراكدة فتنهار المنطقة الميتة». العكس:
   منطقة `牛来-USD` الميتة ‎0.17%‎ ومنطقة `BTC-USD` ‎0.02%‎.
3. ❌ «النتيجة غير قابلة لإعادة الحساب من الشمعات المخزَّنة». كانت
   **أداةُ القياس** هي الخطأ — نسيتُ ضرب ختم الشمعة في ألف فاختلّ
   `closedBars`. بعد التصحيح: ‎1152‎ من ‎1155‎ تُعاد بالضبط.

> **والدرس أقوى من الفرضيات**: `أداةُ القياس تُختبر قبل أن يُستنتج
> منها` — قاعدةٌ موثّقة من قبل، ووقعتُ فيها مرّةً أخرى وكدتُ أُبلّغ عن
> عطلٍ في ‎171‎ رمزاً لا وجود له.

### وما ثبت سليماً — بقياسٍ حيٍّ على المجدول الفعلي
إصلاحات ‎19‎ سبتمبر تعمل، وأربع مقارنات على كتاباتٍ حقيقية تثبت الطرفين:

| | شمعتُه ثابتة | شمعتُه تقدّمت |
|---|---|---|
| `summary.json` — النتيجة | **‎0‎** تغيّر | ‎50‎ من ‎72‎ |
| `strategies.json` — المؤكَّد | **‎0‎** من ‎998‎ | ‎52‎ (‎12‎ انقلاباً) |

### العلّة الحقيقية — `side()` بلا ذاكرة حالة
`tolOf(atr) = atr × DEAD_ATR` شريطٌ **متماثلٌ وبلا ذاكرة**: يمنع
الانقلاب *عند* الخطّ ولا يمنعه *حوله*، ولا يفرّق بين «ابقَ» و«انقلب».
و`sBandStable` كانت تُثبِّت **قوّة** النتيجة ولا شيء يُثبِّت **جهتها**.

قياسٌ على `data/strat/*.json` (أربعٌ وعشرون ساعة):

| الاستراتيجية | سلاسل | ذهابٌ وإياب | وسيط حركة السعر للرحلة كاملةً |
|---|---|---|---|
| **`vwapRec`** | 72 | **349** | **‎0.22%‎** |
| `volSpike` | 247 | 93 | ‎0.84%‎ |
| **`orb`** | 72 | 18 | **‎0.06%‎** |
| `tfAlign` · `pbTrend` · `sqzExp` | 388 | **0** | — |

`AAVE-USD|vwapRec` انقلب ‎▲→▼→▲‎ في ‎67‎ دقيقة والسعر تحرّك ‎0.31%‎.
والمتّهمتان الأُولَيان هما استراتيجيتا **عبور مستوى** — سعرٌ يتذبذب على
جانبَي خطّ.

### `DIR_HOLD = 3` — الحلّ، ولماذا الصمود لا النتيجة
| مدّة بقاء الاتجاه بعد الانقلاب | ربيع١ | وسيط | ربيع٣ |
|---|---|---|---|
| انقلابٌ يعود أدراجه (ضجيج) | ‎7‎د | **‎17‎د** | ‎60‎د |
| انقلابٌ يثبت (إشارة) | ‎360‎د | **‎571‎د** | ‎960‎د |

التوزيعان **منفصلان تماماً**، فالصمود مميِّزٌ حاسم: ثلاث شمعات تقتل
‎70%‎ من التذبذب و**لا تُلغي إشارةً واحدة** (‎0%‎ عند كل عتبة).

- **وجُرّب `sc` أولاً فسقط**: توزيعُه عند الانقلاب المتذبذب شبه مطابقٍ
  للثابت (وسيط ‎65‎ مقابل ‎70‎)، وأفضلُ نسبة فائدة ‎1.80×‎ — أي أنه
  يُسكت من الصحيح قريباً ممّا يُسكت من الضجيج. **مقياسٌ ضعيف التمييز
  لا يصلح بوابة** مهما بدا معقولاً.
- **والعدّ بالشمعات المغلقة المتمايزة لا بالدورات**: `evalStrategy`
  تُنادى كل دقيقتين والشمعة تُغلق كل خمس عشرة، فالعدّ بالنداءات يجعل
  «ثلاثاً» تسع دقائق ويتبدّل معناه بتغيير دورة المجدول.
- **والمرساة تنجو من السكون**: `side` صفراً يُنشر صامتاً و`ld` تبقى.
  بدونه يصير المسار ‎+1 → 0 → −1‎ بابَ تحايلٍ يلتفّ على الآلية كلّها.
- **وبلا ختمِ شمعةٍ لا إمساك**: ختمٌ غير معروف يعني عدّاداً لا يتقدّم
  أبداً — واتجاهٌ **مُمسَكٌ إلى الأبد** أسوأ من الانقلاب الذي بُنيت
  الآلية لمنعه.
- **وفي `evalStrategy` وحدها** — المسار الذي تمرّ به العشر جميعاً،
  و`opt.prev` ممرَّرٌ فيه أصلاً فلا سباكة جديدة. نفس درس
  `planFrom(inputs, d)` و`evalGates(only)`: خيارٌ في المسار لا فرعٌ
  في الشيفرة.
- **والمرساة تُمرَّر إلى المتصفّح** (`evalAllConfirmed(c, holdBy)`)
  كما تُمرَّر `at` و`px0` و`band`: الهيستريسس يحتاج ذاكرةً بالدورة
  السابقة والخادم وحده يملكها. تركُها في المتصفّح يجعل البطاقة تعرض
  جهةً غير التي نشرها الخادم — خلافُ محرّكين لا إصلاحُ واحد.
- **والتربّص يُعلَن ولا يُكتم** (`pend` وشارة «‎▲‎ متربّص ‎1/3‎»):
  الآلية تمنع نشرَ الانقلاب لا إخفاءَ أنّه يتكوّن، وكتمُه يجعل الشاشة
  تبدو جامدةً أمام سوقٍ يتحرّك.

### الحالة تُخزَّن خارج `rows` — وتُقلَّم
`rows` لا تحمل إلا ما تفعّل (`if (!rC.dir) continue`)، فالمرساة في
الصفّ وحده تضيع عند أوّل سكون. وخريطةٌ مستقلّة `hold` بمفتاح
`SYM|strat` وقيمة ‎[ld, pd, pn, pb, ts]‎ — **بمهلة ساعة** (صمتت ساعاتٍ
ثم نطقت ليست منقلبة بل مبتدئة)، و**لا يُخزَّن منها إلا ما لا يُشتقّ من
الصفّ**: قِيس ‎1063‎ مدخلاً قبل التقليم و‎35‎ بعده، والملفّ ‎402 → 365‎
ك.ب وهو يُقرأ عند فتح التبويب.

### الأرشيف يجب أن يقيس ما يُنشر
`backtest-strategies` كان ينادي `evalStrategy(st, c)` بلا إمساك —
فيشهد لنظامٍ لم يعد قائماً، وهو الخطأ الموثَّق بعينه. صار يحمل مرساةً
لكل (رمز × استراتيجية) تتقدّم شمعةً شمعة، و**التهدئة صارت تمنع التسجيل
لا التقييم**: منعُه يجمّد العدّاد ثماني شمعات فيختلف مسارُ الأرشيف عن
مسار الخادم في الموضع الذي بُني ليطابقه.
**ويلزم إعادة بناء `strategy-edge.json`** (`node local/run.mjs stratbt`).

### الإثبات
- إعادةُ تشغيل `dirHold` الحقيقية على تسلسل سبعة أيام (‎51,688‎ نقطة ·
  ‎3,835‎ سلسلة): الانقلابات **‎4,885 → 2,499‎ · انخفاض ‎49%‎**.
- المجدول التقطها في أوّل دورة وأمسك ستّ حالاتٍ حقيقية، **كلُّها في
  `vwapRec` و`orb`** — الاستراتيجيتان اللتان حدّدهما القياس.
- وفي المتصفّح: `GRAM-USD` يعرض «‎▼‎ استعادة VWAP ‎75‎ قوي · ‎▲‎ متربّص
  ‎1/3‎» — الخادم والمتصفّح على جهةٍ واحدة وصفر خطأ في `__diag`.
- و`check-strategies` **يُسقط نفسه**: بإعادة `DIR_HOLD = 1` تفشل أربعة
  اختبارات، وتعود كلُّها بالاسترجاع.

```bash
node scripts/check-strategies.mjs     # 32 فحصاً منها خمسةٌ للهيستريسس
```

---

## القائمة كانت تتبدّل لأن ترتيبها ينتظر ملفّاً — لا لأن شمعةً أُغلقت

بلّغ المستخدم أن العلّة «لا تزال» على الموقع المنشور بعد ثلاث جولاتِ
إصلاحٍ أُعلن نجاحُها. **وكان محقّاً، وكلُّ إصلاحٍ سابق كان صحيحاً ولا
يمسّها**: العلّة في طبقةٍ لم يكن يغطّيها فحصٌ واحد.

### ما ثبت سليماً — بقياسٍ حيٍّ لا بافتراض
| ما قِيس | النتيجة |
|---|---|
| الشيفرة المنشورة مقابل القرص | مطابقة (10 ملفّات · `md5`) |
| الخادم بين دورتين داخل نفس الشمعة | صفر تغيّر في `pc`/`cbar`/`score`/`band`/`tfScore` · صفر انقلاب CONFIRMED |
| تذبذب السعر اللحظي في المتصفّح | صفر تبدّل على 11 مسحاً |
| الفحوص الذاتية الستّة | ‎88‎ فحصاً · صفر فشل |

فالشمعة ثابتة والبيانات ثابتة والشيفرة صحيحة — **والقائمة تتبدّل**.

### السبب: `go("screen")` يطلب أربعة ملفّات كسولة **ثم يرسم فوراً**
وكلُّ ملفٍّ يصل يعيد الرسم. فالقائمة تُبنى ثلاث مرّات في كل فتحة،
بترتيبٍ مختلف في كل مرّة. القياس على الموقع المنشور:

| ما وصل | الأثر |
|---|---|
| `strategies.json` (‎~800‎ مللي) | **‎437‎ صفّاً** يتغيّر ترتيبه · إزاحة تبلغ **‎14‎ مرتبة** · فارق **‎15‎ نقطة** في الدرجة · وصدارةُ مسحَين تتبدّل (`pullback`: CSX ← RTX · `cheap`: HON ← SMCI) |
| `wide.json` | `vol` من **‎0‎ إلى ‎113‎** صفّاً · `divBull` من ‎8‎ إلى ‎51‎ |
| `fundamentals.json` | `cheap` من ‎0‎ إلى ‎53‎ |

وهو بالحرف ما وصفه المستخدم: «‎88‎ نقطة تصير ‎85‎» (والفارق الأقصى
‎15‎ = `SCS_W`)، و«الترتيب يُقلب»، و«رموزٌ تدخل وتخرج».

- **ولا علاقة له بالشمعة**، ولهذا لم تمسَّه إصلاحاتُ `confirmCtx` و
  `closedBars` و`dirHold` — وكلُّها صحيحةٌ وباقية. المتغيّر **أيُّ
  الملفّات وصل** لا **أيُّ شمعةٍ أُغلقت**.
- **وسطر «الترتيب بالمسح وحده» كان يُقال فعلاً** ولا يُقرأ: من يرى
  قائمةً تُعاد ترتيبها أمامه لا يقرأ سطراً يشرح، بل يستنتج عطلاً.
  **إعلانُ حالةٍ مؤقّتة لا يغني عن عدم عرضها.**

### الحلّ: لا يُعرض ترتيبٌ سيُنقض
حارسٌ في `renderScreen` يؤجّل الرسم ما دام ملفٌّ من ملفّات الترتيب
**في الطريق**، ويعرض هيكلاً. ثمنُه ‎~800‎ مللي في أوّل فتحة.

- **والفشل ليس انتظاراً**: `need` تحذف `lazyWait[f]` عند الخطأ، فالملفّ
  الساقط يخرج من الشرط وتُعرض القائمة بترتيب المسح الخام مع سطرها
  المعلَن — التدهور الهادئ كما هو بلا تغيير.
- **والطبقة الواسعة تُطلب قبل الحارس لا بعده**: طلبُها بعده يجعلها غير
  «منتظَرة» في أوّل رسم، فتُعرض قائمةٌ ناقصة ثم تكتمل.
- **ومهلةٌ عليا ثماني ثوانٍ**: انتظارٌ بلا سقف يترك الشاشة هيكلاً إلى
  الأبد، وهي أسوأ من ترتيبٍ يُعدَّل.

### `scripts/check-live.mjs` — الفحص الذي كان ناقصاً، ولماذا لا يغني عنه غيره
كلُّ فحوصنا تشهد للمنطق على القرص، والمستخدم يرى **الصفحة المنشورة بعد
أن يمرّ عليها عاملُ الخدمة والشبكة وترتيبُ وصول الملفّات**. والعلّة
عاشت في هذه الفجوة بالضبط. يفحص ما لا يستطيع Node فحصه:
١) أن الشيفرة المنشورة هي شيفرة القرص (بإلغاء عامل الخدمة أوّلاً — وإلا
   شهد لنسخةٍ مخزَّنة ومرّ كذباً)، ٢) أن ما يراه المستخدم لا يتبدّل بعد
ظهوره، ٣) أن تذبذب السعر لا يحرّك القائمة، ٤) صفرُ استثناءٍ وصفرُ
`diag`.

- **والقياس هو ما يراه المستخدم لا ما يصل الذاكرة**: هيكلُ التحميل ليس
  قائمة، فمقارنتُه بالقائمة تقيس وصول البيانات لا ثباتها. الشرط
  المنضبط: **من اللحظة التي تظهر فيها صفوف، لا تتغيّر بعدها.**
- **وتأخيرٌ صناعيّ للملفّات الكسولة شرطُ أن يكون الفحص حاسماً**: بلا
  `page.route` بتأخير ‎1400‎ مللي كان يمرّ على localhost **حتى بعد
  تعطيل الحارس** — لأن الملفّات تصل قبل أوّل رسم فلا يقع السباق أصلاً.
  أُثبت أنه يُسقط نفسه: بلا الحارس يفشل على ‎10‎ مسوح، ومعه يمرّ.

> **القاعدة**: أيُّ ترتيبٍ أو رقمٍ معروض يعتمد ملفّاً كسولاً يجب ألّا
> يُرسم قبل وصوله. وأيُّ إصلاحٍ يُعلَن يجب أن يُختبر على **الرابط
> المنشور** لا على القرص.

```bash
node scripts/check-live.mjs                       # على الموقع المنشور
node scripts/check-live.mjs http://localhost:8791/stocks/   # على نسخة محلية
```

### والعلّة الثانية نفسُ الجذر: الحوافُّ أوزانُ الإجماع لا زينتُه

بعد إصلاح القائمة بقي انقلابُ الجهة المُبلَّغ عنه. والسبب أن
`strategy-edge.json` يصل **بعد** `strategies.json`، و`weightFor` تزن كل
استراتيجية بحافّتها المقيسة (‎0.4..1.8‎) — فالإجماع المحسوب قبل وصولها
يزن الكلَّ بواحد. **وهو إجماعٌ آخر لا إجماعٌ ناقص.**

قِيس على الكون الحيّ: وصولُ الحوافّ يقلب جهة **ثلاثة رموز** —
`AVGO` من «متعارض» إلى **هبوط**، و`TXN` و`MCHP` من **صعود** إلى
«متعارض». وهو بالحرف «تظهر صعود ثم تنقلب خلال دقائق بلا إغلاق شمعة».

- **`consReady()` مُسنَدٌ واحد لخمسة مستهلكين**: `consSnapshot` و
  `rkRows` و`renderScreen` وبطاقة السهم وتحليل الماسح. وخمسُ نسخٍ من
  «هل يمكن الحساب الآن؟» تتباعد بأول تعديل.
- **وتحليلُ الماسح ينتظر ملفّيه** (`await Promise.all([need, need])`)
  بدل أن يرسم ثم يُعيد — والانتظار مضمون الانتهاء: `need` تنتهي نجاحاً
  أو فشلاً ولا تبقى معلّقة.

### ثلاث حالاتٍ للملفّ الكسول لا حالتان — وخلطُها أبقى الحارس ميتاً

أوّل صيغةٍ لـ`consReady` فحصت `lazyWait` وحده، فمرّت: الملفُّ الذي
**لم يُطلب بعد** يبدو كالملفّ الذي **وصل**. والنتيجة حارسٌ يبدو أنه
يعمل ولا يُطلق في الحالة التي بُني لها — نفس عائلة «الحارس الميت»
الموثّقة ثلاث مرّات قبله (`!sc.dir` · `{ secMed: {} }` · `conv !== "long"`).

| الحالة | الدليل | التصرّف |
|---|---|---|
| وصل | `state.X` موجود | يُحسب |
| في الطريق | `lazyWait[f]` موجود | **يُنتظر** |
| سقط | `lazyFail[f]` | يُحسب بأوزانٍ محايدة ويُقال |
| لم يُطلب | لا هذا ولا ذاك | **يُطلب** ثم يُنتظر |

`lazyFail` أُضيف لأجل التمييز الثالث، و`need` تضبطه في الطرفين.
**القاعدة: حقلٌ غيابُه له أكثر من معنى لا يُفحص بوجوده.**

### وكلُّ حارسٍ يُسقط فحصَه وحده — عزلٌ سببيّ لا نجاحٌ جماعي
| المعطَّل | «ما يراه المستخدم» | «اتجاه الإجماع» |
|---|---|---|
| حارس الترتيب | **✗ ‎10‎ مسوح** | ✓ |
| حارس الإجماع | ✓ | **✗ ‎3‎ رموز** |
| لا شيء | ✓ | ✓ |

نجاحٌ جماعي يخفي حارساً لا أثر له؛ وهذا الجدول يثبت أن كلَّ حارسٍ
يمنع علّته هو.

---

## قائمة الفرص كانت مزجَ ملفّين بإيقاعين مختلفين — واللقطة الذرّية أنهت ذلك

بلّغ المستخدم بثلاث صورٍ موقَّتة (‎02:51‎ · ‎02:53‎ · ‎02:57‎ الرياض =
‎23:51‎ · ‎23:53‎ · ‎23:57‎ UTC) — كلُّها داخل شمعة ‎23:45–00:00‎:
`NEAR-USD` من ‎82‎ إلى ‎85‎ نقطة، و`ARB-USD` خرج، و`BTC-USD` و
`PENDLE-USD` دخلا. **ولا شمعةَ أُغلقت بينها.**

وكان محقّاً: الإصلاح السابق (تأجيل الرسم حتى تصل الملفّات) عالج سباقَ
**التحميل** داخل الصفحة الواحدة، ولا يمسّ تبدّلَ **البيانات** بين
الزيارتين.

### ثلاثة إيقاعات لا يقسم أحدها الآخر

| الكاتب | الملفّ | الدورة |
|---|---|---|
| `fetch-market.mjs` | `summary.json` (`score` · `cbar` · `tfScore`) | **‎10‎ دقائق** |
| `track-strategies.mjs --only-price` | `strategies.json` (`confBar` · CONFIRMED) | **‎دقيقتان‎** |
| الشمعة نفسها | — | **‎15‎ دقيقة** |

```
23:45 أُغلقت الشمعة → 23:46 confBar يتقدّم → 23:51 cbar يتقدّم
                       └──────── خمس دقائق يصف فيها الملفّان شمعتين ───────┘
```

والقائمة كانت **مزجَهما**. فتتبدّل مرّتين في كل ربع ساعة، في لحظتين
لا علاقة لأيّهما بإغلاق شمعة — فتُقرأ انقلاباً عشوائياً.

- **وأُثبت حيّاً لحظة بناء الأداة**: أوّل تشغيل لـ`build-opportunities`
  ردّ بالنصّ «المصدران على شمعتين: summary=23:45Z · strategies=00:00Z».
  البوّابة التقطت العلّة في أوّل ثانية من وجودها.
- **و`fetch-quotes` بريء**: لا يكتب `score` ولا `pc` ولا `cbar` —
  يكتب `p` و`pReg` و`chg` وحدها. الاتّهام الأوّل كان خاطئاً، والقياس
  هو الذي برّأه.

### الحلّ: `data/opportunities.json` — لقطةٌ ذرّية مفتاحُها شمعة

- **الحساب انتقل إلى الخادم كاملاً** (`stocks/opportunities.js` +
  `scripts/build-opportunities.mjs`). والمتصفّح **يقرأ ويعرض**: لا
  يرتّب ولا يحسب درجةً ولا يعيد تقييم شرط.
- **السعر اللحظي يُعرض سعراً فقط** ولا يدخل عضويةً ولا ترتيباً ولا
  درجة — وفحصٌ يثبّت ذلك بتذبذبٍ ‎±0.6%‎ و‎+40%‎ حجماً وصفرِ أثر.
- **حارسٌ في الواجهة لم يكن ليكفي**: يؤجّل العرض ولا يثبّت المحتوى.
  ما دام الحساب في المتصفّح على ملفّات متحرّكة، تتحرّك القائمة.

#### البوّابات الثلاث — كلُّها ترفض الكتابة ولا تُصلح
١. **المصدران على شمعةٍ واحدة**: `max(cbar) === confBar` وإلا **انتظار**.
   واختلافُهما ليس خطأً بل الحالة الطبيعية لدقائق بعد كل إغلاق —
   والصواب الانتظار لا المزج.
٢. **لا كتابة داخل نفس `candleKey`**: بصمةٌ تتغيّر بلا تقدّم المفتاح
   **تُرفض** وتبقى اللقطة السابقة. وهذا ما يمتصّ تدويرَ الطبقة الواسعة
   وسط الشمعة (`WIDE_PER_RUN`) — يُؤجَّل أثرُه إلى الإغلاق التالي.
٣. **استثناءٌ واحد معلَن**: تغيّرُ `strategyVersion` (بصمة `scans.js`
   و`confluence.js` و`direction.js` و`consensus.js` و`strategies.js`
   و`score.js` و`opportunities.js`) يسمح بإعادة البناء داخل نفس
   الشمعة. بدونه لا يصل أيُّ إصلاحٍ إلى المستخدم قبل ربع ساعة.

- **الترتيب على الكون كاملاً مرّةً واحدة**، ومرشّحُ نوع السوق يصفّي
  ولا يعيد الترتيب. كان يُعاد داخل كل نوع، فدرجةُ الرمز تتغيّر بإخفاء
  رموزٍ أخرى — **ودرجةٌ تتبدّل بتغيير مرشّحِ عرضٍ ليست درجةَ الرمز**.
  وهو أيضاً ما جعل اللقطة ثلثَ حجمها (‎113‎ ك.ب لـ‎500‎ صفّ).
- **الكتابة ذرّية** (ملفّ مؤقّت ثم `rename`): الكتابة المباشرة تترك
  نافذةً يقرأ فيها المتصفّح نصف ملفّ.
- **`build-opportunities` يلي `track-strategies` مباشرةً** في دورة
  السوق: كلاهما يُكتب في التشغيل الواحد من نفس الملفّات، فتتحقّق
  البوّابة الأولى بالضرورة. وفي دورة الأسعار يُشغَّل ويُمسك غالباً —
  وهو الصواب.
- **سجلّ التدقيق** `opportunities-log.json`: لكل محاولة `action`
  (`write`/`hold`/`skip`) و`why` و`candleKey` و`rowsHash` و
  `strategyVersion` و`count`. آخر مئتين — تشخيصٌ لا أرشيف، ومستبعَدٌ
  من النشر.

### مصيدةٌ في الأداة نفسها: `secMed` كائنٌ لا رقم
أوّل صيغةٍ لوسائط القطاعات أعادت رقماً، و`cheap` يقرأ `m.pe` — فخرج
الشرط **صفر صفّ** وهو ‎55‎، بلا خطأ ولا استثناء. نفس عائلة
`{ secMed: {} }` الموثّقة: **حارسٌ يقرأ حقلاً لا وجود له فيصمت.**
والشكل جزءٌ من العقد لا تفصيلاً.

### الإثبات — أداتان، وكلٌّ تفحص ما لا تفحصه الأخرى
```bash
node scripts/build-opportunities.mjs --check          # البوّابة: 10 فحوص بلا شبكة
node scripts/check-opportunity-production.mjs 7 120   # الإنتاج: 7 عيّنات × دقيقتين
node scripts/check-live.mjs                           # الموقع المنشور: 9 فحوص
```
- **والشرط مزدوجٌ في الاختبار الإنتاجي**: صفرُ اختلافٍ داخل المفتاح،
  **و**تغيُّرٌ عند تقدّمه. نظامٌ لا يتحرّك أبداً ليس ثابتاً بل معطَّلاً،
  وفحصٌ يقبل الجمود يشهد للعطل.
- **ويُثبت أن السعر تحرّك** بين العيّنات: بلا ذلك يكون «صفر اختلاف»
  سكونَ سوقٍ لا أثرَ إصلاح.

### دورة السوق حوذِيت مع شبكة الشمعة — ‎15‎ دقيقة عند ‎:01/:16/:31/:46‎

بعد اللقطة الذرّية بقيت مسألةُ **الطزاجة** لا الثبات: اللقطة لا تتقدّم
إلا حين يتقدّم `cbar`، وهو يتقدّم بدورة السوق. وبدورةٍ عشريّة تقع
اللحظةُ في أيّ موضعٍ من الشمعة، فقد تتأخّر اللقطة أربع عشرة دقيقة.
قِيس فعلاً: شمعة ‎00:00‎ أُغلقت ‎00:15‎ ولم تُكتب إلا ‎00:23‎.

بـ‎15‎ دقيقة عند ‎:01‎ يقع كلُّ تشغيلٍ **بعد إغلاق شمعةٍ بدقيقة**،
فيتقدّم `cbar` و`confBar` معاً فوراً وتُكتب اللقطة. وهو أيضاً **أقلُّ
حملاً** (أربع دورات في الساعة بدل ستّ) — والدورة كانت تتجاوز مدّتها
أصلاً (‎23‎ دقيقة مقيسة على دورة عشر دقائق).

- والتصادم مع دورة الأسعار عند ‎:16‎ و‎:46‎ يمتصّه القفل بالانتظار
  (~‎13‎ ثانية)، لا بالانسحاب — وهو ما صار عليه القفل منذ جولةٍ سابقة.

### الإثبات الإنتاجي — سبع عيّنات من الجدولة الحيّة

| العيّنة | الوقت | مفتاح الشمعة | البصمة | الصفوف |
|---|---|---|---|---|
| ١ | 00:22 | 23:45Z | `230cccd6a482` | 500 |
| ٢ | 00:24 | **00:00Z** | `3485cec72eb6` | 561 |
| ٣ | 00:26 | 00:00Z | `3485cec72eb6` | 561 |
| ٤ | 00:28 | 00:00Z | `3485cec72eb6` | 561 |
| ٥ | 00:30 | 00:00Z | `3485cec72eb6` | 561 |
| ٦ | 00:32 | 00:00Z | `3485cec72eb6` | 561 |
| ٧ | 00:34 | **00:15Z** | `10542be87a4c` | 563 |

- **أربعةُ أزواجٍ متتالية بفاصل دقيقتين داخل شمعةٍ واحدة: صفر اختلاف**
  في العضوية والترتيب والدرجة والتوافق والاتجاه وقيمة المسح.
- **و‎839‎ تغيّر سعرٍ من ‎1338‎ مقارنة** في نفس النافذة — فالثبات ليس
  سكونَ سوق.
- **والمفتاح تقدّم مرّتين فتغيّرت القائمة** — النظام حيٌّ لا جامد.
